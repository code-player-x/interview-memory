# Redis

> 题目数量：**75** ｜ 渲染时间：自动 ｜ 源：authored/redis.jsonl

---

## 1. 本地缓存与 Redis 组成多级缓存时，如何保持数据同步与最终一致？

> 原题 ID：`q0080`

**高频程度**：★★★★★

**考察点**：本地缓存与 Redis 多级缓存的同步策略、Cache-Aside、延时双删、MQ 广播失效。

**回答框架**：

1）写路径：更新 DB → 删 Redis → 广播失效消息 → 各节点删本地缓存；2）读路径：本地 → Redis → DB，逐级回填；3）防脏读：延时双删应对并发写，或订阅 binlog/CDC 统一失效；4）一致性等级：按业务选最终一致；5）反模式：写时同时双写本地+Redis 而不失效。

**参考回答**：

多级缓存里本地缓存（Caffeine）最快但各实例孤立，和 Redis 的同步我一般这样做。写路径：先更新数据库，再删除 Redis 缓存，然后通过消息队列广播失效消息，各应用节点收到后删除自己的本地缓存；读路径：本地缓存 → Redis → 数据库，逐级回填，回填时给本地缓存设较短 TTL 作为兜底。这里有两个关键点：一是为什么是"删缓存"而不是"更新缓存"，因为并发更新时双写顺序无法保证，容易出现旧值覆盖新值，删除更安全，下次读自然回源；二是怎么防止并发下的脏读，可以用延时双删——写库删缓存后，隔几百毫秒再删一次，把这段时间内被并发读回填的旧值清掉；更彻底的做法是订阅 binlog 或 CDC，由数据变更事件统一驱动各层失效，避免业务代码漏删。一致性等级上，多级缓存一般不追求强一致，按业务接受最终一致即可，关键是失效要快、要全覆盖。最容易踩的坑是写的时候同时更新本地和 Redis 而不失效，一旦其中一台机器更新失败或者广播丢失，脏数据会一直留着。

**常见追问**：1）延时双删的延时怎么定，能彻底解决吗？2）MQ 广播消息丢了怎么办？

---

## 2. Redis 大 Key 和热 Key 分别怎么处理？

> 原题 ID：`q0087`

**高频程度**：★★★★★

**考察点**：大 Key 与热 Key 的根因区分（体积 vs 频率）、危害与处理手段。

**回答框架**：

1）大 Key：value 过大或集合元素过多，危害是阻塞单线程、过期/删除卡顿、网络包大、迁移慢；处理：结构拆分（大 hash 分桶、大 list 分片）、压缩、异步 unlink、hscan 分批删；2）热 Key：访问集中，危害是单节点打满、读放大；处理：本地缓存短 TTL、key 加随机后缀复制多副本打散、请求合并、限流；3）根因不同处理不同。

**参考回答**：

大 Key 和热 Key 是两类问题，根因不同，一个是体积、一个是频率。大 Key 指 value 过大或集合元素过多，比如一个 100 万字段的 Hash、一个超长的 List。危害有三：Redis 单线程处理时序列化、网络传输会阻塞其他请求；过期和删除时释放内存卡顿；主从同步和集群迁移时会形成大包、迁移慢甚至超时。处理方式是结构拆分，比如把 100 万字段的 Hash 拆成 100 个 1 万字段的子 Hash 用 key 后缀分桶；大 List 分片成多个 key；能压缩就压缩；删除时用 unlink 异步删除，或者用 hscan 渐进式分批删除，避免一次 DEL 卡住。热 Key 指单个 key 的访问量极大，比如秒杀商品、明星微博。危害是压力集中到一个节点，单节点 CPU 或网卡打满，集群里其他节点却很闲。处理方式：客户端加本地缓存并设短 TTL 和随机过期时间，减少回源；把热 key 复制成 N 个副本、key 加随机后缀，把读请求打散到不同节点；入口做请求合并和限流；也可以在 Redis 代理层做统一的多级缓存。所以一句话总结：大 Key 治"拆"，热 Key 治"散"。

**常见追问**：1）怎么发现线上有大 Key 和热 Key？2）集群模式下热 key 打散后怎么写数据？

---

## 3. Redis所有数据类型的底层实现了解吗？

> 原题 ID：`q0369`

**高频程度**：★★★★

**考察点**：Redis 五大类型在不同规模下的编码（encoding）与底层数据结构。

**回答框架**：

1）String：int / embstr / raw（SDS）；2）List：3.2 前 ziplist+linkedlist，3.2 后 quicklist，7.0 用 listpack；3）Hash：ziplist/listpack 或 hashtable；4）Set：intset 或 hashtable；5）ZSet：ziplist/listpack 或 skiplist+字典；6）编码随数据规模自动转换，目的是省内存或提性能。

**参考回答**：

Redis 的所有数据类型的底层实现都基于高效的数据结构，每种类型会根据数据规模或特性选择不同的编码方式（`encoding`），以优化内存和性能。以下是详细解析：

**1. String（字符串）**

- **底层实现**：

  - **`int`**：存储 8 字节以内的长整型（如 `SET key 123`）。
  - **`embstr`**：短字符串（≤44 字节），内存连续分配，减少碎片。
  - **`raw`**：长字符串（>44 字节），动态分配内存（SDS，Simple Dynamic String）。
- **特点**：  SDS 结构包含长度字段，O(1) 获取字符串长度。支持动态扩容，避免缓冲区溢出。

**2. List（列表）**

- **底层实现**：

  - Redis 3.2之前：ziplist（元素少）+ linkedlist（元素多）
  - Redis 3.2-6.2：quicklist（双向链表 + ziplist）  -
  - Redis 7.0+：quicklist（双向链表 + listpack）
- 特点：

  - LPUSH/RPOP 时间复杂度 O(1)
  - quicklist兼顾了内存连续（listpack/ziplist）和插入删除效率（链表）

**3. Hash（哈希表）**

- **底层实现**：

  - **`ziplist`**：  字段少（默认 `hash-max-ziplist-entries 512`）且字段值小（默认 `hash-max-ziplist-value 64 字节`）。
  - `listpack`（7.0之后）：元素少。比 `ziplist` 更高效的内存布局。支持 **O(1) 的头部/尾部插入**，避免 `ziplist` 的级联更新问题。
  - **`hashtable`**（字典）：数据量大时使用，链式哈希解决冲突。
- **特点**：渐进式 rehash 避免阻塞。

**4. Set（集合）**

- **底层实现**：

  - **`intset`**：元素为**整数且数量少**（默认 `set-max-intset-entries 512`）。
  - **`hashtable`**：元素非整数或数量多时，使用字典（值设为 `NULL`）。
- **特点**：去重，支持交并差集操作（`SINTER`/`SUNION`）。

**5. ZSet（有序集合）**

- **底层实现**：

  - **`ziplist`（7.0之前）**：元素少（默认 `zset-max-ziplist-entries 128`）且元素小（默认 `zset-max-ziplist-value 64 字节`）。
  - `listpack`（7.0之后）：元素少。比 `ziplist` 更高效的内存布局。支持 **O(1) 的头部/尾部插入**，避免 `ziplist` 的级联更新问题。
  - **`skiplist + dict`**（跳表+字典）：跳表支持 O(logN) 范围查询（如 `ZRANGE`）。字典实现 O(1) 单元素查询（如 `ZSCORE`）。
- **特点**：跳表通过多层索引加速遍历。

记忆技巧：（黑哥SB)

**6. HyperLogLog（基数统计）**

- **底层实现**：特殊 String 编码，固定 12KB 内存，误差率 0.81%。
- **特点**：统计不重复元素（如 UV）。

**7. Geospatial（地理坐标）**

- **底层实现**：使用 ZSet 存储，坐标经度纬度编码为 `score`。
- **特点**：支持半径查询（`GEORADIUS`）。

**8. Stream（流）**

- **底层实现**：  **`radix tree`**（基数树）：存储消息列表，支持多消费者组。
- **特点**：类似 Kafka 的持久化消息队列。

**9. Bitmap（位图）**

- **底层实现**：基于 String 的 `raw` 编码，按位操作（`SETBIT`/`GETBIT`）。
- **特点**：节省空间，适合布尔统计（如用户签到）。

> 在 Redis 的 **Geospatial（地理坐标）** 类型中，虽然经度（longitude）和纬度（latitude）确实是两个独立的数值，但 Redis 通过 **GeoHash 编码** 将它们合并为一个 52 位整数作为 `ZSet` 的 `score`，从而利用有序集合的跳表结构实现高效的地理位置查询（如 `GEORADIUS`）。

**如何查看编码类型？**

在redis-cli中使用`OBJECT ENCODING key`

| **数据类型** | **底层实现** | **适用场景** |
|-|-|-|
| String | int / embstr / raw (SDS) | 缓存、计数器 |
| List | quicklist (ziplist + linkedlist) | 消息队列、最新列表 |
| Hash | ziplist / hashtable | 对象属性存储 |
| Set | intset / hashtable | 去重、集合运算 |
| ZSet | ziplist / skiplist + dict | 排行榜、延迟队列 |
| Bitmap | raw (String) | 布尔统计、位操作 |
| HyperLogLog | 特殊 String | 基数估算（UV/PV） |
| Stream | radix tree | 消息流（类似 Kafka） |

Redis 通过灵活选择编码方式，在内存和性能之间取得平衡。理解底层实现有助于优化使用（如控制 `ziplist` 的阈值）。

**常见追问**：1）ziplist 和 listpack 的区别是什么？2）编码转换是单向的吗，会转回来吗？

---

## 4. 什么是Redis？

> 原题 ID：`q0370`

**高频程度**：★★

**考察点**：Redis 的定义（Remote Dictionary Server）、支持的数据结构与功能特性。

**回答框架**：

1）全称 Remote Dictionary Server，基于内存的 KV 存储；2）数据结构：String/List/Hash/Set/ZSet，扩展 Bitmap/HyperLogLog/GEO/Stream；3）定位：可作数据库、缓存、消息中间件；4）内置能力：复制、Lua 脚本、LRU 淘汰、事务、持久化；5）高可用：哨兵与 Cluster。

**参考回答**：

Redis 的全称是 "Remote Dictionary Server"。Redis 是一个**基于内存**、**支持多种数据结构**的存储系统，可以作为**数据库**、**缓存**和**消息中间件**。它支持的数据结构有字符串（strings）、哈希（hashes）、列表（lists）、集合（sets）、有序集合（sorted sets）等，除此之外还支持 bitmaps、HyperLogLog 和地理空间（geospatial）索引半径查询等功能。

它内置了复制（Replication）、 LUA 脚本（Lua scripting）、 LRU 驱动事件（LRU eviction）、事务（Transactions）和不同级别的磁盘持久化（persistence）功能，并通过 Redis 哨兵（哨兵）和集群（Cluster）保证缓存的高可用性（High availability）。

**常见追问**：1）Redis 还适合当主数据库用吗？2）Stream 和 List 做消息队列有什么区别？

---

## 5. 为什么用Redis作为MySQL的缓存？

> 原题 ID：`q0372`

**高频程度**：★★★★

**考察点**：用 Redis 做 MySQL 缓存的原因、内存 vs 磁盘 IO、QPS 提升与一致性问题。

**回答框架**：

1）MySQL 数据在磁盘，查询要走磁盘 IO，复杂查询代价高；2）Redis 数据在内存，微秒级返回，避免磁盘 IO；3）缓存热点数据能显著提升 QPS、降低 DB 压力；4）代价与风险：一致性、缓存穿透击穿雪崩、内存成本；5）典型架构：Cache-Aside，读多写少场景收益最大。

**参考回答**：

**MySQL**是数据库系统，对于**数据的操作需要访问磁盘**，而将数据放在Redis中，访问就可以直接从内存获取，避免磁盘I/O，提高操作的速度，使用Redis+MySQL结合的方式可以有效提高系统QPS。

**常见追问**：1）为什么是删除缓存而不是更新缓存？2）什么样的数据不适合放缓存？

---

## 6. Redis 常用的业务场景有哪些？

> 原题 ID：`q0373`

**高频程度**：★★★★

**考察点**：Redis 常见业务场景：缓存、限时业务、分布式锁、计数器、排行榜、消息队列，以及相关命令与原子性依据。

**回答框架**：

1）热点数据缓存；2）限时业务：expire 做验证码、优惠活动；3）分布式锁：SET NX EX、Lua 释放；4）计数器：INCR/INCRBY 原子递增，用于秒杀、限流、序列号；5）排行榜：ZSet；6）消息队列：List 的 push/pop 或 Stream；7）会话与去重：Set、Bitmap、HyperLogLog。

**参考回答**：

主要常用的业务场景有：**（限时锁队列，地里热鸡排）**

- 对**热点数据的缓存**；因为 Redis 支持多种数据类型，数据存储在内存中，访问速度快，所以 Redis 很适合用来存储热点数据；
- **限时类业务的实现**；可以使用 expire 命令设置 key 的生存时间，到时间后自动删除 key，例如使用在**验证码验证**、**优惠活动**等业务场景；
- **分布式锁**实现；可以利用 Redis 的 setnx 命令进行。
- **计数器**的实现；因为 incrby 命令可以实现原子性（**因为redis是单线程**）的递增，所以可以运用于**高并发的秒杀活动**、分布式序列号的生成。例如限制一个手机号发多少条短信、一个接口一分钟限制多少请求、一个接口一天限制调用多少次等业务场景。（**因为redis是单线程**）
- **排行榜**的实现；借助 Sorted Set 进行热点数据的排序。例如：下单量最多的用户排行榜，最热门的帖子（回复最多）等业务场景；
- **消息队列**实现：

  - **队列机制**实现；Redis 提供了 list push 和 list pop 这样的命令，所以能够很方便的执行队列操作。
  - **发布/订阅模式**：支持发布/订阅功能，可用于事件驱动架构中不同组件之间的通信。

  ```Shell
  # 订阅名为 "news" 和 "updates" 的通道
  SUBSCRIBE news updates

  # 订阅符合 "chat.*" 模式的通道
  PSUBSCRIBE chat.*

  # 向 "news" 通道发布消息 "Breaking news: ..."
  PUBLISH news "Breaking news: ..."

  # 向 "updates" 通道发布消息 "System will be down for maintenance"
  PUBLISH updates "System will be down for maintenance"

  # 查看活跃的通道
  PUBSUB CHANNELS

  # 获取 "news" 和 "updates" 通道的订阅者数量
  PUBSUB NUMSUB news updates

  # 获取模式订阅者的数量
  PUBSUB NUMPAT
  ```
- **地理空间索引**：Redis从版本3.2开始引入了地理空间索引功能，允许你**存储地理位置**并执行**距离相关的查询。**

**常见追问**：1）用 List 做队列有什么缺点？2）秒杀库存扣减怎么保证不超卖？

---

## 7. Redis如何保证访问安全？

> 原题 ID：`q0374`

**高频程度**：★★

**考察点**：Redis 访问安全措施：密码认证、绑定 IP、TLS、防火墙、ACL、保护模式与危险命令禁用。

**回答框架**：

1）requirepass 设置密码认证，客户端 AUTH；2）bind 绑定内网 IP，不暴露公网；3）TLS 加密通信；4）防火墙/安全组限制来源；5）Redis 6+ ACL 做细粒度用户与命令权限；6）禁 rename 危险命令（FLUSHALL、KEYS、CONFIG）、开启 protected-mode。

**参考回答**：

**回答**

为了保证 Redis 的访问安全，可以综合使用以下措施：
1. 设置密码认证。
2. 绑定访问 IP。
3. 启用 TLS 加密通信。
4. 使用防火墙限制访问。
5. 使用 ACL 控制权限。

**推荐学习**

> Redis 是一个高性能的内存数据库，广泛应用于缓存、消息队列等场景。为了保证 Redis 的访问安全，可以采取以下措施：
>
> **1. 设置密码认证（AUTH）**
>
> Redis 提供了简单的密码认证机制，可以通过配置文件或命令行设置密码，客户端连接时需要提供密码才能访问。
>
> - **配置方法**：在 Redis 配置文件 `redis.conf` 中设置：`requirepass yourpassword`或者在运行时通过命令设置：`CONFIG SET requirepass yourpassword`
> - **客户端连接**：客户端连接时需要提供密码：`AUTH yourpassword`
> - **注意事项**：
>
>   - 密码以明文形式存储在配置文件中，需确保配置文件的安全性。
>   - 密码认证只能防止未授权访问，不能加密通信内容。
>
> **2. 绑定访问 IP**
>
> 通过配置 `bind` 参数，限制 Redis 只监听特定的 IP 地址，避免暴露在公网。
>
> - **配置方法**：在 `redis.conf` 中设置：`bind 127.0.0.1`，这样 Redis 只接受来自本地的连接。
> - **注意事项**：如果 Redis 需要被其他服务器访问，可以绑定内网 IP。不要将 `bind` 设置为 `0.0.0.0`，否则会暴露在公网。
>
> **3. 启用 TLS 加密通信**
>
> Redis 从 6.0 版本开始支持 TLS（Transport Layer Security），可以通过 TLS 加密客户端与服务器之间的通信，防止数据被窃听或篡改。
>
> - **配置方法**：
>
>   1. 生成 TLS 证书和私钥。
>   2. 在 `redis.conf` 中配置：
>
>      ```Plain Text
>      tls-port 6379
>      tls-cert-file /path/to/redis.crt
>      tls-key-file /path/to/redis.key
>      ```
> - **客户端连接**：客户端需要使用 TLS 连接 Redis。
>
> **4. 使用防火墙限制访问**
>
> 通过防火墙（如 `iptables` 或云服务商的安全组）限制 Redis 端口的访问范围，只允许特定的 IP 或 IP 段访问。使用 `iptables` 限制只允许特定 IP 访问 Redis 端口（默认 6379）：
>
> ```Bash
> iptables -A INPUT -p tcp --dport 6379 -s 192.168.1.0/24 -j ACCEPT
> iptables -A INPUT -p tcp --dport 6379 -j DROP
> ```
>
> **5. 启用保护模式**
>
> Redis 的保护模式（protected mode）可以防止未绑定 IP 或未设置密码的 Redis 实例被外部访问。
>
> - **配置方法**：在 `redis.conf` 中设置：`protected-mode yes`
> - **注意事项**：如果绑定了 IP 或设置了密码，保护模式会自动关闭。
>
> **6. 使用 ACL（访问控制列表）**
>
> Redis 6.0 引入了 ACL 功能，可以为不同用户设置不同的权限。
>
> - **配置方法**：
>
>   1. 创建用户并设置权限：`ACL SETUSER alice on >password ~cached:* +get +set`
>   2. 客户端连接时使用用户名和密码：`AUTH alice password`
> - **注意事项**：ACL 可以细粒度地控制用户对键和命令的访问权限。

**常见追问**：1）为什么说 bind 和防火墙比密码更重要？2）ACL 怎么限制一个用户只能读某些前缀的 key？

---

## 8. Redis 的数据类型有哪些？

> 原题 ID：`q0375`

**高频程度**：★★★★

**考察点**：Redis 数据类型总览及各自典型用途。

**回答框架**：

1）五种基础类型：String、List、Hash、Set、ZSet，各说一句用途；2）扩展类型：Bitmap、HyperLogLog、GEO、Stream；3）按场景选型：缓存/计数用 String、队列用 List/Stream、对象用 Hash、去重/交并用 Set、排行用 ZSet。

**参考回答**：

**分析**

Redis 提供了丰富的数据类型，常见的有五种数据类型：String（字符串），Hash（哈希），List（列表），Set（集合），Zset（有序集合）。

</sheet>

随着 Redis 版本的更新，后面又支持了四种数据类型： BitMap (2.2 版新增)、HyperLogLog (2.8 版新增)、GEO (3.2 版新增) 、Stream (5.0 版新增) 。

Redis 五种数据类型的应用场景：

- String 类型的应用场景：缓存对象、常规计数、分布式锁、共享 session 信息等。
- List 类型的应用场景：消息队列（但是有两个问题：1. 生产者需要自行实现全局唯一 ID；2. 不能以消费组形式消费数据）等。
- Hash 类型：缓存对象、购物车等。
- Set 类型：聚合计算（并集、交集、差集）场景，比如**点赞、共同关注、抽奖活动**等。
- Zset 类型：排序场景，比如排行榜、电话和姓名排序等。

Redis 后续版本又支持四种数据类型，它们的应用场景如下：

- BitMap (2.2 版新增) ：二值状态统计的场景，比如**签到、判断用户登陆状态、连续签到用户总数**等；
- HyperLogLog (2.8 版新增) ：海量数据基数统计的场景，比如百万级网页 UV 计数（**UV (Unique Visitor)**: 指的是独立访客的数量）等；
- GEO (3.2 版新增) ：存储地理位置信息的场景，比如滴滴叫车；
- Stream (5.0 版新增) ：消息队列，相比于基于 List 类型实现的消息队列，有这两个特有的特性：自动生成全局唯一消息ID，**支持以消费组形式消费数据**。

  > - 添加消息
  >
  > ` XADD mystream * field1 value1 field2 value2`
  >
  > - 读取消息
  >
  > ` XREAD COUNT 1 STREAMS mystream $`
  >
  > - 创建消费者组
  >
  > ` XGROUP CREATE mystream mygroup $ MKSTREAM`
  >
  > - 从消费者组中读取消息
  >
  > `XREADGROUP GROUP mygroup consumer1 COUNT 1 STREAMS mystream >`
  >
  > Redis Streams 提供了一个强大且灵活的消息传递解决方案，适用于多种应用场景，并且随着 Redis 的不断发展，其功能也在持续增强。

面试能回答出五种常见的数据类型就可以，其他新的数据类型则按需学习。

**回答**

Redis 支持的数据类型有：

</sheet>

**推荐学习**

Redis 常见数据类型和应用场景

**常见追问**：1）什么场景应该选 Hash 而不是 String？2）Stream 相比 List 做队列的优势是什么？

---

## 9. Set一个已有的数据会发生什么？

> 原题 ID：`q0377`

**高频程度**：★★★

**考察点**：SET 命令覆盖已有 key 的三种效果：覆盖值、擦除过期时间、可以覆盖不同类型。

**回答框架**：

1）覆盖原有值，旧值直接丢弃；2）擦除原有 TTL，key 变回永久；3）可以跨类型覆盖（List 被 String 覆盖），等价于隐式 DEL 再 SET；4）例外：不带 KEEPTTL 时过期时间清空，SET ... KEEPTTL 可保留；5）实践：改值想保留 TTL 要用 SET KEEPTTL 或 SETEX。

**参考回答**：

**回答**

1. 覆盖原有值 ✅
2. 擦除过期时间 ✅
3. 可以覆盖不同类型（如list被string覆盖）✅

如果是同种类型，会覆盖原有的值，同时会覆盖或者擦除键的过期时间。

`set` 命令还可以直接把 `key` 抹除，例如原本有个列表键 `mylist`，可以直接 `set mylist abc` 来覆盖这个列表键。类似隐式的 `del` (或者 `unlink`异步删除) `mylist` + `set mylist abc`。

**推荐学习**

string基本操作命令，重点熟悉‌﻿‬‌‌‬‍‌‬﻿‍‍⁠String 中提到的命令)。更多命令可以从Redis 字符串(String)了解

**常见追问**：1）KEEPTTL 和 SETEX 该用哪个？2）不同类型覆盖会有性能问题吗？

---

## 10. 浮点型在String是用什么表示？

> 原题 ID：`q0378`

**高频程度**：★★

**考察点**：浮点数在 String 中的存储方式：转字符串后按长度选 embstr 或 raw。

**回答框架**：

1）浮点数先转成字符串，再作为 SDS 存储；2）编码取决于转换后的字符串长度：≤44 字节用 embstr，>44 字节用 raw；3）44 字节阈值来自 jemalloc 64 字节分配档与 redisObject 开销；4）所以浮点不是原生存储，INCR 会报错，要用 INCRBYFLOAT。

**参考回答**：

Redis 的 String 保存的是二进制安全字节序列；当内容可解析为整数时，内部可能使用整数编码，其他数值通常仍以字符串表示。`INCRBYFLOAT` 由 Redis 按其数值语义解析和格式化，客户端不应把它等同于某一种固定的 IEEE 754 内存布局。

涉及金额、精确计数或跨系统对账时，应明确小数精度和舍入规则，常见做法是使用最小货币单位的整数或固定精度 decimal 方案，而不是依赖浮点字符串的显示结果。

**常见追问**：为什么金额累计通常更适合以分为单位的整数保存？

---

## 11. Redis String 的内存占用为何与版本、位宽和分配器有关？如何正确评估？

> 原题 ID：`q0380`

**高频程度**：★★★

**考察点**：String 的三种编码 INT / EMBSTR / RAW 及其转换条件。

**回答框架**：

1）INT：能用 long 表示的整数，直接存数值；2）EMBSTR：字符串长度 ≤44 字节，一次内存分配、只读；3）RAW：长度 >44 字节，两次分配，修改会原地更新；4）转换：EMBSTR 修改后变 RAW，append 超阈值也转 RAW；5）目的：省内存、减少碎片。

**参考回答**：

Redis String 的实际内存占用取决于 Redis 版本、对象编码、64/32 位构建、jemalloc 等分配器、键名长度和过期元数据。网上常见的“44 字节”只是在特定旧版本、64 位和特定分配器下的示例，不能当作所有实例的固定结论。

容量规划应在目标版本和真实 key/value 分布上测量，例如采样使用 `MEMORY USAGE key`、`MEMORY STATS`、RDB/AOF 大小和业务指标；同时考虑小对象分配粒度、哈希表负载、复制缓冲和碎片率。

**常见追问**：为什么仅按 value 的字节长度估算 Redis 内存，往往会低估实际占用？

---

## 12. SDS有什么用？

> 原题 ID：`q0383`

**高频程度**：★★★★

**考察点**：SDS 相对 C 字符串的三大优势：O(1) 取长度、预留空间减少重分配、二进制安全。

**回答框架**：

1）O(1) 长度：SDS 有 len 字段，C 字符串要 O(n) 遍历；2）空间预分配与惰性释放：扩容时多分配、缩容时不立即回收，减少内存重分配；3）二进制安全：以 len 判断结束而非 \0，可存任意二进制；4）避免缓冲区溢出：扩容前检查容量；5）兼容 C 字符串函数。

**参考回答**：

**分析**

Redis是用C语言写的，SDS可以说是C字符串的封装，一般对比普通C字符串。可以从计算长度、扩容、缩容、二进制存储这几个场景来描述。

**回答**

主要有三点：

1. SDS包含已使用容量字段，O(1)时间快速返回字符串长度。相比之下，C原生字符串需要O(n)。
2. 有预留空间，在扩容时如果预留空间足够，就不用再重新分配内存，节约性能。缩容时也可以将减少的空间先保留下来，后续可以再使用。
3. 不再以`'\0'`作为判断标准，二进制安全，可以很方便地存储一些二进制数据。

**推荐学习**

《Redis学习与面试》 String

**常见追问**：1）SDS 扩容的预分配策略具体是什么？2）为什么说 C 字符串不二进制安全？

---

## 13. List是完全先进先出吗？

> 原题 ID：`q0384`

**高频程度**：★★★

**考察点**：List 是双端操作结构，支持 FIFO 也支持 LIFO。

**回答框架**：

1）List 是双端结构，头尾都能 O(1) 插入删除；2）LPUSH+RPOP 是先进先出（队列）；3）LPUSH+LPOP 是后进先出（栈）；4）LPUSH+LRANGE 取最新 N 条；5）不是严格 FIFO，取决于用哪两端。

**参考回答**：

**分析**

题目所说的完全先进先出，很明显就是指只允许从尾部入队、从头部出队，而List是一个双端操作对象，所以不是完全先进先出，他也可以后入先出。

这道题就是考察是否对LIST有最基本的认识。

**回答**

List是双端操作对象，所以不是完全的先进先出，List也可以后入先出。

**常见追问**：1）List 和 Stream 做队列怎么选？2）BRPOP 阻塞取数据有什么坑？

---

## 14. List对象底层编码方式是什么？

> 原题 ID：`q0385`

**高频程度**：★★★★

**考察点**：List 编码演进：3.2 前 ziplist/linkedlist，3.2+ quicklist（内部 listpack）。

**回答框架**：

1）3.2 之前：元素少且短用 ziplist，否则 linkedlist；2）3.2 之后统一 quicklist = 双向链表 + ziplist；3）7.0 起 ziplist 被 listpack 取代（quicklist 内部为 listpack）；4）quicklist 兼顾内存紧凑与操作效率，可配置每个节点的大小；5）意义：避免 ziplist 连锁更新和大链表内存碎片。

**参考回答**：

**分析**

在Redis3.2版本之前和之后，List对象的底层编码方式是不同的。面试的时候如果能对不同版本Redis的List对象底层编码方式做出分析，是一个加分项。

**回答**

在3.2版本之前，List对象的编码是ZIPLIST和LINKEDLIST。ZIPLIST适用于元素数量较少、且元素都较短的情况，否则用LINKEDLIST。

3.2版本之后，List对象的编码全部由QUICKLIST实现。QUICKLIST是一个压缩列表组成的双向链表，结合了ZIPLIST和LINKEDLIST两者的优点，在后面比较新的版本（Redis 7.0）中ZIPLIST优化为了LISTPACK。

**推荐学习**

《Redis学习与面试》 List

**常见追问**：1）ziplist 的连锁更新是什么问题？2）quicklist 每个节点的容量怎么调？

---

## 15. ZIPLIST是怎么压缩数据的？

> 原题 ID：`q0386`

**高频程度**：★★★

**考察点**：ziplist 的压缩原理：连续内存、双端链表语义、变长编码。

**回答框架**：

1）一块连续内存，元素紧挨存储，无指针开销；2）语义上是双端链表，可实现前后遍历；3）每个 entry 用变长编码（prevlen/encoding/内容），按数据大小选择最短表示；4）对比普通链表：省指针、省碎片、缓存友好；5）缺点：插入删除可能触发内存移动和连锁更新。

**参考回答**：

**分析**

这个问题就是对ZIPLIST数据结构本身的考察。ZIPLIST是为了节约内存而开发的，回答这个问题可以从ZIPLIST的结构入手。下面是ZIPLIST的结构：

```C
* The general layout of the ziplist is as follows:
*
* <zlbytes> <zltail> <zllen> <entry> <entry> .. <entry> <zlend>
```

其中entry结构为：

<prevlen> <encoding> <entry-data>

为了方便记忆和理解，可以将ZIPLIST分为三部分讲述：

1. 结构头，即header，包括<zlbytes> <zltail> <zllen>字段。
2. 数据部分，即entry列表，entry中有 <prevlen> <encoding> <entry-data>字段。
3. 结尾标识，即zlend。

**回答**

压缩列表是一块连续的内存空间，元素之间是紧挨着存储的。一个压缩列表中可以包含多个节点（entry），是在连续的内存空间上实现的双端链表。对于一个普通的双向链表，链表中每一项都占用独立的一块内存，各项之间用地址指针（或引用）连接起来，这种方式会带来大量的内存碎片，而且地址指针也会占用额外的内存。而ziplist却是将表中每一项存储在前后连续的地址空间内，一个ziplist整体占用一大块内存。

而且ziplist entry对于不同类型 会有不同长度的数据存储，保证尽可能的压缩内存

**推荐学习**

《Redis学习与面试》 List

**常见追问**：1）listpack 怎么解决连锁更新？2）ziplist 适合存多长的元素？

---

## 16. ZIPLIST下List可以从后往前遍历吗？

> 原题 ID：`q0387`

**高频程度**：★★

**考察点**：ziplist 反向遍历的实现：靠每个节点的 prevlen 字段回退。

**回答框架**：

1）可以反向遍历，因为 List 是双端结构；2）ziplist 每个节点保存了前一个节点的长度（prevlen）；3）用当前节点地址减去 prevlen 得到上一个节点的起始位置；4）因此不需要额外指针；5）代价：prevlen 变化可能触发连锁更新。

**参考回答**：

**分析**

首先，一切的基石，在于要知道List是一种双端数据结构，无论哪种底层编码，都需要能支持从后往前遍历。

接着，需要阐述ZIPLIST是如何做到从后往前遍历的，其实还是在考察ZIPLIST的数据结构。

ZIPLIST下entry的结构包含了上一个节点的长度，所以可以通过上一个节点的长度，找到上个节点的起始位置，这样就能实现从后往前遍历。

**回答**

可以，List是双端数据结构，无论哪种底层编码，都需要能支持从后往前遍历。ZIPLIST每个节点中都保存了上一个节点的长度，所以可以用当前节点地址减去上一个节点长度来找到上个节点起始位置，进而实现从后往前的遍历。

**推荐学习**

《Redis学习与面试》 List

**常见追问**：1）为什么不用固定长度存 prevlen？2）zltail 字段有什么用？

---

## 17. 在ZIPLIST数据结构下，查询节点个数的时间复杂度是多少？

> 原题 ID：`q0388`

**高频程度**：★★

**考察点**：ziplist 查询节点个数：头部记录 + 超过 65535 后退化 O(n)。

**回答框架**：

1）正常情况下 O(1)：ziplist header 有 zllen 字段记录节点数；2）限制：zllen 只有 2 字节，最大 65535；3）超过后 zllen 不再准确，需要 O(n) 遍历统计；4）面试点：知道"O(1) 但有上限"这个细节。

**参考回答**：

**分析**

由于ZIPLIST的header中定义了记录节点数量的字段zllen，所以通常是在O(1)时间复杂度直接返回。为什么说通常呢？是因为zllen是2个字节的。当zllen大于65535时，zllen就存不下了，所以真实的节点数量需要遍历来得到。

**回答**

在ZIPLIST编码下，查询节点个数的时间复杂度是O(1)，因为ZIPLIST中的header定义了记录节点数量的字段，但是这里也有个限制。记录节点数量的字段只有2字节，也就说如果节点数量超过65535，就失效了，此时只能通过O(n)复杂度的遍历来查节点总数。

**推荐学习**

《Redis学习与面试》 List

**常见追问**：1）为什么 zllen 用 2 字节而不是 4 字节？2）切换到 hashtable 后数量统计为什么是 O(1)？

---

## 18. Set是有序的吗？

> 原题 ID：`q0391`

**高频程度**：★★★

**考察点**：Set 的有序性：intset 有序、hashtable 无序，业务上应按无序使用。

**回答框架**：

1）intset 底层是有序数组，所以有序；2）hashtable 无序，遍历顺序不保证；3）业务不应依赖 Set 顺序，因为编码会随规模变化；4）要顺序需求应改用 List 或 ZSet；5）SMEMBERS 的顺序不可依赖。

**参考回答**：

**分析**

Set的底层实现是整数集合或字典，前者是有序的，后者是无序的。

**回答**

Set的底层实现是**整数集合**或**字典**，前者是有序的，后者是无序的。整体来看，但是不应该依赖SET的顺序，业务使用适合始终应该按无序来用

**推荐学习**

Set

**常见追问**：1）SSCAN 的游标机制是怎么保证不丢元素的？2）Set 交集在 intset 下有优化吗？

---

## 19. Set为什么要用两种编码方式？

> 原题 ID：`q0392`

**高频程度**：★★★★

**考察点**：Set 两种编码的取舍：intset 省内存 vs hashtable 查找快。

**回答框架**：

1）小且全整数用 intset：紧凑、省内存、二分查找；2）规模大或含非整数用 hashtable：O(1) 查找、支持任意元素；3）目的：小数据场景内存优先，大数据场景性能优先；4）转换由 set-max-intset-entries 控制；5）体现 Redis 编码自适应的设计思想。

**参考回答**：

**分析**

我们可以从编码转换的条件来进行思考，Set的底层编码从INTSET到HASHTABLE的条件是元素个数或者元素类型的的变化。所以采用两种编码方式的原因是INTSET更节约内存，所以在小数据量时使用，而数据多起来了，需要HASHTABLE的查找性能。

实际上如果Set中保存的所有元素都是整数，而且元素个数不是特别多的情况下，使用intset会比较节约内存，Redis用一个含有三个字段的结构体来表示intset，分别是编码方式、元素数量和实际存储元素的有序柔性数组:

```C
typedef struct intset {
    uint32_t encoding;   //编码格式
    uint32_t length;     //元素数量
    int8_t contents[];   //保存元素的数组
} intset;
```

intset用来保存元素的数组默认情况下是int16编码，后续如果插入更大的整数，才会升级到int32或者int64编码，这种策略可以尽可能的节约内存以及提升整数集合的灵活性。

但是升级也有弊端，升级之后，整个数组的编码会变成与最大元素的类型一致。假如这个时候，元素的数量非常多，就不那么节约内存了，而且数组查找的平均时间复杂度是 O(logn)，不如使用字典编码。

**回答**

Set的底层编码是**整数集合**和**字典**，当元素数量小并且全部是整数的时候，会使用整数集合编码，更加的节约内存。元素**数量变大会使用字典编码**，查找元素的速度会更快。

**推荐学习**

Set

> 在 Redis 中，`set` 数据结构用于存储无序的、唯一的元素集合。然而，Redis 内部实现 `set` 时，并不是一直使用同一种数据结构。实际上，Redis 的 `set` 可以根据情况选择两种不同的底层实现方式：当 `set` 中的元素数量较少且每个元素都是整数（或可以转换为整数）时，它会使用 `intset`（整数集合），而当不满足这些条件时，则会使用哈希表。
>
> **IntSet (整数集合)**
>
> `intset` 是一个紧凑型的数据结构，专门用来存储整数值。它具有以下特点：
>
> 1. **有序性**：`intset` 中的元素是有序的。这意味着当你向 `intset` 添加新的整数时，Redis 会确保所有元素都按照从小到大的顺序排列。这种排序特性使得查找和删除操作可以通过二分查找算法来高效执行。
> 2. **去重性**：由于 `intset` 是基于二叉搜索树或者数组实现的（具体取决于 Redis 版本），并且内部元素保持有序，所以它可以很容易地保证成员的唯一性。尝试插入已存在的元素不会改变 `intset` 的状态。
> 3. **内存效率**：为了节省内存，`intset` 使用了不同长度的整数类型（`int16_t`、`int32_t` 或 `int64_t`）来存储值，这取决于集合中最大的整数值。如果添加了一个更大范围的数字，整个集合将升级到更大的整数类型。
> 4. **性能优化**：由于 `intset` 的有序性和紧凑性，对于小规模的数据集，它的插入、删除和查找操作都非常快。
>
> **当 Set 转换为哈希表**
>
> 随着 `set` 中元素数量的增长或当元素不再是整数时，Redis 会自动将 `intset` 转换为哈希表。哈希表中的键是元素本身，值通常是一个简单的占位符（如 `NULL`）。这样做是为了保持 `set` 操作的时间复杂度接近 O(1)，即使是在较大的集合上也能提供高效的插入、删除和查找性能。
>
> **总结**
>
> - `intset` 是 Redis 内部用于表示小规模整数集合的一种高效的数据结构。
> - `intset` 中的元素是有序且唯一的。
> - 对于非整数元素或较大规模的集合，Redis 会切换到使用哈希表作为 `set` 的底层实现。
>
> 因此，如果你在 Redis 中使用 `set` 来存储整数，并且集合大小保持在一个较小范围内，那么你可以期望它们是以有序的方式存储在 `intset` 中的。不过，一旦超出了一定界限或包含非整数元素，Redis 将采用哈希表来管理 `set`，此时集合内的元素将不再保持显式的顺序。

**常见追问**：1）为什么 intset 查找是 O(logN) 而不是 O(1)？2）大数据量 Set 内存怎么优化？

---

## 20. Hash的编码方式是什么？

> 原题 ID：`q0393`

**高频程度**：★★★

**考察点**：Hash 的编码：ziplist/listpack 与 hashtable 及切换阈值。

**回答框架**：

1）小数据用 ziplist（7.0 后 listpack）：字段少且值短时省内存；2）大数据用 hashtable：字段多或值长时保证 O(1)；3）阈值：hash-max-ziplist-entries（默认 512）、hash-max-ziplist-value（默认 64 字节）；4）转换单向不可逆；5）实践：大 Hash 应分桶拆分。

**参考回答**：

Redis Hash 会根据元素数量、字段和值的大小选择紧凑编码或普通哈希表。较新的 Redis 使用 listpack 作为紧凑表示，并由 `hash-max-listpack-*` 等配置控制阈值；旧版本可能使用 ziplist，因此不能把 ziplist 名称或阈值当作当前通用事实。

紧凑编码节省小对象内存，但在元素变大或数量增多后会转换为更适合查找和更新的哈希表。排障和容量设计应查看目标 Redis 版本、配置与 `OBJECT ENCODING` 的实际结果。

**常见追问**：为什么紧凑编码的 Hash 在字段数变大后可能自动转换？

---

## 21. Hash查找某个key的平均时间复杂度是多少？

> 原题 ID：`q0394`

**高频程度**：★★★

**考察点**：Hash 查找单个 field 的复杂度随编码变化：ziplist O(N)，hashtable O(1)。

**回答框架**：

1）ziplist/listpack 编码：连续内存顺序遍历，O(N)；2）hashtable 编码：哈希定位，平均 O(1)，最坏 O(N)（哈希冲突严重）；3）判断方法：OBJECT ENCODING；4）实践：字段多的 Hash 要预期 O(N) 退化；5）大 Hash 拆分。

**参考回答**：

**分析**

这种问对象复杂度的，要考虑多种底层编码。ZIPLIST需要遍历，平均复杂度为O(N)，HashTable是字典，可以O(1)找到对应key。

**回答**

Hash有两种底层结构，ZIPLIST时是O(N)，HashTable则是O(1)

**推荐学习**

Hash

**常见追问**：1）哈希冲突在 Redis 里怎么解决？2）rehash 期间查找会怎么做？

---

## 22. Redis中HashTable查找元素总数的平均时间复杂度是多少？

> 原题 ID：`q0395`

**高频程度**：★★

**考察点**：hashtable 统计键值对数量 O(1) 的原因：dict 表头的 used 字段。

**回答框架**：

1）dict 结构中有 used 字段记录已存键值对数量；2）查询总数直接返回该字段，O(1)；3）与 ziplist 的 zllen 类似，但无 65535 限制；4）对照：ziplist 需遍历；5）体现"元数据换性能"的常见设计。

**参考回答**：

**分析**

这题考察的是Redis字典的表头结构，如果表头结构中有储存键值对个数的字段，那么查找元素总数的平均时间复杂度就是O(1)，而如果没有这个字段，那字典就需要去遍历所有的键值对。

下面是Redis字典的表头结构：

```C
// from Redis 5.0.5
typedef struct dict {
    dictEntry **table;
    unsigned long size;
    unsigned long sizemask;
    // 储存对象数量
    unsigned long used;
} dictht;
```

可以发现字典的表头结构中的used，就记录了当前键值对数量的字段。

**回答**

HashTable查找元素总数的平均时间复杂度是O(1)，因为HashTable的**表头结构中有储存键值对数量的字段**，这个字段我记得叫used。

**推荐学习**

底层结构HASHTABLE

**常见追问**：1）dict 里还有哪些关键字段？2）rehash 时 used 怎么统计？

---

## 23. 一个数据在HashTable中的存储位置，是怎么计算的？

> 原题 ID：`q0396`

**高频程度**：★★★

**考察点**：hashtable 的索引计算：hash 函数求哈希值，与 sizemask 做按位与得到索引。

**回答框架**：

1）先用哈希函数（SipHash 等）计算 key 的哈希值；2）与哈希掩码 sizemask（表大小 - 1）按位与得到索引；3）要求表大小是 2 的幂，这样取模可以用按位与加速；4）解决冲突用链地址法，新节点插入链表头；5）扩容时表大小为 2 的幂同样保证该计算成立。

**参考回答**：

**分析**

当一个新的键值对要插入到HashTable中时，首先会使用哈希函数计算这个key的哈希值，Redis使用的哈希算法是MurmurHash2哈希算法，然后把**哈希值和哈希掩码做与运算得到索引值**，哈希掩码其实就是哈希表数组的大小减去1，然后程序会根据索引值把键值对插入到相应的位置。

**回答**

首先会通过哈希函数计算出key的哈希值，然后与哈希掩码做与运算得到索引值，索引值就是这个数据在HashTable中的存储位置。

**推荐学习**

<cite doc-id="wikcnf09sLEEIaQb4IU6VptnxEc" file-type="wiki" title="底层结构HASHTABLE" type="doc"></cite>

**常见追问**：1）为什么要保证表大小是 2 的幂？2）Redis 怎么做哈希表的扩容和 rehash？

---

## 24. HashTable怎么扩容？

> 原题 ID：`q0397`

**高频程度**：★★★★

**考察点**：hashtable 扩容流程：分配 1 号表、渐进式 rehash（rehashidx 推进）、完成后表切换。

**回答框架**：

1）为 1 号表分配空间，大小是第一个大于等于 0 号表大小 × 2 的 2 的幂；2）进入 rehash 状态，rehashidx 从 0 开始；3）每次增删改查都顺带迁移 rehashidx 位置的数据，然后 rehashidx 加 1；4）全部迁移完后把 1 号表设为 0 号表，再新建空白 1 号表；5）渐进式避免一次性迁移阻塞。

**参考回答**：

**分析**

HashTable的扩容通过渐进式rehash操作来完成。

**回答**

首先程序会为HashTable的1号表分配空间，空间大小是第一个大于等于0号表大小\*2的2 ^ n。在rehash进行期间，标记位rehashidx从0开始，每次对字典的键值对执行增删改查操作后，都会将rehashidx位置的数据迁移到1号表，然后将rehashidx加1，随着字典操作的不断执行，最终0号表的所有键值对都会被rehash到1号表上。之后，1号表会被设置成0号表，接着在1号表的位置创建一个新的空白表。

**推荐学习**

<cite doc-id="wikcnf09sLEEIaQb4IU6VptnxEc" file-type="wiki" title="底层结构HASHTABLE" type="doc"></cite>

**常见追问**：1）rehash 期间删除操作怎么处理？2）如果长时间没有操作，rehash 会一直不完成吗？

---

## 25. HashTable怎么缩容？

> 原题 ID：`q0398`

**高频程度**：★★★

**考察点**：hashtable 缩容流程：1 号表大小为第一个大于等于 used 的 2 的幂，同样渐进式 rehash。

**回答框架**：

1）为 1 号表分配空间，大小是第一个大于等于原表 used 的 2 的幂；2）标记 rehashidx，进入渐进式 rehash；3）每次字典操作迁移 rehashidx 位置的数据并递增；4）迁移完成后 1 号表变 0 号表，再建空白表；5）触发条件：负载因子小于 0.1。

**参考回答**：

**分析**

HashTable的缩容也通过渐进式rehash操作来完成。

**回答**

首先程序会为HashTable的1号表分配空间，新表大小为第一个大于等于原表used的2次方幂。在rehash进行期间，标记位rehashidx从0开始，每次对字典的键值对执行增删改查操作后，都会将rehashidx位置的数据迁移到1号表，然后将rehashidx加1，随着字典操作的不断执行，最终0号表的所有键值对都会被rehash到1号表上。之后，1号表会被设置成0号表，接着在1号表的位置创建一个新的空白表。

**推荐学习**

<cite doc-id="wikcnf09sLEEIaQb4IU6VptnxEc" file-type="wiki" title="底层结构HASHTABLE" type="doc"></cite>

**常见追问**：1）为什么缩容阈值和扩容阈值差这么多？2）缩容期间会不会又有新写入导致反复？

---

## 26. 什么时候扩容，什么时候缩容？

> 原题 ID：`q0399`

**高频程度**：★★★★

**考察点**：哈希表扩缩容的触发条件：负载因子与是否在执行 BGSAVE/BGREWRITEAOF。

**回答框架**：

1）扩容条件一：没有执行 BGSAVE/BGREWRITEAOF 且负载因子 ≥ 1；2）扩容条件二：正在执行 BGSAVE/BGREWRITEAOF 且负载因子 ≥ 5；3）缩容条件：负载因子 < 0.1；4）为什么后台持久化时阈值更高：利用写时复制，减少内存页分裂与额外内存占用；5）负载因子 = used / size。

**参考回答**：

**分析**

这是问扩容时机，HashTable的扩容与缩容由哈希表的负载因子决定，负载因子 = 键值对数量 / 哈希表大小。

**回答**

我先说扩容，当以下两个条件中的任意一个被满足时，哈希表会自动开始扩容：

- 第一个是服务器目前没有在执行BGSAVE或者BGREWRITEAOF，**并且负载因子 >= 1**
- 第二个是服务器目前正在执行BGSAVE或者BGREWRITEAOF，**并且负载因子 >= 5**

缩容的话也是负载因子影响，当**哈希表的负载因子小于0.1时**，程序会自动开始对哈希表进行收缩操作。

**常见追问**：1）写时复制在这里是怎么影响决策的？2）负载因子怎么计算和观测？

---

## 27. 跳表编码模式下，查询节点总数的平均时间复杂度是多少？

> 原题 ID：`q0401`

**高频程度**：★★

**考察点**：跳表查询节点总数 O(1)：表头 length 字段。

**回答框架**：

1）跳表结构 zskiplist 有 length 字段记录节点数；2）查询总数的 API 直接返回该字段，O(1)；3）另有 level 字段记录当前最大层数；4）对比普通链表统计要 O(N)；5）ZCARD 因此在两种编码下都是 O(1)。

**参考回答**：

**分析**

这个问题是对Redis跳表数据结构的考察，首先我们可以想想跳表的表头结构：

```C
typedef struct zskiplist {
    struct zskiplistNode *header, *tail;
    // 节点数量
    unsigned long length;
    int level;
} zskiplist;
```

跳表的表头结构中定义了保存节点数量的字段：length，所以对Redis跳表查询节点总数的平均时间复杂度应该是 O(1)，我们也可以进一步查看相关的API底层源码：

```C
unsigned int zsetLength(robj *zobj) {
    int length = -1;
    // zset使用ZIPLIST节点数在128以内，一定 < 65535，所以也是 O(1)
    if (zobj->encoding == REDIS_ENCODING_ZIPLIST) {
        length = zllength(zobj->ptr);
    // O(1)
    } else if (zobj->encoding == REDIS_ENCODING_SKIPLIST) {
        length = ((zset*)zobj->ptr)->zsl->length;
    } else {
        redisPanic("Unknown sorted set encoding");
    }
    return length;
}
```

查询有序集合成员总数的API是 zsetLength，而当编码模式是 `REDIS_ENCODING_SKIPLIST`，源码中会直接返回跳表表头结构的length字段，平均时间复杂度是 O(1)。

**回答**

跳表编码模式下，查询节点总数的平均时间复杂度是 O(1)，因为跳表的表头结构中定义了一个保存节点数量的字段length，源码中调用查询节点总数的API时会直接返回这个字段。

**推荐学习**

<cite doc-id="wikcnSYLsLh7kMET8oBSAZLv0af" file-type="wiki" title="底层数据结构跳表" type="doc"></cite>

**常见追问**：1）zskiplist 结构里还有哪些字段？2）跳表怎么支持反向遍历？

---

## 28. 跳表插入一条数据的平均时间复杂度是多少？

> 原题 ID：`q0402`

**高频程度**：★★★

**考察点**：跳表插入的平均时间复杂度 O(logN)：先查找定位再重建索引。

**回答框架**：

1）跳表是多级索引结构，查询效率接近二分查找；2）插入先自顶向下查找插入位置，平均 O(logN)；3）随机决定新节点层高，然后逐层修改前驱的后继指针；4）整体平均 O(logN)，最坏 O(N)；5）对比平衡树：实现简单、范围查询方便、无需旋转。

**参考回答**：

**分析**

这个问题同样是对跳表这种数据结构本身的考察，跳表实际上就是在一维链表上建立多层索引的二维链表，多出来的层数可以让跳表实现类似二分查找的算法。所以跳表的插入平均时间复杂度是 O(logN)。

**回答**

跳表是一种支持多级索引的结构，查询效率可以媲美二分查找，它插入一条数据也是需要先查找，找到之后会进行索引的重建，整体平均时间复杂度是 O(logN)。

**推荐学习**

<cite doc-id="wikcnSYLsLh7kMET8oBSAZLv0af" file-type="wiki" title="底层数据结构跳表" type="doc"></cite>

**常见追问**：1）跳表的 span 字段是干什么用的？2）为什么不用红黑树实现 ZSet？

---

## 29. 为什么跳表和HashTable要配合使用？

> 原题 ID：`q0403`

**高频程度**：★★★★

**考察点**：跳表与字典配合的原因：字典管 member→score 单点查询 O(1)，跳表管按 score 的范围查询与排名。

**回答框架**：

1）ZSet 有两类核心操作：按 member 查 score，按 score 范围/排名查询；2）字典：member→score 映射，单点 O(1)；3）跳表：按 score 有序，支持 ZRANGE/ZRANK/ZRANGEBYSCORE，O(logN)；4）两者共享 member/score 对象，只多指针开销；5）单一结构无法兼顾两类操作。

**参考回答**：

**分析**

使用了两种数据结构来实现自然是，为了让有序集合拥有这两种数据结构的优势。可以结合跳表和字典相较于各自的优势来进行回答。

**回答**

为了结合这两种数据结构各自的优势，当ZSet要根据成员来查找分值的时候，将使用字典来实现，时间复杂度为O(1)。而当ZSet要执行范围操作时，比如ZRANK、ZRANGE等命令时，将使用原本就有序的跳跃表来实现。

**推荐学习**

<cite doc-id="wikcnSYLsLh7kMET8oBSAZLv0af" file-type="wiki" title="底层数据结构跳表" type="doc"></cite>

**常见追问**：1）两个结构怎么保证不重复存数据？2）ZRANK 是怎么算排名的？

---

## 30. 跳表中一个节点的层高是怎么决定的？

> 原题 ID：`q0404`

**高频程度**：★★★★

**考察点**：跳表节点层高的随机生成：初始 1 层，每层晋升概率 25%，最高 32 层。

**回答框架**：

1）插入前随机计算层高；2）新节点初始 1 层；3）每增加一层的概率是 25%，期望层高约 1.33；4）最高 32 层，限制空间与防止退化；5）随机而非固定，让跳表整体近似平衡。

**参考回答**：

**分析**

这个问题是对跳表这种数据结构本身的考察，跳表中插入一个节点之前会选择一个随机化的层数，因为如果跳表的层数从下至上呈一定的比例关系，那么后期插入和删除的时候就又需要去维护这种比例关系，会使时间复杂度退化。所以跳表选择在插入节点的时候，选择一个随机化的层数。

但是生成的随机层数得遵循一个算法，使得生成小数值层数的概率很大，而生成大数值层数的概率很小，这个算法就是幂次定律。跳表在插入新节点之前，会利用这个幂次定律算法生成一个随机层数。

**回答**

跳表在插入新节点之前会计算一个随机的层高，具体来说，跳表的每一个节点一开始默认都是1层，然后每增加一层的概率都是 25%，最高为32层

**推荐学习**

<cite doc-id="wikcnSYLsLh7kMET8oBSAZLv0af" file-type="wiki" title="底层数据结构跳表" type="doc"></cite>

25%源码：https://github.com/redis/redis/blob/7.0/src/t_zset.c

**常见追问**：1）层高期望值怎么算出来的？2）如果概率设成 50% 会有什么问题？

---

## 31. zset为什么用跳表而不用平衡树？

> 原题 ID：`q0405`

**高频程度**：★★★★

**考察点**：跳表 vs 平衡树：实现简单、区间查询高效、内存与索引策略灵活可调。

**回答框架**：

1）实现简单：平衡树插入删除要旋转和再平衡，跳表只改指针；2）区间查询高效：跳表 O(logN) 定位起点后沿底层链表顺序遍历，平衡树中序遍历不连续；3）内存与效率可调：改变层高概率即可调节索引密度；4）并发友好：无全局旋转；5）结论：代码简单、易读、不易出错，Redis 选跳表。

**参考回答**：

Redis ZSet 需要同时支持按 member 查 score 和按 score 范围有序遍历。跳表和各种平衡树都能提供近似的对数复杂度；Redis 选择跳表主要是实现简洁、范围遍历直接，并能与字典索引组合，不是因为跳表在所有 CPU 或缓存场景下天然更快。

因此讨论应落在接口、内存、更新模式和版本实现上，而不是把某一种数据结构说成普适最优。实际大集合还要关注成员长度、更新频率、网络输出和命令时间复杂度。

**常见追问**：为什么 ZSet 既需要按 member 定位，也需要按 score 做范围查询？

---

## 32. 一万个用户我要统计最近十天连续签到的所有用户的个数不需要知道用户具体是谁如何用Redis的bitmap实现？

> 原题 ID：`q0407`

**高频程度**：★★★★

**考察点**：Bitmap 按位统计：每日一个 bitmap、SETBIT 标记、BITOP AND 求交集、BITCOUNT 统计人数。

**回答框架**：

1）数据结构：每天一个 bitmap，key 形如 sign:yyyy-MM-dd，用户 ID 作为 bit 偏移；2）签到：SETBIT sign:date uid 1；3）连续十天：BITOP AND dest 十个 key，得到十天的交集位图；4）统计人数：BITCOUNT dest，返回 1 的个数；5）注意：用户 ID 需映射为数字偏移，偏移过大会浪费内存，可做 ID 压缩映射并设过期。

**参考回答**：

要使用Redis的bitmap统计最近十天连续签到十天的用户数量（不关心具体是谁），可以采用以下方案：

**数据结构设计**

1. **每日签到bitmap**：

   - 每天创建一个bitmap，key格式为 `sign:yyyy-MM-dd`
   - 每个用户ID对应一个bit位（用户ID需要能映射为数字）
2. **连续签到统计**：

   - 对最近10天的bitmap进行AND操作，统计结果为1的位数

具体实现步骤

1. 用户签到记录

```Bash
SETBIT sign:2023-10-01 12345 1
SETBIT sign:2023-10-02 12345 1
...
SETBIT sign:2023-10-10 12345 1
```

2. 统计连续10天签到的用户数

```Bash
BITOP AND 10days_continuous sign:2023-10-01 sign:2023-10-02 ... sign:2023-10-10
#BITOP: Redis的位图操作命令
#AND: 逻辑与操作类型
#10days_continuous: 存储结果的临时key
BITCOUNT 10days_continuous
```

优化建议

1. **用户ID映射**：

   - 如果用户ID不是数字，需要先建立用户ID到数字的映射关系
   - 可以使用一个自增ID或哈希函数将用户ID转为数字
2. **内存优化**：

   - Bitmap会自动扩展，未签到的用户不会占用空间
   - 10天的bitmap AND操作会创建一个临时bitmap，操作完成后应及时删除
3. **性能考虑**：

   - BITOP操作复杂度是O(N)，对于大数据量可能较慢
   - 可以考虑在低峰期执行此统计
4. **替代方案**：

   - 如果用户量极大(>千万)，可以考虑分片处理
   - 或者使用Redis的Lua脚本实现更复杂的逻辑

这种方案非常节省内存，1千万用户每天的签到记录只需要约1.2MB内存空间。

Redis对使用者而言，最常见的还是用命令来操作各种数据对象以达到存储数据的目的，这些操作命令在Redis中具体是怎么运行的，是非常关键的。我们学习过程中需要聚焦整体的执行流程、同时部分细节也需要有所了解。

**常见追问**：1）如果用户 ID 是稀疏的大整数怎么办？2）BITOP 的复杂度是多少，会阻塞吗？

---

## 33. 哪些条件会触发 Redis RDB 快照？优雅关闭、全量同步和手动快照有何区别？

> 原题 ID：`q0417`

**高频程度**：★★★

**考察点**：RDB 的触发时机：SAVE/BGSAVE 命令、配置周期、关闭前，以及主从全量复制等。

**回答框架**：

1）手动命令：SAVE（阻塞）与 BGSAVE（fork 子进程）；2）配置周期：save 900 1 等条件命中自动触发；3）Redis 关闭前触发一次保存；4）主从全量复制时主节点生成 RDB 发送给从节点；5）FLUSHALL 等边缘场景；6）生产用 BGSAVE 避免阻塞。

**参考回答**：

RDB 是某一时刻数据集的快照，常由 `SAVE`、`BGSAVE`、`save` 配置触发的自动快照，或复制全量同步等特定流程产生。优雅关闭、`FLUSHALL`、从节点升主并不必然在所有版本和配置下创建 RDB；AOF 刷盘与重写又是另一条持久化路径。

应结合当前 Redis 版本、`save`、AOF、复制拓扑和运维命令确认恢复点与数据丢失窗口。生产环境还应定期验证备份可恢复性，而不是只确认文件存在。

**常见追问**：RDB 与 AOF 同时启用时，恢复时应重点检查哪些时间点与文件完整性问题？

---

## 34. AOF混合持久化方案是什么？

> 原题 ID：`q0420`

**高频程度**：★★★

**考察点**：AOF 混合持久化：重写时前半部分写 RDB 格式全量，后续增量仍写 AOF 格式。

**回答框架**：

1）重写时用 RDB 方式把内存数据写入新 AOF 文件开头，格式是 RDB；2）重写期间的增量写命令继续以 AOF 格式追加到新文件；3）最终文件 = RDB 格式全量 + AOF 格式增量；4）收益：恢复速度接近 RDB，数据丢失仍按 AOF 策略；5）配置：aof-use-rdb-preamble，Redis 4.0+ 支持。

**参考回答**：

**分析**

需要记住的是，AOF混合持久化，就是在AOF重写的基础上 做了一些改动

**回答**

- AOF混合持久化 会使用**RDB持久化函数** 将**内存数据写入到新的AOF文件中** （数据格式也是RDB格式）
- 而重写期间 新的写入命令 追加到 新的AOF文件 仍然是AOF格式
- 此时新的AOF文件 就是 由 RDB格式 和 AOF格式组成的 日志文件

**推荐学习**

- <cite doc-id="wikcnXWt88S4lrl0pSVmBlG9I8b" file-type="wiki" title="AOF优化-混合持久化" type="doc"></cite>

**常见追问**：1）混合持久化文件怎么被识别和加载？2）如果增量部分损坏了会怎样？

---

## 35. 简单描述AOF重写流程？

> 原题 ID：`q0421`

**高频程度**：★★★★

**考察点**：AOF 重写流程：fork 子进程、子进程读内存生成新文件、主进程写重写缓冲区、完成后合并。

**回答框架**：

1）触发重写时主进程 fork 出子进程；2）子进程读取当前 Redis 内存数据，以命令格式写入新 AOF 文件；3）重写期间主进程把新增写命令追加到 AOF 重写缓冲区；4）子进程写完后，主进程通过管道把重写缓冲区数据发给子进程；5）子进程追加到新文件，保证完整性，最后原子替换旧文件。

**参考回答**：

**分析**

AOF重写就三个关键点：

1. 子进程读取Redis DB中的数据以字符串命令的格式（也可以看作AOF文件格式）写入到新AOF文件
2. 如果有新数据，由**主进程将数据写入到aof重写缓冲区**（aof_rewrite_buf）
3. 当子进程完成重写操作后，主进程通过管道将aof重写缓冲区中的数据传输给子进程，然后子进程追加到新aof文件中

**回答**

当AOF重写触发那一刻，主进程就会fork出一个子进程，然后这个子进程读取Redis DB中的数据，以字符串命令的格式写入到新AOF文件中。

如果这个时候Redis接收到了新的写入命令，那么主进程会将这些“增量数据”写入到AOF重写缓冲区中。

在子进程将数据都写入到新AOF文件后，**主进程会通过管道将AOF重写缓冲区里面的数据发送给子进程**，子进程再将这一份数据追加到新AOF文件中，保证新AOF文件的完整性。

**推荐学习**

- <cite doc-id="wikcnzIaD8C0QLJ4Ld6zEjmBN7f" file-type="wiki" title="AOF详解" type="doc"></cite>重写部分

**常见追问**：1）为什么重写不直接重放旧 AOF？2）重写期间如果子进程失败会怎样？

---

## 36. AOF重写你觉得有什么不足之处么？

> 原题 ID：`q0422`

**高频程度**：★★★

**考察点**：AOF 重写的三类额外开销（CPU、内存、磁盘）与 Redis 7.0 的优化（Multi Part AOF）。

**回答框架**：

1）CPU 开销：主进程写重写缓冲、管道传输、子进程写新文件；2）内存开销：AOF 缓冲与重写缓冲内容重复，浪费一份；3）磁盘开销：重写期间旧 AOF 和新 AOF 同时写，磁盘多占一份；4）Redis 7.0 用 multi-part AOF 优化，重写不再需要额外缓冲区；5）权衡：重写是必要代价，靠控制频率与实例大小缓解。

**参考回答**：

**分析**

- 重写期间 新的写命令，会将数据写入到两处地方（AOF缓冲和AOF重写缓冲）中，这是额外的CPU和内存开销；
- 重写时会将AOF 缓冲 和 AOF重写缓冲 分别 写入到 旧日志 和 新日志中，这是额外的磁盘开销
- 除了清楚 AOF的不足之处，如果还知道改进方案，那么会令面试官刮目相看，Redis 7.0就对此做了新的改进。

**回答**

我认为主要有3点不足之处：

1. 额外的CPU开销：

   - 在重写时，主进程需要将新的写入数据 写入到 AOF重写缓冲(aof_rewrite_buf)
   - 主进程 需要通过管道向子进程发送AOF重写缓冲的数据
   - 子进程还需要将这些数据 写入到新的AOF日志中
2. 额外的内存开销：在重写时，AOF缓冲 和 AOF重写缓冲 中的数据都是一样的（浪费了一份）
3. 额外的磁盘开销：在重写时，AOF缓冲需要刷入 旧的AOF日志，AOF重写缓冲也需要刷入 到新的AOF日志，导致在重写时磁盘多占一份数据

Redis在7.0版本也做了对应的优化，我可以讲一下吗？（如果可以跳到下道题）

**推荐学习**

<cite doc-id="wikcnzIaD8C0QLJ4Ld6zEjmBN7f" file-type="wiki" title="AOF详解" type="doc"></cite> 重写部分

Redis持久化

**常见追问**：1）multi-part AOF 的文件结构是怎样的？2）重写频率怎么定？

---

## 37. 介绍下Redis LRU回收算法？

> 原题 ID：`q0427`

**高频程度**：★★★

**考察点**：LRU 的定义与 Redis 的近似 LRU 实现思路。

**回答框架**：

1）LRU = Least Recently Used，最近最少使用，淘汰最久未访问的数据；2）标准 LRU 用哈希表+双向链表，访问时把节点移到表头，淘汰时删表尾；3）Redis 不维护完整链表，而是在 redisObject 里存 24 位 lru 访问时间戳；4）淘汰时随机采样若干 key，挑最久未访问的淘汰；5）3.0 后加淘汰池优化。

**参考回答**：

**分析**

谈谈LRU概率，这里问题是Redis的LRU算法，可以提一下是近似的。

**回答**

LRU，即最近最久未使用，记录每个key的最近访问时间，淘汰最久未没访问到的数据。但是Redis并不是标准的LRU算法，而是做了优化，这块我可以展开讲讲么？

- PS：面试官说可以的话，也就是下面这题的回答

**推荐学习：**

- <cite doc-id="CRDnwiWdOizj6EkDRGNc5eOMnnc" file-type="wiki" title="内存满了怎么办" type="doc"></cite>

**常见追问**：1）为什么不用标准 LRU？2）近似 LRU 的采样数配置是多少？

---

## 38. Redis LRU算法是标准的吗？为什么不用标准的？

> 原题 ID：`q0428`

**高频程度**：★★★★

**考察点**：近似 LRU 的原因与实现：随机采样 n（默认 5）+ 时间戳比较 + 淘汰池优化。

**回答框架**：

1）标准 LRU 要维护双向链表，内存成本巨大（每个 key 加两个指针）；2）近似做法：随机采样 n 个 key，n 默认 5，淘汰其中最旧的；3）内存不足则继续采样淘汰，反复进行；4）3.0 后加入淘汰池：候选池按空闲时间排序，新采样 key 只有更旧才入池，池满则淘汰最旧；5）效果接近真实 LRU，成本极低。

**参考回答**：

**分析**

关键点在近似LRU，以及内存池优化

**回答**

标准LRU需要维护双链表，内存成本巨大，所以Redis采用近似LRU采样来做淘汰，具体步骤是随机采样 n 个 key，这个采样个数默认为 5，然后根据时间戳就淘汰掉最旧的那个 key，如果淘汰后内存还是不足，就继续随机采样来淘汰。

在3.0之后，Redis还针对近似LRU算法做了淘汰池优化，也就是维护一个候选池，池中的数据根据访问时间进行排序。第一次随机选取的key都会放入池中，然后淘汰掉最久未访问的，比如第一次选了5个，淘汰了1个，剩下4个继续留在池子里。

随后每次随机选取的key只有空闲时间大于 池子里当前空闲时间最小的key时，才会放入池中。当池子装满了，如果有新的key需要放入，则将池中空闲时间最大的key淘汰掉

**推荐学习：**

- <cite doc-id="CRDnwiWdOizj6EkDRGNc5eOMnnc" file-type="wiki" title="内存满了怎么办" type="doc"></cite>

**常见追问**：1）采样数调大有什么影响？2）淘汰池的大小是多少？

---

## 39. 什么是LFU算法，为什么Redis要引入LFU算法？

> 原题 ID：`q0429`

**高频程度**：★★★★

**考察点**：LFU 的定义、与 LRU 的差异（频率 vs 时间）、以及引入原因与计数衰减机制。

**回答框架**：

1）LFU = Least Frequently Used，最不经常使用；2）同时记录访问计数和上一次访问时间；3）计数增长受访问频率影响，且会随时间衰减（对数递增 + 定期衰减）；4）解决 LRU 的痛点：偶发批量访问把热点数据挤出去；5）适用：热点集中、访问分布长尾的场景。

**参考回答**：

**分析**

LRU的一个弊端是只看最近访问时间，而忽略了频率，比如下图

![图片展示了LRU算法的一个弊端。图中有两条时间轴，分别标注为“key niuniu”和“key mart”。两条时间轴上均有多个箭头，代表对key的访问。在两条时间轴的右侧，有文字说明“触发LRU淘汰，淘汰niuniu还是mart？”。该图与上下文紧密相关，用于直观呈现LRU算法只看最近访问时间的弊端，即LRU算法无法区分访问频率，仅依据最近访问时间来决定淘汰key，如图中niuniu和mart在相同时间点被访问，LRU算法无法判断应淘汰哪个key。](https://feishu.cn/file/TervbcyrZoqMFhxS7YvcmMYenMd)

**回答**

LFU，即将访问频率也加入到影响因素中，具体而言，LFU下同时记录了上一次访问时间和访问计数，访问计数同时受上一次访问时间和访问频率的影响，因为每次访问都可能会增加计数。

**推荐学习**

- <cite doc-id="CRDnwiWdOizj6EkDRGNc5eOMnnc" file-type="wiki" title="内存满了怎么办" type="doc"></cite>

**常见追问**：1）LFU 计数为什么设计成 8 位、上限 255？2）衰减机制具体是怎么做的？

---

## 40. 你有实际使用过Redis做什么应用么？

> 原题 ID：`q0430`

**高频程度**：★★

**考察点**：结合实际项目说明 Redis 的应用场景（缓存、分布式锁等），体现动手经验。

**回答框架**：

1）缓存场景：商品/用户/订单等热点数据用旁路缓存；2）分布式锁：SET NX EX + Lua 校验释放，或 Redisson；3）计数与排行榜：秒杀库存、ZSet 排行；4）限流与去重：INCR 计数、Set/Bitmap；5）表达技巧：讲清业务背景、为什么用、遇到什么问题、怎么解决的。

**参考回答**：

**分析**

有些同学回答没做过，这种肯定不行，只要了解相关应用场景，就可以说有使用经验。

一般而言，没经验的同学说2个场景会比较好，太多容易被质疑，太少觉得单薄。

如果实际情况应用多于2个，按实际情况说就行。

**回答**

有在实习中/工作中/实验室中涉及过Redis缓存场景、Redis分布式锁场景

**常见追问**：1）分布式锁为什么用 Lua 释放？2）Redisson 看门狗的原理是什么？

---

## 41. Redis缓存是如何应用的？

> 原题 ID：`q0431`

**高频程度**：★★★

**考察点**：旁路缓存（Cache-Aside）的读路径：先查缓存，未命中查库并回填。

**回答框架**：

1）读：先查 Redis，命中返回；2）未命中回源 MySQL 查询；3）查到结果写回 Redis 并设置 TTL；4）查不到时可写空值或短 TTL 防穿透；5）写：更新 DB 后删除缓存（而非更新缓存）。

**参考回答**：

**分析**

一般都是旁路缓存。这里面面试官不特别问的话，没必要背一堆缓存模式，只说旁路缓存就行。

**回答**

我们是用作旁路缓存，在我们订单项目中，是先查询Redis，没有查到，再查MySQL并将数据加载到Redis。

**推荐学习**

- <cite doc-id="wikcno9vxy6cFFA8VrI7uL1Kjtc" file-type="wiki" title="缓存基础" type="doc"></cite>

**常见追问**：1）为什么不在写的时候直接更新缓存？2）缓存和数据库都没查到该怎么处理？

---

## 42. Redis做秒杀场景可以吗？讲讲思路？

> 原题 ID：`q0434`

**高频程度**：★★★

**考察点**：Redis 做秒杀的思路：库存预热到 Redis、原子扣减、异步落库、可靠性与限流。

**回答框架**：

1）库存预热：活动前把库存写入 Redis；2）扣减：用 Lua 脚本或 DECR 保证原子性，库存不足直接返回；3）结果异步落库：扣减成功后发 MQ 异步创建订单，削峰；4）防超卖：Lua 里判断库存再扣，单线程保证原子；5）限流与防刷：网关限流、一人一单、令牌桶；6）高可用：主从+哨兵或 Cluster，多节点分摊。

**参考回答**：

**分析**

Redis在秒杀场景主要的应用方式是两个。

一个是把Redis当消息队列用，用于削峰，一个是用Redis记录库存，进行库存的加减。

**回答**

Redis可以用来记录库存，利用Redis的高性能进行库存的扣减，一个Redis处理6W的请求问题不大，100W/s流量就20台Redis来支撑，当然，每个节点都要做主从容灾。

另一个方式就是把Redis作为轻量级消息队列，来接受请求，但是不如kafka这种可靠。

**推荐学习**

- <cite doc-id="wikcnMjrBLhw661kReWXuLYDJJh" file-type="wiki" title="秒杀" type="doc"></cite>

**常见追问**：1）Redis 扣减成功但订单创建失败怎么办？2）为什么不在 Lua 里直接写数据库？

---

## 43. Redis 分布式锁为何需要 owner token？如何安全释放？

> 原题 ID：`q0438`

**高频程度**：★★★★

**考察点**：分布式锁 owner（唯一标识）的作用：保证锁的对称性，防止释放别人的锁。

**回答框架**：

1）锁要保证对称性：谁加锁谁释放；2）反例：A 加锁后因业务耗时长或 GC 卡顿导致锁过期，B 拿到锁，A 执行完释放了 B 的锁；3）后果：连锁误删，锁失效，业务互斥被破坏；4）做法：value 存唯一标识（UUID/请求ID），释放时用 Lua 比对再删；5）进阶：看门狗自动续期。

**参考回答**：

引入 owner 也就是唯一标识，是为了保证分布式锁的对称性——谁加的锁只能由谁来释放。假设没有这个机制，会出现这样的问题：服务 A 成功获取到了锁，但由于业务流程比较长，或者发生网络延迟、GC 长时间停顿等原因，锁的 TTL 到期自动释放了，而 A 的业务还在继续执行；这时候服务 B 拿到了同一把锁开始执行临界区，之后 A 执行完业务了，去"释放锁"，实际上删掉的却是 B 持有的锁，于是 B 的锁失效了，其他服务 C 又可以进来，互斥就被彻底破坏了。这就是所谓的连锁误删。所以加锁时需要在 value 里写入一个全局唯一标识，比如 UUID 或者请求 ID，释放时先用 Lua 脚本读取当前锁的 value，判断是不是自己写的，是才删除，不是就什么都不做。因为判断加删除必须是原子的，所以要用 Lua，不能分两条命令做。进一步说，即便用 owner 解决了误删，锁过期和业务未完成的矛盾依然存在，所以生产上常用 Redisson 的看门狗机制，在业务还没执行完时自动给锁续期，避免锁提前失效，同时业务侧也要做好幂等，防止极端情况下重复执行。

**常见追问**：1）锁自动续期有什么风险？2）为什么释放锁必须用 Lua 而不能用 del？

---

## 44. Redis Lua 脚本能保证何种原子性？边界和风险是什么？

> 原题 ID：`q0439`

**高频程度**：★★★★

**考察点**：Lua 脚本"原子性"的真正来源是 Redis 单线程串行执行，而非 Lua 语言本身。

**回答框架**：

1）Lua 本身不具备原子性；2）Redis 单线程执行命令，脚本整段执行期间不会被打断；3）因此脚本内的多个命令对外表现为一个原子操作；4）注意：脚本内没有回滚，出错也不会撤销已执行的写；5）脚本要避免耗时操作，会阻塞整个实例。

**参考回答**：

严格来说 Lua 本身不具备原子性，Lua 语言层面并没有事务回滚或者隔离保证。我们说用 Lua 保证原子性，是因为 Redis 处理命令是单线程的，一段 Lua 脚本提交给 Redis 后会被当作一个整体执行，执行过程中不会插入其他客户端的请求，所以在别的客户端看来，脚本内的多条命令要么都执行完了、要么还没开始，不会看到中间状态，这就是"原子性"的来源——它其实来自 Redis 的单线程串行执行模型，而不是 Lua 语言的语义。举个我们实际用的例子：释放分布式锁时，需要先 GET 判断 value 是不是自己的，再 DEL 删除，这两步如果分开做，中间可能被其他请求插入；打包进 Lua 后就是原子的。不过有两点必须注意：第一，Lua 脚本没有失败回滚，如果脚本执行到一半报错，前面已经执行的写操作不会撤销，所以脚本逻辑要自己保证正确性，别依赖"出错自动回滚"。第二，脚本执行期间整个 Redis 实例是被独占的，所以脚本里不能有耗时操作（比如大 key 遍历、复杂计算），否则会阻塞所有请求；Redis 也有 lua-time-limit 限制，超过后只会打日志和允许 SCRIPT KILL 中断只读脚本，不会自动终止，所以设计上要保证脚本短小。

**常见追问**：1）Lua 脚本执行中超时会怎样？2）想实现回滚语义该怎么办？

---

## 45. 布隆过滤器是怎么工作的？

> 原题 ID：`q0443`

**高频程度**：★★★★

**考察点**：布隆过滤器的结构（位图 + N 个哈希函数）、写入与查询流程、特点（误判、不可删除）。

**回答框架**：

1）结构：一个初始全 0 的位图数组加 N 个哈希函数；2）写入：数据经 N 个哈希函数得到 N 个哈希值，对数组长度取模定位，把对应位置置 1；3）查询：同样算出 N 个位置，全为 1 则可能存在，有一个为 0 则一定不存在；4）特点：空间效率极高但存在误判，不支持删除；5）用途：缓存穿透前置拦截、去重。

**参考回答**：

布隆过滤器由**初始值都为0的位图数组**和**N个哈希函数**两部分组成。在写入数据库数据时，在布隆过滤器里做个标记，这样下次查询数据是否在数据库时，只需要查询布隆过滤器，如果查询到数据没有被标记，说明不在数据库中。

- 第一步，使用 N 个哈希函数分别对数据做哈希计算，得到 N 个哈希值
- 第二步，将第一步得到的 N 个哈希值对位图数组的长度取模，得到每个哈希值在位图数组的对应位置
- 第三步，将每个哈希值在位图数组的对应位置的值设置为 1

举个例子，假设有**一个位图数组长度为 8，哈希函数 3 个**的布隆过滤器。

![图片展示了布隆过滤器在数据库写入数据x后，将数据x标记在位图数组时的计算过程。位图数组长度为8，有3个哈希函数，分别计算出3个哈希值，对8取模后结果为1、4、6，对应位图数组的第1、4、6位置的值被设置为1。该图与上文对布隆过滤器工作原理的描述相呼应，通过具体示例直观呈现了数据写入时位图数组值的设置情况。](https://feishu.cn/file/PTfRbg0fFoXcZkxcvUrcBYkwnkf)

在数据库写入数据 x 后，把数据 x 标记在布隆过滤器时，数据 x 会被 3 个哈希函数分别计算出 3 个哈希值，然后在对这 3 个哈希值对 8 取模，假设取模的结果为 1、4、6，然后把位图数组的第 1、4、6 位置的值设置为 1。当应用要查询数据 x 是否存在于数据库时，通过布隆过滤器只要查到位图数组的第 1、4、6 位置的值是否全为 1，只要有一个为 0，就认为数据 x 不在数据库中。

布隆过滤器由于是基于哈希函数实现查找的，高效查找的同时存在哈希冲突的可能性，比如数据 x 和数据 y 可能都落在第 1、4、6 位置，而事实上，可能数据库中并不存在数据 y，存在误判的情况。

所以，查询布隆过滤器说数据存在，并不一定证明数据库中存在这个数据，但是查询到数据不存在，数据库中一定就不存在这个数据。

**常见追问**：1）怎么降低布隆过滤器的误判率？2）数据删除后布隆过滤器怎么办？

---

## 46. Redis 的主从复制模式有什么优缺点？

> 原题 ID：`q0448`

**高频程度**：★★★

**考察点**：主从复制模式的优点（读写分离、读扩展、数据冗余）与缺点（写单点、内存受限、异步丢数据）。

**回答框架**：

1）优点：读写分离提升读吞吐与响应速度；2）数据冗余，从节点可做备份、可分担读；3）缺点：写只能主节点，写能力与内存容量受单机限制；4）主节点单点风险，故障需人工或依赖哨兵；5）异步复制，故障切换可能丢数据；6）扩展性有限，容量瓶颈靠 Cluster 解决。

**参考回答**：

主从复制的模式相对于单节点的好处在于，实行读写分离提高了系统的读写效率，提高了网站数据的读取加载速度。但是缺点是由于写数据主要在主节点上操作，**主节点内存空间有限**，并且主节点存在单点风险。

**常见追问**：1）怎么降低异步复制丢数据的风险？2）主从延迟很大时读从节点会有什么问题？

---

## 47. 哨兵机制的工作原理？

> 原题 ID：`q0450`

**高频程度**：★★★★

**考察点**：哨兵机制工作原理四步：判活（主观/客观下线）、选 leader、选新主、通知客户端。

**回答框架**：

1）判活：哨兵周期性 PING 主节点，超时先标记主观下线，再询问其他哨兵，达到 quorum 票数标记客观下线；2）选 leader：候选者拉票，每个哨兵一票，过半且超过阈值当选；3）选新主：从从节点中按优先级、复制偏移量、runid 排序选出最优者；4）通知客户端：通过发布订阅推送新主地址，客户端切换；5）此后旧主恢复后降为从节点。

**参考回答**：

口诀：判活，投票选leader，选新更主通客户

**判活：判断主节点是否存活**

哨兵会周期性地给所有主节点发送PING命令，来判断其他节点是否正常运行。如果PING命令响应失败哨兵会将节点标记为**主观下线**。然后该哨兵会向其他节点发出投票命令，当票数达到设定的阈值之后这个主节点就被标记为**客观下线**。然后哨兵会从从节点中选择一个作为主节点。（Redis 心跳是通过 `PING` 命令来完成的，而不是传统的 TCP 心跳包。这种方式更贴合 Redis 自身的协议和运行机制。）

**投票选leader**

哨兵集群中会选择一个leader来负责主从切换，选举是一个投票过程：判断主节点为**客观下线**的是候选者，候选者向其他哨兵发送命令表示要成为leader，其他哨兵会进行投票，每个哨兵只有一票，可以投给自己或投给别人，但是只有候选者才能把票投给自己。候选者之后拿到半数以上的赞成票并且票数大于设置的阈值，就会成为leader。

**选出新主节点**

把网络状态不好的从节点给排除：先把已经下线的从节点过滤掉，然后把以往网络连接状态不好的从节点排除掉。接下来要对所有从节点进行**三轮考察**：**优先级、复制进度、ID号**。在进行每一轮考察的时候，哪个从节点优先胜出，就选择其作为新主节点：

- 第一轮考察：哨兵首先会根据从节点的**优先级来进行排序**，优先级的值越小排名越靠前。
- 第二轮考察：如果优先级相同，则**查看复制的下标**，哪个接收的复制数据多哪个就靠前。
- 第三轮考察：如果优先级和下标都相同，**选择ID较小的那个**。

**更换主节点**

选出新主节点之后，哨兵leader让已下线主节点属下的所有从节点指向新主节点。

**通知客户主节点已更换**

客户端和哨兵建立连接后，客户端会订阅哨兵提供的频道。主从切换完成后，哨兵就会向 `+switch-master` 频道发布新主节点的 IP 地址和端口的消息，这个时候客户端就可以收到这条信息，然后用这里面的新主节点的 IP 地址和端口进行通信了。

**将旧主节点变为从节点**

继续监视旧主节点，当旧主节点重新上线时，哨兵集群就会向它发送SLAVEOF命令，让它成为新主节点的从节点。

**推荐资料**

Redis中的哨兵选举算法是如何实现的？（推荐看）

**常见追问**：1）quorum 和 majority 有什么区别？2）为什么心跳用 PING 不用 TCP？

---

## 48. Redis sentinel（哨兵）模式优缺点有哪些？

> 原题 ID：`q0451`

**高频程度**：★★★

**考察点**：哨兵模式的优点（自动故障转移、高可用）与缺点（主节点单点、切换期不可用、可能丢数据）。

**回答框架**：

1）优点：自动监控与故障转移，无需人工介入，实现高可用；2）客户端可通过哨兵自动感知新主；3）缺点：本质仍是主从，主节点单点写入，容量和写能力受限；4）切换期间有短暂不可用，客户端可能报错重连；5）异步复制导致切换可能丢少量数据；6）哨兵本身需多数派存活，部署建议奇数个。

**参考回答**：

Redis 哨兵的好处在于可以**保证系统的高可用**，**各个节点可以对故障自动转移**。但缺点是使用的主从模式，主节点单点风险高，主从切换过程可能会出现丢失数据的问题。

**常见追问**：1）min-replicas-to-write 怎么用？2）哨兵和 Cluster 该选哪个？

---

## 49. 说说 Redis 哈希槽的概念？

> 原题 ID：`q0452`

**高频程度**：★★★★

**考察点**：Redis Cluster 的哈希槽概念：16384 个槽、CRC16 取模定位、节点负责部分槽。

**回答框架**：

1）Cluster 不用一致性哈希，用哈希槽；2）总共 16384（2^14）个槽；3）key 通过 CRC16 校验后对 16384 取模得到槽号；4）每个主节点负责一部分槽，槽可以迁移；5）客户端根据槽路由到对应节点，MOVED/ASK 重定向。

**参考回答**：

Redis 集群并**没有使用一致性 hash**，而是引入了哈希槽的概念。Redis 集群有 16384 (2^14) 个哈希槽，每个 key 通过 CRC16 校验后对 16384 取模来决定放置哪个槽，集群的每个节点负责一部分 hash 槽。

**常见追问**：1）为什么不用一致性哈希？2）hash tag 有什么使用风险？

---

## 50. Redis Cluster 的 16384 槽位如何工作？它与集群规模有什么关系？

> 原题 ID：`q0453`

**高频程度**：★★★★

**考察点**：哈希槽数 16384 的原因：心跳包大小（位图 2KB vs 8KB）、节点数上限、集群规模与网络开销的权衡。

**回答框架**：

1）心跳携带槽位图，16384 位 = 2KB，若用 65536 则是 8KB；2）槽位图在心跳里频繁传输，2KB 网络开销可接受；3）16384 个节点规模对集群已足够（官方建议不超过 1000 节点）；4）槽位数远大于节点数，便于均匀分配；5）奇数 vs 2 的幂的历史权衡，实际是 2^14 的位图大小与扩容需求平衡的结果。

**参考回答**：

哈希槽是 16384 个，主要有两个考虑。第一，是心跳消息的大小问题。Cluster 节点之间要定期发送心跳包，心跳包里需要带上自己的槽位图，表示"我负责哪些槽"。如果用位图表示，16384 个槽需要 16384 个 bit，也就是 2KB；如果槽数是 65536，位图就变成 8KB。这些心跳消息是周期性、全节点互相发送的，节点数一多，网络开销和内存开销会被放大好几倍，2KB 是作者认为可接受的平衡点。第二，是集群规模的现实考虑。Redis Cluster 官方建议的节点规模上限在 1000 个左右，16384 个槽分配到这些节点上，平均每个节点还有十几个槽，足够做均匀分配和细粒度再平衡；而如果只有几百个节点，65536 个槽完全是浪费，每个槽都要维护元数据、参与迁移计算。另外还有一个历史点和容量判断：哈希槽数量远大于节点数是为了让槽可以作为"最细粒度的迁移单位"，扩容时只需搬一部分槽，而不需要重新分配整个 key 空间。所以结论是 16384 是在"心跳包大小"和"集群可扩展规模"之间权衡出来的经验值，不是理论上唯一正确，而是工程上够用且开销合理。

**常见追问**：1）为什么槽数是 2 的幂？2）超过 1000 个节点会有什么问题？

---

## 51. Redis SCAN 的遍历保证、重复与新增/删除边界是什么？

> 原题 ID：`q0454`

**高频程度**：★★★★★

**考察点**：KEYS 命令的性能与阻塞问题、生产禁用原因、SCAN 游标替代方案。

**回答框架**：

1）性能问题：KEYS 遍历整个键空间，key 多时非常耗时；2）阻塞问题：单线程执行，期间阻塞所有其他客户端请求；3）生产环境一般直接禁用；4）替代方案 SCAN：游标分批遍历，非阻塞；5）SCAN 参数：MATCH 模式、COUNT 提示、游标 0 开始到 0 结束；6）注意 SCAN 不保证不重复、只保证在遍历期间存在的元素都被返回。

**参考回答**：

KEYS 命令的问题主要有两个。第一是性能问题：KEYS 需要遍历整个 Redis 的键空间，如果 key 数量非常庞大（几十万上百万），遍历过程非常耗时，CPU 时间被大量占用。第二也是更致命的，是阻塞问题：Redis 是单线程处理命令的，KEYS 执行期间会阻塞所有其他客户端对数据库的访问，直到它执行结束，也就是说这期间整个 Redis 几乎停止响应其他请求，会导致服务不可用或响应极慢，所以生产环境一般是禁止使用 KEYS 的，很多公司会在配置里直接用 rename-command 禁掉它。替代方案是 SCAN 命令，它通过游标（cursor）的方式分批遍历 key 空间，每次只返回一小部分，不会长时间阻塞 Redis。用法是 SCAN cursor [MATCH pattern] [COUNT count]，其中 cursor 初始值为 0，每次调用返回一个新游标，直到返回 0 表示遍历完成；MATCH 用模式匹配过滤 key；COUNT 是每次遍历的提示数量，默认 10，调大能加快遍历但会增加单次阻塞时间。使用 SCAN 有个重要特点要注意：它只保证在遍历期间一直存在的元素会被返回，但不能保证不重复返回，也不保证返回严格完整——因为遍历过程中如果发生了 rehash 或数据变更，可能有元素被返回多次或漏掉，所以业务上要容忍重复并做幂等。除了 SCAN，还有 HSCAN、SSCAN、ZSCAN 分别用于遍历 Hash、Set、ZSet 的元素。

**常见追问**：1）SCAN 的 COUNT 调大会有什么影响？2）SCAN 为什么可能返回重复元素？

---

## 52. Redis中的Monitor命令了解吗？

> 原题 ID：`q0455`

**高频程度**：★★

**考察点**：MONITOR 命令的作用、使用场景与性能影响。

**回答框架**：

1）作用：实时打印 Redis 收到的所有命令，用于调试和排查；2）输出包含时间戳、客户端地址、命令、数据库编号；3）性能影响大：每条命令都要转发和格式化，会降低吞吐；4）仅建议短时调试使用，生产慎用；5）替代：SLOWLOG 看慢命令、CLIENT LIST 看连接、审计日志。

**参考回答**：

`MONITOR` 是 Redis 中的一个调试命令，用于实时查看 Redis 服务器接收到的所有命令。它可以帮助开发者或管理员监控 Redis 的操作，但会显著影响性能，因此仅建议在调试时使用。

**常见追问**：1）MONITOR 输出会丢吗？2）怎么快速定位是谁在写某个 key？

---

## 53. Redis中如何查看大Key，热Key？

> 原题 ID：`q0456`

**高频程度**：★★★★

**考察点**：大 Key 与热 Key 的检测手段：redis-cli --bigkeys、--hotkeys、SCAN 采样、MONITOR、客户端埋点。

**回答框架**：

1）大 Key：redis-cli --bigkeys 扫描各类结构找最大的 key；也可用 SCAN 采样 MEMORY USAGE 或 STRLEN/LLEN/HLEN 判断；2）热 Key：redis-cli --hotkeys（需 maxmemory-policy 为 LFU）、MONITOR 统计、客户端埋点统计、代理层统计；3）注意：--bigkeys 和 --hotkeys 都是扫描全库，有开销，建议从节点或低峰执行；4）也可用 RDB 离线分析工具。

**参考回答**：

```Shell
//查看大key
redis-cli --bigkeys

//Redis 6.0 及以上版本提供了
//--hotkeys 选项，可以直接扫描并找出热 Key。
redis-cli --hotkeys

也可使用 Lua 脚本统计 Key 访问频率
```

**常见追问**：1）--hotkeys 为什么依赖 LFU 策略？2）客户端埋点统计热 key 有什么开销？

---

## 54. Redis中的大key怎么处理？

> 原题 ID：`q0457`

**高频程度**：★★★★

**考察点**：大 Key 的问题（内存、网络、持久化、阻塞）与处理手段（拆分、集群、压缩、渐进删除、业务优化）。

**回答框架**：

1) 识别大 Key 对内存、网络、持久化、复制和事件循环延迟的影响；2) 按访问模式把一个大对象拆成多个独立 key；3) Cluster 只能分散已拆分的多个 key，不能自动拆分单个大 key；4) 用迁移、渐进删除或 UNLINK 降低在线风险；5) 通过内存、延迟和复制指标验证。

**参考回答**：

Redis 大 Key 是指单个 key 对应的数据量或元素数过大，例如数 MB 的 String、含大量成员的 Hash、Set 或 ZSet。它会放大内存占用、网络传输、持久化和复制开销；同步删除或过期还可能拉长事件循环的响应时间。

根本治理是按业务访问模式拆分：把一个大集合或大 Hash 分桶为多个独立 key，分页或按需读取，避免把本应查询的数据整体塞进一个 value。Redis Cluster 可以把这些拆分后的多个 key 分布到不同槽位和节点，但不能自动把“一个大 key”拆到多台机器上。压缩 value 只能在 CPU、延迟和可查询性可接受时使用。

处理既有大 Key 时，先通过采样、`MEMORY USAGE`、`--bigkeys` 或业务指标确认影响，再进行双写/迁移和逐步切流。删除大型复合结构可分批删除字段，或在合适场景使用 `UNLINK` 将释放工作交给异步 lazy-free；仍要观察内存、延迟与复制压力。

**常见追问**：为什么把一个大 Hash 拆成多个 key 后，才可能通过 Redis Cluster 分散单节点压力？

---

## 55. 高并发系统发现Redis热点数据五种实现方式？

> 原题 ID：`q0458`

**高频程度**：★★★

**考察点**：热 Key 的五种发现方式：hotkeys 指令、MONITOR 统计、客户端埋点、代理层统计、离线数据分析。

**回答框架**：

1）redis-cli --hotkeys：scan + OBJECT FREQ，需 LFU 策略，实时性差、信息不够丰富；2）MONITOR 命令统计：实时抓命令后聚合，但开销大；3）客户端埋点：SDK 侧统计 key 访问频率，最实时；4）代理层统计：如 Twemproxy/Codis 层统计；5）离线分析：基于 RDB/日志做分析；6）实践：埋点+代理层实时监控，低峰做巡检。

**参考回答**：

**hotkeys指令**

Redis自4.0起在Redis-cli中提供了hotkeys参数来方便用户进行实例级的热key分析功能，Redis-cli通过向Redis-server节点发送`scan + object freq`命令以遍历的方式分析Redis实例中所有key，然后返回实例中热key信息。

![图片展示了Redis-cli使用`--hotkeys`参数进行实例级热key分析的命令及结果。命令为`redis-cli --hotkeys`，扫描整个键空间以查找热键及每种键类型的平均大小，可使用`-i 0.1`参数让每100个SCAN命令暂停0.1秒。结果显示“key:__rand_int__”为热键，计数为37，还总结了采样了5个键空间中的键。该图片与上文介绍Redis自4.0起提供的`hotkeys`参数进行热key分析的内容相关，直观呈现了该功能的使用效果。](https://feishu.cn/file/HGowbMcMYoBPEjxZ5incwHS3nue)

该方式存在以下几个问题：

1. 使用该方案的**前提条件是**需要将Redis-server的淘汰策略`maxmemory-policy`参数设置为LFU（volatile-lfu或allkeys-lfu）
2. 实时性差。**由于需要扫描整个keyspace**，实时性较差，扫描时间与key数量正相关，如果key数量比较多，耗时可能会非常长。
3. 信息不够丰富。首先**记录的访问频率是一个与访问次数的对数成比例的相关近似值**，不能够很直观的看出来热key的访问频率；另外，返回的信息中也没有key的类型和热key出现的时间等。

**monitor命令统计**

Redis提供monitor命令可以实时抓取出Redis服务器接收到的命令，可以对抓取的数据结合一些现成的分析工具（比如Redis-faina）统计出抓取时间段内的访问热key。

```go
 1 127.0.0.1:6379 > monitor
 2 OK
 3 1616541192.039933 [0 127.0.0.1:57070] "PING"
 4 1616541276.052331 [0 127.0.0.1:57098] "set" "user:2398423hu" "KutMo"
```

该方式存在以下几个问题：

1. 该命令在高并发的条件下，有内存暴增的隐患，还会降低Redis的性能，只能紧急情况下短暂使用，不能长时间使用。
2. 该方式只能统计开启monitor命令的期间访问热key情况，对于过去已经发生的访问热key无法获取，无法应对一些瞬时的突发热key等情况。

**Redis节点抓包分析**

Redis客户端使用TCP协议与服务端进行交互，并且通信协议采用自定义的RESP协议，可以使用libpcap库对Redis-server监听端口抓包，然后按照RESP协议解析数据，并统计抓包期间内访问的热key。

> libpcap是一个用于网络流量捕获的开源库，支持Unix和Windows系统。它允许开发者编写应用程序来捕获和分析网络数据包，广泛应用于网络监控、分析工具及安全软件中。通过提供一致的API，libpcap使得不同应用能够在多种平台上高效地进行数据包处理。

![图片展示了Client/Proxy端收集Redis热点数据的方式。Client端发送请求到Redis Server，Server端通过6379 libpcap进行数据收集，然后将收集到的数据上报到统一服务进行聚合计算。这种方式可对客户端工具进行封装，在发送请求前进行数据收集采集，同时定时上报数据，适用于对Redis热点数据进行统计分析，但需注意对Redis-server性能有一定损耗，且无法长时间开启抓包。](https://feishu.cn/file/Q6NtbwUNno4CIvx61iqcEcFHnF7)

该方式存在以下几个问题：

1. 该方式实现相对比较复杂，有一定的开发成本。
2. 同样只能统计开启抓包期间的访问热key情况，无法获取过去的热key。
3. 开启期间对访问Redis-server性能有一定的损耗，而且ECS一般会部署多个Redis-server，全量开启会对系统负载有一定影响，因此无法长时间开启进行实时处理。

**Client/Proxy端收集**

可以对客户端工具进行封装，在发送请求前进行收集采集，同时定时把收集到的数据上报到统一的服务进行聚合计算。或者，如果业务通过Proxy访问缓存的话，可以在Proxy上进行收集，其他思路与Client端收集模式一致。

目前，比如，有赞自研分布式缓存系统zanKV、京东零售开源的热key探测框架（JD-hotkey）、得物热点探测框架（Burning）都是类似这种方案，在客户端进行收集，在聚合中心worker节点上进行热key统计，统计出来的热key可以推送到客户端进行本地缓存。

该方式存在以下几个问题：

1. 在客户端收集的方案对客户端代码有一定的侵入，而且每种语言的SDK都需要进行开发，后期开发维护成本较高。
2. 框架比较复杂，开发成本高。由于同一个key的访问可能同时出现在多个不同的客户端或者Proxy上，因此，在单个客户端或者Proxy上是无法统计热key的，因此，该方案需要一个聚合中心计算平台，收集不同Client/Proxy上访问的key，然后计算热key信息。

下图为京东开源的热key探测框架系统架构图：

![图片为京东开源的热key探测框架系统架构图。图中展示了Dashboard控制台、ETCD集群、worker节点等组件，以及各节点间的数据交互流程。Dashboard控制台负责接收Redis实例上报的热key信息，ETCD集群用于存储热key配置信息，worker节点负责热key的统计与上报。图中还标注了各节点在系统中的具体功能，如worker节点上报热key信息、ETCD集群存储配置信息等，直观呈现了热key探测框架的系统架构。](https://feishu.cn/file/WjXJb9sgSo8dwjxR9pscAcwendg)

**基于Redis内核的热key统计**

从上面的分析可以看到，目前存在的一些方案，要么无法高效快速的获取实时热key信息，要么架构比较复杂或者对业务有一定的侵入，得物自建Redis设计并研发基于Redis内核的热key统计方案，可以高效的统计并记录Redis实时热key信息，同时提供热key产生与热key失效的订阅通知。

基于内核的Redis热key统计方案在Redis-server端实现，包含热key统计模块和热key通知模块两部分，另外提供热key日志记录查询与重置命令。

热key统计模块基于LRU队列实现统计key每秒内访问次数，当访问次数达到设置的热key阈值时，被判定为热key，热key加入热key队列用于提供实时查询。

基于内核的Redis热key统计方案提供热key订阅与主动通知功能，提供读热key、写热key、热key失效三个订阅通道channel，可用于Client/Proxy订阅热key消息，当key被判定为热key时，Redis-server主动向对应的消息通道广播热key消息。

Redis热key是在Redis使用过程中一个比较常见的现象，同时，热key的实时探测与解决一直是业界的一个难点问题。得物自建Redis结合当前各种热key探测方案的优缺点，实现基于Redis内核的高性能实时热key统计方案。

该方案具备如下优点：

- 完全无侵入
- 实时性强：可实时统计热key信息，统计粒度为每秒
- 热key信息详细：热key信息包含热key出现的时间、访问次数、key类型、读操作或写操作等信息
- 支持订阅与查询：支持读热key、写热key、热key失效三种类型通知，可查询热key日志记录

缺点：

- 开发难度最大，需要熟悉Redis内核源码并进行改造
- 只能解决单点Redis统计问题，多节点Redis仍然需要聚合平台

**推荐资料**

高并发系统发现Redis热点数据五种实现方式

基于Redis内核的热key统计实现方案（重点）

**常见追问**：1）发现热 key 之后怎么处理？2）OBJECT FREQ 的值为什么是近似值？

---

## 56. 什么是延迟双删，用于解决什么问题？

> 原题 ID：`q0459`

**高频程度**：★★★★★

**考察点**：延迟双删的流程、解决的问题（并发回填脏数据、主从延迟回填）、适用场景与局限。

**回答框架**：

1）流程：删除缓存 → 更新数据库 → 延迟一段时间 → 再删缓存；2）解决的问题一：更新前读请求读到旧值并回填缓存导致脏数据长期存在；3）解决的问题二：读写分离下从库延迟，读到旧数据回填缓存；4）延迟时长的选择；5）局限：不能保证强一致，仍需 TTL 兜底，延迟时间难精确。

**参考回答**：

**延迟双删**是一种用于解决 **缓存与数据库一致性** 问题的策略，特别是在 **写操作后** 可能出现缓存脏数据的情况。

**核心流程**（以更新数据为例）：

1. **第一次删除缓存**：在更新数据库前，先删除缓存（防止旧数据被读到）。
2. **更新数据库**：执行数据库写操作。
3. **延迟后第二次删除缓存**：在数据库更新完成后，延迟一小段时间（如几百毫秒），再次删除缓存，确保可能因并发读操作导致的脏数据被清理掉。

**解决的问题**：

- **缓存与数据库不一致**：在高并发场景下，可能出现：

  - 读请求在数据库更新前读取旧数据并回填缓存（导致缓存长期存储旧数据）。
  - 数据库主从延迟时，从库未同步最新数据，读请求回填旧数据到缓存（主负责写，从负责读，从库未同步数据，是旧数据，回填的也是旧数据）。
- **减少脏数据残留**：通过二次删除，清理可能因并发读写导致的缓存不一致问题。

**适用场景**：

- **对一致性要求较高的业务**（如订单、支付等）。
- 数据库采用主从架构，可能存在同步延迟的情况。

**缺点**：

- 无法完全保证强一致性（极端情况下仍可能不一致）。
- 延迟时间需要合理设置（太短可能无效，太长影响性能）。

**替代方案**：

- 使用 **Cache Aside Pattern + 消息队列异步清理缓存**
- 采用 **分布式锁** 或 **数据库 Binlog 监听（如 Canal）** 保证一致性

延迟双删（流程总结）
1. 第一次删除 Redis（快速失效缓存，防止旧数据被读到）
2. 更新 MySQL（确保数据持久化）
3. 延迟一段时间（等待主从同步/业务完成）
4. 第二次删除 Redis（清理可能的旧数据回填）

**常见追问**：1）延迟时间怎么确定，有没有更精确的方案？2）双删失败怎么办？

---

## 57. 如果硬件内存特别大，MySQL 缓存能否替代 redis？

> 原题 ID：`q0486`

**高频程度**：★★★

**考察点**：MySQL 面向磁盘设计 vs Redis 面向内存设计，无法靠加大内存互相替代。

**回答框架**：

1）设计目标不同：MySQL 各模块（buffer pool、日志、事务并发）首要目标是降低 IO 代价，不是降低内存访问代价；2）访问路径：MySQL 查数据要查页、走 B+ 树 O(logN)，Redis 用 Hash 可 O(1)；3）更新开销：MySQL 为保证隔离性要加锁、为持久性要刷 redo/binlog，Redis 更新不加锁、可不持久化；4）结论：内存再大也改变不了架构定位；5）正确姿势：两者协同而非替代。

**参考回答**：

**分析**

这个是一个字节的面试题，这里的 MySQL 缓存代表用于缓存数据页的 buffer pool。所以问题的意思是，假设 buffer pool 无限大，能在内存装下所有数据，是否可以替代 redis？

问题的回答思路，要去想 Redis 缓存哪些优点是 MySQL 没有的？

**回答**

我觉得还是不能替代。

MySQL 所有模块，比如 buffer pool、日志技术、事务并发模块，都是面向磁盘页而设计的，因此其首要目标不是减少内存访问的代价，而是I/O代价，所以内存访问代价低却并不是最优的选择，而 Redis 是面向内存而设计的数据库。

MySQL 在内存查询一个数据页的时候，都需要先查页表，也就是需要走 b+ 树的搜索过程，时间复杂度是 O(logdN)，而 Redis 提供了很多种的数据类型，比如用Hash 数据对象的时候，可以在 O(1) 时间复杂度查到数据。

MySQL 在更新数据的时候，MySQL 为了保证事务的隔离性，是需要加锁的，而 Redis 更新操作都是不需要加锁的，还有MySQL为了保证事务的持久性，还需要刷盘 redolog 日志和 binlog 日志，Redis 可以选择不持久化数据。

因此，即使 buffer pool 无限大，MySQL 缓存的性能还是没有 Redis 好的。

**推荐学习**

既然有了innodb buffer pool为什么要有redis？ - 知乎

**常见追问**：1）MySQL 8 的查询缓存和 Redis 缓存有什么区别？2）如果只是简单 KV 场景，能只用 MySQL 吗？

---

## 58. MySQL事务和Redis事务有什么区别？

> 原题 ID：`q0535`

**高频程度**：★★★★

**考察点**：Redis 事务与 MySQL 事务的区别：原子性（无回滚）、持久性、隔离性、使用方式。

**回答框架**：

1）MySQL 支持完整 ACID；2）Redis 事务不保证原子性：命令执行中途出错不停止不回滚，继续执行剩余命令，可能半成功；3）不保证持久性：RDB/AOF 都不能确保不丢数据；4）隔离性：靠单线程串行执行，而非锁/MVCC；5）命令：MULTI/EXEC/DISCARD/WATCH；6）本质：Redis 事务是"命令打包顺序执行"而非真正的事务。

**参考回答**：

Redis 的 `MULTI`/`EXEC` 会先把命令入队，再在执行阶段连续串行执行，执行期间不会被其他客户端命令穿插。因此它提供了命令序列的隔离执行，但并不等于关系数据库的完整事务。

它没有自动回滚：入队阶段的语法/命令错误会导致 EXEC 失败；执行阶段某条命令的运行时错误会单独返回错误，已经成功的命令不会撤销。需要乐观并发控制时可配合 `WATCH`，需要复杂原子逻辑时可用 Lua/Functions，并仍要设计幂等和错误处理。

**常见追问**：为什么 Redis 事务中一条运行时命令失败后，前面成功命令不会自动回滚？

---

## 59. 为什么引入 Redis？

> 原题 ID：`q1453`

**高频程度**：★★★★

**考察点**：引入 Redis 的四个层面原因：性能（缓存）、高并发削峰、丰富数据结构、分布式场景支撑。

**回答框架**：

1）性能提升：MySQL 走磁盘 IO，QPS 几千，Redis 内存单机十万级、亚毫秒，缓存挡住大部分读流量；2）高并发与削峰：秒杀场景做计数器、限流、队列缓冲；3）丰富数据结构：String/Hash/List/Set/ZSet/Bitmap/HLL/GEO/Stream 直接支撑业务模型；4）分布式支撑：会话共享、分布式锁、分布式 ID、消息队列；5）代价：内存成本与一致性复杂度。

**参考回答**：

引入 Redis 主要是为了解决传统数据库在性能、并发和分布式场景下的瓶颈，可以从四个层面讲。第一，性能提升也就是缓存：MySQL 这类关系型数据库数据在磁盘上，一次查询要经过 SQL 解析、优化、磁盘 IO、锁竞争，QPS 通常在几千量级，还容易在热点数据上出现锁和 IO 瓶颈；Redis 数据放内存，单机 QPS 可达十万级、延迟亚毫秒。把热点数据放 Redis，请求先查 Redis，未命中再查库并回写，能挡住 90% 以上的读流量，很好地保护数据库。第二，高并发与削峰：秒杀、抢购这类场景瞬时流量极大，直接打数据库会被压垮，Redis 可以做计数器扣库存、做接口限流、做请求缓冲队列，把瞬时洪峰接住再慢慢消费。第三，丰富的数据结构：String、Hash、List、Set、ZSet、Bitmap、HyperLogLog、GEO、Stream 等，能直接实现排行榜、点赞、去重、签到、附近的人、消息队列这些功能，不用自己造轮子，而且这些操作都是服务端原子的，省掉了应用层的并发控制。第四，分布式场景支撑：分布式系统里多台机器需要共享状态，Redis 可以做 Session 共享、分布式锁、分布式 ID 生成、幂等标记、以及基于 Stream 的异步任务队列。当然引入它也有代价：内存比磁盘贵、容量有限，而且多了一层缓存就带来一致性问题，要考虑穿透、击穿、雪崩和高可用。所以正确的做法是让它承担它擅长的部分——热点、高速、原子的场景，把权威数据留在 MySQL。

**常见追问**：1）什么数据不该放 Redis？2）引入 Redis 后系统复杂度体现在哪？

---

## 60. 基于 Redis Stream 实现简历分析、知识库向量化等场景的异步（还能解耦，分析和向量化可以使用其他编程语言来做）？

> 原题 ID：`q1973`

**高频程度**：★★★★

**考察点**：Redis Stream 的异步任务架构：XADD 投递、消费者组、XACK 确认、XAUTOCLAIM 重投、多语言消费者解耦。

**回答框架**：

1）原理：Stream 是 5.0 引入的追加式日志，消息有唯一 ID，支持消费者组；2）生产者：落库后 XADD 投递任务并立即返回任务 ID；3）消费者组：XGROUP CREATE 建组，XREADGROUP 消费，XACK 确认，PENDING 列表看未确认；4）容错：消费者宕机用 XAUTOCLAIM/XCLAIM 重投；5）优势：解耦、削峰、消费者可用任意语言实现；6）对比 List：支持多消费组、消息确认与重投，不像 Pub/Sub 会丢离线消息。

**参考回答**：

用 Redis Stream 做简历分析、知识库向量化这类异步任务，核心是把耗时操作从请求链路里剥离出来。原理上，Stream 是 Redis 5.0 引入的追加式日志结构，每条消息有唯一 ID（默认是时间戳加序号），支持消费者组、消息确认和未确认消息重投，比 List 的 LPUSH/BRPOP 更适合做消息队列——因为它支持多个消费者分组消费、支持 XACK 确认和 pending 列表查看、还支持消息重投，而且不像 Pub/Sub 那样消费者离线就丢消息。典型架构是：第一，生产者（比如 Web 后端）收到简历上传或文档后，先落库或存对象存储，然后用 XADD stream * task_type analyze_resume payload '{...}' 投递任务，立刻返回任务 ID，前端通过轮询或 WebSocket 获知进度，请求链路非常短。第二，消费者端用 XGROUP CREATE 创建消费者组，多个消费者用 XREADGROUP 并行消费，处理完用 XACK 确认；因为是消费者组，同一条消息只会被组内一个消费者处理，可以水平扩容消费者。第三，容错处理：用 XPENDING 查看未确认消息，如果某个消费者宕机，它的 pending 消息可以用 XAUTOCLAIM（Redis 6.2+）或 XCLAIM 转移给其他消费者重投，避免任务丢失；业务上还要保证幂等，因为重投可能重复执行。第四，因为消息格式是通用的，消费者可以用任何语言实现——比如用 Python 做向量化、用 Java 做简历解析，天然实现了服务解耦和技术栈解耦。相比专业的 Kafka，Redis Stream 的优势是部署简单、延迟低、和已有的 Redis 复用运维；不足是持久化和堆积能力不如 Kafka，适合中等吞吐、实时性要求高的任务场景。

**常见追问**：1）消费者处理失败后怎么不丢消息？2）Stream 堆积太多内存怎么办？

---

## 61. Redis 这类缓存和 Caffeine 的区别？

> 原题 ID：`q3395`

**高频程度**：★★★★

**考察点**：Redis（分布式远程缓存）与 Caffeine（进程内本地缓存）在延迟、容量、一致性、可用性、淘汰策略上的对比与多级组合。

**回答框架**：

1）部署形态：Caffeine 进程内、堆内存、纳秒到微秒；Redis 独立服务、走网络、0.1-1ms；2）容量成本：Caffeine 受单机堆限制且加重 GC，Redis 可扩到 TB 级；3）一致性：Caffeine 每 JVM 一份、天然不一致，靠广播失效；Redis 共享存储、多实例读同一份；4）淘汰与结构：Caffeine 用 W-TinyLFU、支持异步刷新；Redis 支持丰富数据结构、持久化、分布式能力；5）实践：多级缓存，Caffeine 挡热读、Redis 做共享层。

**参考回答**：

这两个缓存可以从几个维度对比。第一，部署形态和访问路径：Caffeine 是进程内缓存，直接操作 JVM 堆内存，一次 get 通常是纳秒到微秒级，完全没有网络开销；Redis 是独立的远程服务，客户端通过 TCP 访问，一次 RTT 大约 0.1 到 1 毫秒，跨机房更高。所以延迟上本地缓存比 Redis 快一到三个数量级。第二，容量和成本：Caffeine 受限于单机堆内存，容量有限，而且大量对象常驻堆会加重 GC 压力，尤其大对象、高 QPS 场景容易触发 Full GC 导致长停顿；Redis 容量可以独立扩展，能存 TB 级数据，但需要网络和运维成本。第三，一致性：这是最关键的差异。Caffeine 每个 JVM 一份副本，多实例之间天然不一致，更新只能靠本地失效或广播通知（Redis Pub/Sub、MQ），存在不一致窗口；Redis 是共享存储，多个实例读的是同一份数据，在单 key 维度天然一致。第四，功能和淘汰策略：Caffeine 用 W-TinyLFU 算法，命中率优于朴素 LRU，还支持异步刷新、过期策略丰富；Redis 提供丰富的数据结构、持久化、分布式锁、消息队列等能力，淘汰策略有 LRU/LFU/RANDOM 等。第五，可用性：Caffeine 随应用进程存活，进程挂了缓存就没了，但也不会成为故障点；Redis 是独立组件，需要考虑高可用（主从/哨兵/Cluster），一旦故障可能影响全部实例。所以工程上常见的做法是多级缓存：Caffeine 作为一级挡在前面承接超热数据，Redis 作为二级共享层保证多实例一致和容量扩展，数据库作为最终真相源；写的时候按"更新 DB → 删 Redis → 广播失效本地"来同步，用 TTL 兜底。

**常见追问**：1）多级缓存怎么保证本地缓存及时失效？2）本地缓存放超大对象会有什么问题？

---

## 62. MySQL 和 Redis 怎么保持数据一致？

> 原题 ID：`q3396`

**高频程度**：★★★★★

**考察点**：MySQL 与 Redis 的一致性方案：Cache-Aside、先更 DB 再删缓存、延迟双删、binlog 订阅、强一致代价。

**回答框架**：

1）前提：MySQL 是真相源，Redis 是缓存副本，一致性指缓存值尽量等于库值；2）为什么难：两个操作无法原子，有失败、并发、延迟；3）方案一：先更新 DB 再删缓存（最常用），删而非更新避免并发覆盖；4）方案二：先删缓存再更新 DB，需延迟双删兜底；5）方案三：Canal 订阅 binlog 异步失效，一致性好但组件多；6）强一致需加锁串行化，代价是并发；7）兜底：TTL。

**参考回答**：

先明确前提：MySQL 和 Redis 通常不是双主写入，而是 MySQL 存全量数据作真相源、Redis 做缓存副本，所谓一致性是指缓存里的值尽量等于 MySQL 的值。为什么难？因为更新 MySQL 和更新 Redis 是两个独立操作，中间可能失败、可能并发、可能延迟，无法像单机事务那样保证原子。常见方案有四类。第一，先更新数据库再删除缓存，也就是 Cache-Aside，这是最常用的做法。读的时候先查缓存，没有就查库并回填；写的时候更新库然后删缓存。为什么删而不是更新缓存？因为并发下两个线程的写顺序无法保证，可能出现旧值后写覆盖新值，而删除只是置为未命中，下次读回源拿的必然是最新值，出错窗口更小。可以用类比理解：MySQL 是账本，Redis 是贴在墙上的便签，改完账本就把便签撕掉，别人要看时重新抄一遍。第二，先删缓存再更新数据库，这种做法的问题是：删完缓存、数据库还没更新时，有读请求进来把旧值回填了缓存，导致脏数据长期存在；所以需要配合延迟双删——更新库后再延迟几百毫秒删一次，清掉并发回填的旧值。第三，订阅 binlog 异步失效，用 Canal 监听 MySQL 变更，通过消息队列广播给各节点删除缓存。这个方案一致性和覆盖面都更好，业务代码也干净，但额外引入 Canal、MQ 和消费服务，运维和排查成本明显上升。第四，如果确实要强一致，只能对读写路径加分布式锁串行化，那并发性能就没了，实际很少这么用。所以我的结论是：一致性不是免费的，要按业务容忍度选等级。绝大多数读多写少的场景（商品、用户资料）用"先更库再删缓存 + TTL 兜底"就够了；对一致性要求高的才上 binlog 订阅；强一致基本不用缓存。另外一定要记得，最终兜底永远是 TTL，它保证即使某次失效失败也能自愈。

**常见追问**：1）为什么"先删缓存再更新 DB"更容易出问题？2）Canal 方案本身有延迟，怎么避免读到旧数据？

---

## 63. redis的RDB和AOF两种持久化机制的原理，以及他们的适用场景是什么？

> 原题 ID：`q3491`

**高频程度**：★★★★★

**考察点**：RDB/AOF 的原理（快照 vs 追加日志）、fork+写时复制、刷盘策略、优缺点与适用场景、混合持久化。

**回答框架**：

1）RDB：某时刻全量数据二进制快照；触发方式 SAVE/BGSAVE 与 save 配置；核心是 fork+写时复制；优点是恢复快体积小，缺点是两次快照间数据会丢、fork 有开销；2）AOF：追加写命令日志，命令先入 aof_buf 再按 appendfsync 刷盘；优点是最多丢 1 秒（everysec），缺点是文件大恢复慢；3）混合持久化：AOF 文件含 RDB 头，兼顾两者；4）选型：备份与快速重启用 RDB，数据安全用 AOF，生产两者结合。

**参考回答**：

先讲 RDB。RDB 是把某一时刻 Redis 内存中的全部数据以二进制压缩形式写入磁盘，生成 dump.rdb 文件。触发方式有两种：手动命令 SAVE（阻塞主线程，不推荐）和 BGSAVE（fork 子进程写盘，主线程继续服务）；也有配置自动触发，比如 save 900 1 表示 900 秒内至少 1 个 key 变化就触发 BGSAVE。核心原理是 fork 加写时复制（COW）：fork 出的子进程与父进程共享内存页，父进程继续处理写请求时，被修改的页才复制一份，子进程看到的是 fork 那一瞬间的一致快照，因此能相对不阻塞地生成全量快照。优点是文件紧凑、恢复速度快、适合做备份和主从全量同步；缺点是两次快照之间的数据会丢失（可能丢几分钟），而且内存大时 fork 本身会短暂阻塞、写时复制还会带来额外内存占用。 再讲 AOF。AOF 以追加方式记录每一条写命令（RESP 协议的文本），重启时重放这些命令来恢复数据。流程是：写命令先追加到 aof_buf 缓冲区，然后按 appendfsync 策略刷盘——always 每条都 fsync 最安全但最慢，everysec 每秒一次最多丢 1 秒是默认策略，no 交给操作系统。AOF 文件会不断膨胀，所以需要重写：fork 子进程根据当前内存数据反推出最小命令集生成新文件，重写期间的增量写入重写缓冲区，最后合并替换。优点是数据更安全、丢失窗口小、可读性好（文本命令便于排查）；缺点是文件大、恢复慢、刷盘有开销。 适用场景上：RDB 适合做周期性备份、灾难恢复、快速重启、主从全量复制；AOF 适合对数据安全要求高的场景，作为主要恢复手段。生产中通常是两者结合：AOF 保证数据尽量不丢，RDB 用于备份和快速重启。Redis 4.0 之后还有混合持久化，把 AOF 重写后的文件前半部分写成 RDB 格式、后半部分是增量 AOF，兼顾恢复速度和数据安全，是现在比较推荐的方案。

**常见追问**：1）写时复制为什么能保证快照一致性？2）appendfsync everysec 会丢多少数据？

---

## 64. Jedis、Lettuce、Redisson、Spring Data Redis 的定位与选择是什么？

> 原题 ID：`q3504`

**高频程度**：★★★

**考察点**：Redis 客户端/接口的层次差异：原生命令、客户端封装（Jedis/Lettuce/Redisson/Spring Data Redis）、高级抽象，及连接模型、序列化、阻塞语义。

**回答框架**：

1）三类接口：原生命令接口、客户端封装接口、高级抽象接口（分布式对象）；2）连接模型：Jedis 同步阻塞+连接池；Lettuce 基于 Netty 支持同步/异步/响应式、连接可共享；Redisson 基于 Netty 偏分布式对象；3）序列化：默认 JDK 序列化易乱码体积大，一般换 String/JSON/Protobuf；4）语义差异：阻塞命令、批量 pipeline、事务与 Lua；5）选型：缓存用 Lettuce/Spring Data，分布式锁用 Redisson。

**参考回答**：

Redis 相关接口通常分三层。第一层是原生命令接口，比如 SET、GET、HSET，直接对应 Redis 协议，语义最清晰，但它只有单命令语义，不含连接管理、序列化和重试。第二层是客户端封装接口，比如 Jedis、Lettuce、Redisson、Spring Data Redis，它们在命令之上加了连接池、线程安全、序列化、重试、集群路由等能力。第三层是高级抽象接口，比如 Redisson 的 RLock、RMap、RTopic，把 Redis 能力映射成 Java 并发对象或消息组件，使用体验像本地对象。它们的区别主要体现在几点：一是连接模型，Jedis 是同步阻塞式，每个连接一个线程、通常要配连接池，连接不可共享；Lettuce 基于 Netty，支持同步、异步和响应式调用，连接是线程安全的可以共享，适合高并发；Redisson 也基于 Netty，但更偏分布式对象封装。二是序列化，Spring Data Redis 默认用 JdkSerializationRedisSerializer，容易产生乱码、体积大、还有反序列化安全问题，生产上一般换成 StringRedisSerializer 或 Jackson/Protobuf。三是功能语义，Pipeline 用于批量命令减少 RTT，事务用 MULTI/EXEC，需要原子逻辑用 Lua 脚本，分布式锁用 Redisson 的 RLock；这些在不同客户端里的支持程度和 API 差别很大。四是阻塞和超时语义，BRPOP 这类阻塞命令在不同客户端里的超时处理和连接占用方式不同。选型上，普通缓存读写用 Lettuce 或 Spring Data Redis 就够；需要分布式锁、限流器、延迟队列、布隆过滤器这些高级能力用 Redisson；老项目历史包袱可能还在用 Jedis，但要配好连接池和超时。

**常见追问**：1）Jedis 的连接池配小了会有什么现象？2）Redisson 的锁是怎么实现的？

---

## 65. Redis 出现热点 Key 时通常有哪些解决方案？

> 原题 ID：`q3555`

**高频程度**：★★★★★

**考察点**：热 Key 的本质（单分片承载超高 QPS）与三层解决方案：本地缓存、读写分离/多副本、key 拆分打散，外加限流与治理。

**回答框架**：

1）本质：Cluster 按 key 分片，同一 key 永远落同一 slot/节点，热 key 把单节点单线程打满，其他节点空闲；2）方案一：应用层本地缓存（Caffeine，短 TTL 1-5 秒）削掉大部分 QPS；3）方案二：读写分离加多副本，读请求分散到从节点（readFrom 策略）；4）方案三：把热 key 拆成多个 key（加随机后缀）分散到不同节点，读时随机取一个；5）方案四：入口限流、请求合并；6）配套：发现手段与监控。

**参考回答**：

先明确问题本质：Redis Cluster 按 key 做 CRC16 分片，同一个 key 永远落在同一个 slot、同一个节点上。所以当某个 key（比如秒杀商品、明星微博、大促爆款）QPS 达到几十万时，压力全部压在单个节点的单线程上，表现为该节点 CPU 打满、其他节点却很闲、延迟飙升，严重时拖垮整个集群。解决方案按成本从低到高有几层。第一，应用层本地缓存：用 Caffeine 或 Guava 在 JVM 里缓存热点数据，设置很短的过期时间，比如 1 到 5 秒，请求先查本地，未命中再查 Redis，这样 Redis 的 QPS 能被削掉 90% 以上。类比是把最常看的书放在自己桌上，而不是每次都去图书馆借。缺点是多实例之间有一致性延迟，适合商品详情、榜单这类对一致性要求不高的场景。第二，读写分离加多副本：主节点负责写，挂多个从节点，客户端通过 Lettuce 或 Jedis 的 readFrom 策略把读请求分发到从节点，把单节点的读压力分散到多台机器上。第三，把热 key 拆成多个 key：在业务上复制 N 份数据，key 加随机后缀（比如 hot:item:1001:0 到 hot:item:1001:9），读的时候随机选一个，这样同一个逻辑热 key 就分散到不同 slot 和节点上，单节点压力降到 1/N；写的时候要按同样规则写入所有副本，或者用定时任务统一刷新。第四，入口治理：在网关或服务层做限流、请求合并（同一时刻相同请求合并成一次查询）、以及对非核心请求降级，从源头减少对热 key 的访问。第五，配套要有发现能力：客户端埋点统计 key 访问频率，或者用 redis-cli --hotkeys 做低峰巡检，做到"能提前发现、而不是打挂之后才知道"。整体思路就是削峰（本地缓存）、分散（多副本和 key 拆分）、保护（限流降级）。

**常见追问**：1）热 key 拆成多副本后写入怎么保证各副本一致？2）本地缓存短 TTL 定多长合适？

---

## 66. Redis 支持哪些数据结构？各自典型场景？

> 原题 ID：`q4057`

**高频程度**：★★★★★

**考察点**：Redis 各数据结构与典型业务场景的一一对应。

**回答框架**：

1）String：缓存、计数、分布式锁；2）Hash：对象属性、部分字段更新；3）List：队列、最新列表、栈；4）Set：去重、交并差、抽奖；5）ZSet：排行榜、延迟队列、范围排名；6）Bitmap：签到、活跃统计；7）HyperLogLog：UV 基数估算；8）GEO：附近的人；9）Stream：消息流、消费组。

**参考回答**：

Redis 支持的数据结构及其典型场景可以一一对应。String 是最常用的，用于缓存值、计数器（INCR）、分布式锁（SET NX EX）、限流；Hash 是 field-value 结构，适合存对象属性，比如用户资料、商品信息，好处是可以只更新某个字段而不重写整个对象；List 是有序可重复的双端结构，适合做队列、栈、最新 N 条消息；Set 是无序不重复集合，适合去重、共同好友、抽奖、标签集合运算（交并差）；ZSet 是有序集合，每个成员带 score，适合排行榜、延迟队列（score 存时间戳）、按分数范围查询和排名；Bitmap 位图用于签到、活跃用户统计，极度省内存；HyperLogLog 用于 UV 基数估算，用极小的内存（12KB）估算海量去重计数，误差约 0.81%；GEO 用于附近的人、距离计算；Stream 是 Redis 5.0 引入的消息流结构，支持消费者组、消息确认、消息回溯，适合做轻量消息队列。总结成一句话的选型逻辑：要缓存和计数用 String，要存对象用 Hash，要队列和最新列表用 List，要去重和集合运算用 Set，要排序和排名用 ZSet，要位统计用 Bitmap，要估算基数用 HyperLogLog，要位置服务用 GEO，要可靠消息流用 Stream。

**常见追问**：1）HyperLogLog 为什么能省这么多内存？2）什么时候该用 Stream 而不是 List？

---

## 67. RDB 和 AOF 两种持久化区别？如何选型？

> 原题 ID：`q4058`

**高频程度**：★★★★★

**考察点**：RDB 快照与 AOF 命令日志的区别、刷盘策略与选型、混合持久化。

**回答框架**：

1）RDB：定时快照，文件小、恢复快，但可能丢最后一次快照后的数据；2）AOF：追加写命令，数据更安全，可设 everysec 刷盘，文件大、恢复慢；3）对比维度：数据安全、恢复速度、文件体积、对主流程影响；4）生产实践：AOF 保数据 + RDB 做备份/快速重启，或直接用混合持久化；5）Redis 7 支持 multi-part AOF。

**参考回答**：

RDB 是定时快照持久化，把某一时刻的全量内存数据以二进制写入磁盘。它的优点是文件紧凑、恢复速度快、对主流程影响相对可控（BGSAVE 用 fork 子进程），适合备份和快速重启；缺点是两次快照之间的数据会丢失，可能丢几分钟，而且实例内存大时 fork 本身会短暂阻塞、写时复制会带来额外内存开销。AOF 是追加日志持久化，记录每一条写命令，重启时重放来恢复数据。它的优点是数据更安全，用 appendfsync everysec 时最多丢 1 秒，always 时几乎不丢；缺点是文件比 RDB 大得多、恢复时要逐条重放所以慢，而且长期运行需要 AOF 重写来控制文件体积。从几个维度对比：数据安全性上 AOF 更好；恢复速度上 RDB 更快；文件体积上 RDB 更小；对主流程的影响上，RDB 主要是 fork 阻塞和 COW 内存开销，AOF 主要是刷盘开销（always 影响最大，everysec 可能有偶发阻塞）。选型上，生产环境通常两者结合：用 AOF 保证数据尽量不丢作为主要恢复手段，同时保留 RDB 用于周期性备份、灾难恢复和主从全量同步。如果不折腾两套，直接用混合持久化（aof-use-rdb-preamble，Redis 4.0+ 默认开启），AOF 文件前半部分是 RDB 格式的全量快照、后半部分是增量命令，兼顾恢复速度和低丢失率；Redis 7.0 还引入了 multi-part AOF，把基础文件和增量文件分离，优化了重写时的内存和磁盘开销。最后补一句判断依据：如果 Redis 只是纯缓存、数据能回源重建，持久化要求可以放低；如果把它当存储用，必须开 AOF 或混合持久化。

**常见追问**：1）为什么 AOF 恢复比 RDB 慢？2）multi-part AOF 解决什么问题？

---

## 68. 缓存穿透、击穿、雪崩分别是什么？如何解决？

> 原题 ID：`q4059`

**高频程度**：★★★★★

**考察点**：缓存穿透/击穿/雪崩的定义、触发条件与对应的解决方案。

**回答框架**：

1）穿透=数据不存在（缓存和库都没有）；解决：布隆过滤器、缓存空值、参数校验限流；2）击穿=单个热点 key 过期瞬间并发回源；解决：互斥锁重建、逻辑过期（不真删）、热点不过期；3）雪崩=大量 key 同时过期或 Redis 宕机；解决：过期时间加随机、多级缓存、高可用集群、熔断降级；4）一句话区分三者；5）治理要组合使用并有监控。

**参考回答**：

这三个概念要能一句话区分，再分别讲方案。缓存穿透是"数据不存在"：请求的数据在缓存和数据库里都没有，每次请求都绕过缓存直接打到数据库，典型是恶意用不存在的 ID 刷接口。解决方案：第一，缓存空值，查不到就写一个占位符并设较短 TTL，下次直接返回空，不再查库；第二，布隆过滤器在入口拦截，它说"不存在"就一定不存在，能挡掉绝大多数非法 key，注意有误判率且标准版不支持删除；第三，参数校验和限流，在网关层做格式和范围校验，对异常来源限流。缓存击穿是"单个热点 key 过期时的并发"：某个高并发热点 key 在过期瞬间，大量请求同时未命中，全部去查库并回写，形成瞬时冲击。解决方案：第一，用互斥锁或分布式锁，只让一个线程回源重建，其他线程等待或返回旧值；第二，逻辑过期，物理上不设 TTL，value 里带逻辑过期时间，发现过期后异步重建，其他请求先返回旧数据，没有空窗期；第三，对超热点数据干脆不设过期，靠后台任务定时刷新。缓存雪崩是"大量 key 同时失效或缓存整体不可用"：比如一批商品统一设了两小时过期同时失效，或者 Redis 节点/集群宕机，导致请求全压到数据库甚至把库打挂。解决方案：第一，过期时间加随机值，把失效时间打散；第二，多级缓存（本地缓存 + Redis）减少对 Redis 的依赖；第三，架构上做主从加哨兵或 Cluster 保证高可用；第四，必须有熔断降级和限流，数据库压力大时快速失败返回兜底内容，保护数据库。最后强调一点：这三者往往需要组合治理，并且要通过监控（缓存命中率、DB QPS、慢查询）来及时发现问题，而不是等出事再处理。

**常见追问**：1）三者的根本区别用一句话怎么概括？2）逻辑过期方案的核心思想是什么？

---

## 69. 主从复制、哨兵、集群分别解决什么？

> 原题 ID：`q4064`

**高频程度**：★★★★★

**考察点**：主从复制（读写分离）、哨兵（自动故障转移）、Cluster（分片水平扩展）各自解决的问题与 16384 槽。

**回答框架**：

1）主从复制：一主多从，读写分离、数据备份，但故障需人工切换、写能力和容量受限于主节点；2）哨兵：监控主从、自动判活选举、故障转移、通知客户端，解决高可用；3）Cluster：16384 个槽分片、多主多从、水平扩展，解决单机写入能力和容量上限；4）演进关系与组合方式；5）各自适用场景。

**参考回答**：

这三种模式分别解决不同层面的问题。主从复制解决的是"读扩展和数据冗余"：一主多从，写操作只在主节点执行，读请求分到从节点，实现读写分离提升读吞吐，同时从节点作为数据备份。它的局限是主节点仍是单点，故障需要人工切换，而且写能力和内存容量都受限于单机，主从复制还是异步的，切换时可能丢数据。哨兵解决的是"高可用"：在主从基础上增加一组哨兵进程，它们周期性 PING 监控节点存活，先标主观下线、达到 quorum 后标客观下线，然后选举 leader、从从节点中挑出新的主节点、通过发布订阅通知客户端切换，实现自动故障转移。它不解决容量和写扩展问题，本质还是主从，切换期间有短暂不可用，配置上哨兵建议部署奇数个以形成多数派。Cluster 解决的是"水平扩展"：把数据按 16384 个哈希槽分片到多个主节点，每个 key 通过 CRC16 取模定位槽，槽归属某个节点；每个主节点再挂从节点保证分片高可用。这样内存总量和写能力都能横向扩展，突破了单机的容量和写入瓶颈。代价是跨槽的多 key 操作受限（需要用 hash tag 把相关 key 放到同一槽）、客户端要支持集群协议、运维复杂度更高。演进关系上：单机 → 主从复制（解决读和冗余）→ 哨兵（解决高可用）→ Cluster（解决容量和写扩展）。实践中选型看需求：只读压力大就用主从；要求自动故障转移就用哨兵；数据量和写入量超过单机上限就必须用 Cluster。

**常见追问**：1）Cluster 模式下客户端怎么路由？2）哨兵和 Cluster 能不能一起用？

---

## 70. ZSet 底层如何实现？为什么能排序？

> 原题 ID：`q4066`

**高频程度**：★★★★★

**考察点**：ZSet 的双结构实现（跳表 + 哈希表）、各自负责的操作与复杂度、元素共享机制。

**回答框架**：

1）需求：既要按 score 排序做范围查询和排名，又要按 member 做单点查找；2）跳表：按 score 有序，支持范围查询 O(logN)、排名靠 span；3）哈希表：member→score 映射，单点查找 O(1)；4）两者共享同一份 member/score 元素，只多存指针和索引，不重复存数据；5）跳表层数随机（25% 晋升、最高 32 层），空间换时间；6）小规模时用 ziplist/listpack 编码。

**参考回答**：

普通编码下，Redis ZSet 通常用字典建立 `member -> score` 的快速定位，同时用按 score 排序的跳表支持范围遍历；它们服务于不同访问路径，不能简单说“完全只存一份数据”。成员字符串可被共享引用，但索引节点、分值和元数据仍有额外成本。

小 ZSet 可使用更紧凑的编码，阈值和单向转换细节受 Redis 版本和配置影响。设计时应通过目标版本的 `OBJECT ENCODING`、内存采样和命令延迟评估，而不是依赖固定的 128/64 一类常数。

**常见追问**：为什么按分数范围查询不能只依赖 member 到 score 的字典？

---

## 71. Pipeline 和事务（MULTI/EXEC）的区别？

> 原题 ID：`q4068`

**高频程度**：★★★★

**考察点**：Pipeline 与事务（MULTI/EXEC）的区别：网络往返优化 vs 原子性，以及各自是否支持回滚。

**回答框架**：

1）Pipeline：把多条命令批量发送、一次性收结果，减少 RTT，提升吞吐；服务端仍逐条执行，不保证原子性，中间可能插入其他客户端命令；2）事务 MULTI/EXEC：命令先入队，EXEC 时一次性按序执行，期间不会被打断（原子性）；3）共同点：都不支持回滚，运行时错误只影响该条命令；4）事务还有 WATCH 做乐观锁、DISCARD 放弃；5）选型：追求吞吐用 Pipeline，追求原子用事务或 Lua。

**参考回答**：

Pipeline 和事务是两个不同层面的东西。Pipeline 解决的是网络往返问题：正常情况下客户端发一条命令要等 Redis 回复，一次 RTT 就浪费了；Pipeline 的做法是把多条命令一次性打包发给服务端，服务端逐条执行后把结果一起返回，这样一条连接的 RTT 只花一次，吞吐能提升很多倍。要注意 Pipeline 不保证原子性，服务端仍然是逐条执行命令的，执行过程中完全可能插入其他客户端的命令，所以它只优化性能，不提供隔离或原子保证。事务 MULTI/EXEC 解决的是原子性问题：MULTI 之后命令不立即执行而是入队，直到 EXEC 才一次性按顺序执行，执行期间不会被其他客户端命令插入，形成了"打包原子执行"的效果；还有 DISCARD 放弃事务、WATCH 监视 key 做乐观锁（如果被其他客户端改动，EXEC 会失败）。两者的共同点是都不支持回滚：Redis 事务里如果某条命令在运行时出错（比如类型不匹配），只有那条命令失败，后面的命令照常执行，已经执行的也不会回滚；只有入队阶段就发现的错误（命令不存在、语法错）会让 EXEC 整体拒绝。所以严格说 Redis 事务不满足 ACID 的原子性。选型上：如果只是要减少网络开销、批量读写，用 Pipeline；如果需要命令之间不被打断，用 MULTI/EXEC；如果需要"判断加操作"这种真正需要原子的复合逻辑，最好的方式是把逻辑写进 Lua 脚本，它既能保证中间不被打断，又比事务更灵活。实践中 Pipeline 和事务也可以组合使用。

**常见追问**：1）为什么 Pipeline 不保证原子性？2）WATCH 失败后怎么处理？

---

## 72. Redis 的过期删除策略和内存淘汰策略？

> 原题 ID：`q4062`

**高频程度**：★★★★

**考察点**：考察过期键清理与 maxmemory 淘汰的区别，以及版本和配置会影响具体调度细节。

**回答框架**：

1) 过期键通过惰性删除与主动过期周期协同处理；2) 主动周期按配置和过期压力自适应执行，不背固定抽样数字；3) maxmemory-policy 仅在内存达到上限时生效；4) 依据缓存价值和写入语义选策略；5) 用命中率、延迟和淘汰量验证。

**参考回答**：

Redis 过期键清理通常结合惰性删除和后台主动过期：访问键时检查 TTL，后台也会在时间预算内持续检查过期键。具体抽样、频率和自适应行为随版本与配置变化，不应背诵固定“每秒几次”的数字。

内存淘汰只在设置 `maxmemory` 且达到上限时生效。标准策略包括 `noeviction`、`allkeys-lru`、`volatile-lru`、`allkeys-lfu`、`volatile-lfu`、`allkeys-random`、`volatile-random`、`volatile-ttl`；`volatile-*` 只考虑带 TTL 的键。选择应根据可重建性和命中价值，并监控淘汰、命中率、延迟与内存。

**常见追问**：为什么过期清理与 maxmemory 淘汰必须作为两套机制分别分析？

**核验资料**：[Redis key eviction](https://redis.io/docs/latest/develop/reference/eviction/)

---

## 73. Redis 从节点如何通过 replication ID 和 offset 判断与主节点的同步状态？

> 原题 ID：`q1416`

**高频程度**：★★★★

**考察点**：考察 Redis replication ID、offset 和 replication backlog 如何共同决定部分重同步或全量同步。

**回答框架**：

1) 说明主节点复制流的 ID 和递增 offset；2) 从节点保存已处理的 ID/offset；3) 重连时通过 PSYNC 发送已知位置；4) 主节点判断 ID 与 backlog 覆盖范围；5) 解释切主后的第二 ID 和全量同步兜底。

**参考回答**：

Redis 主节点为复制流维护 replication ID 和递增 offset；从节点记录自己已处理到的 replication ID/offset。连接中断后，从节点通过 PSYNC 告诉主节点自己最后看到的 ID 与 offset，主节点据此判断它是否仍能从 replication backlog 中补发缺失命令。

当 ID 匹配且 offset 仍落在 backlog 可覆盖的范围内，可进行部分重同步；ID 不匹配、backlog 已覆盖不到该位置或其他条件不满足时，需要全量同步。发生故障转移时，旧主复制流与新主的历史衔接需要额外处理，Redis 会维护第二 replication ID 等信息帮助判断可延续的历史。

具体诊断应查看 `INFO replication` 中的角色、ID、offset、backlog 和连接状态，并结合版本和故障转移时序；不要只比较一个 offset 数字就断定数据一定一致。

**常见追问**：为什么 backlog 太小会提高网络短暂抖动后触发全量同步的概率？

---

## 74. Redis 为什么快？

> 原题 ID：`q4063`

**高频程度**：★★★★★

**考察点**：考察 Redis 性能来自内存、事件驱动、数据结构和命令复杂度的组合，而不是“单线程”这一句口号。

**回答框架**：

1) 先看数据是否驻留内存及网络路径；2) 区分命令执行与 I/O 线程；3) 说明事件循环和非阻塞 I/O；4) 结合具体数据结构/命令复杂度；5) 用慢命令、延迟和资源指标定位瓶颈。

**参考回答**：

Redis 许多常用操作快，首先因为工作集通常在内存中，避免了磁盘随机 I/O；其事件驱动、非阻塞网络 I/O 和较简单的 RESP 协议也降低了连接处理开销。核心命令执行路径长期以单线程串行化为主，这减少了共享数据结构上的锁竞争，但并不意味着所有 Redis 工作都只有一个线程：不同版本可使用 I/O 线程，持久化、删除等也可能由后台线程处理。

性能还取决于命令和数据结构。小而受控的 String/Hash/Set 操作与一次遍历百万成员、超大 value、复杂 Lua 或阻塞命令的成本完全不同；持久化、复制、网络带宽、CPU、内存碎片和客户端输出缓冲也会成为瓶颈。

排障应看命令延迟、slowlog、`LATENCY` 事件、CPU、网络、复制和内存指标，并按具体 workload 选择拆分、限流、批量或架构调整。

**常见追问**：为什么一个 O(N) 命令即使只执行一次，也可能影响同实例上的其他客户端？

---

## 75. Redis 的发布订阅（pub/sub）和 Stream 区别？

> 原题 ID：`q4067`

**高频程度**：★★★

**考察点**：Pub/Sub 广播无持久化 vs Stream 持久化可靠队列

**回答框架**：

1. Pub/Sub：广播、无持久化、离线丢失、无 ACK；2. Stream：持久化、消费组、ACK、阻塞读、回溯重放、pending；3. 场景选择

**参考回答**：

核心差异：① Pub/Sub 是广播模型，消息不持久化，消费者离线就丢，没有 ACK 与重投；② Stream 是持久化消息流（类似 Kafka），支持消费组、ACK、阻塞读取（XREAD BLOCK）、按 ID 回溯与重放、pending 消息管理。所以 Pub/Sub 适合实时通知、在线推送等允许丢消息的场景；Stream 适合需要可靠投递、可重放的消息队列场景。

**常见追问**：Stream 的 pending 列表是干什么的？怎么处理一直没 ACK 的消息？

---
