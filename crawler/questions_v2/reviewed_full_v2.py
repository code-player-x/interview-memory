"""Second-pass editorial decisions. IDs remain stable across rendering.

MERGED_INTO records the surviving question for each semantic duplicate.
OVERRIDES updates all teaching fields together so old premises cannot survive
in a framework or follow-up after the answer itself has been corrected.
"""

MERGED_INTO = {
    "q1921": "q1272", "q1784": "q1442", "q3977": "q151",
    "q0070": "q0003", "q3519": "q0003", "q1141": "q0003",
    "q1112": "q1113", "q3598": "q0003", "q1283": "q2991",
    "q1489": "q2161", "q1500": "q3426", "q1505": "q3426",
    "q1101": "q3426", "q1546": "q3426",
    "q3660": "q1094", "q1713": "q1441",
    "q1749": "q1748", "q3629": "q158",
    "q3744": "q1056", "q3904": "q1056",
    "q1105": "q1289", "q1153": "q1289", "q1848": "q0073",
    "q2212": "q1071", "q3565": "q1071", "q3653": "q1071",
    "q3475": "q3827", "q0036": "q3547", "q3209": "q1271",
    "q1345": "q3426", "q2129": "q0090", "q3463": "q0090",
    "q1711": "q1441", "q3447": "q1240",
    "q3582": "q3559", "q0071": "q152", "q2020": "q1094",
    "q2893": "q1203", "q2894": "q2112",
}

MOVE_TO = {
    "q1288": "agent", "q2975": "agent", "q3573": "agent",
    "q158": "safety", "q0010": "evaluation",
    "q1144": "agent", "q3764": "llm-pretraining", "q1178": "multimodal",
}


def question(title, focus, outline, answer, followup="", sources=()):
    return {
        "title": title,
        "kaodian": focus,
        "framework": "\n".join(f"{i}) {part}" for i, part in enumerate(outline, 1)),
        "answer": answer.strip(),
        "followup": followup,
        "sources": [{"title": label, "url": url} for label, url in sources],
    }


OVERRIDES = {
    "q3053": question(
        "Transformers 中的推测解码如何实现？适用条件是什么？",
        "草稿生成、目标模型验证、不同 tokenizer 的适配，以及加速的实际条件。",
        ["小模型起草多个 token", "目标模型批量验证并校正", "普通 assisted decoding 与 UAD", "测接受率与端到端耗时"],
        """推测解码用较便宜的 assistant model 连续起草若干 token，再由目标模型一次前向验证这些候选。接受的前缀直接进入输出，遇到不接受的位置则校正并继续。标准精确推测采样需要接受/拒绝与重采样算法来保持目标分布，不能简单把小模型生成的文本拼到结果里。

Transformers 可通过 `generate(..., assistant_model=...)` 启用相应路径。同 tokenizer 是常见的简单配置，但不是所有实现的必要条件：Universal Assisted Decoding 支持同时传入 `tokenizer` 和 `assistant_tokenizer`，通过重编码与对齐连接不同词表。具体模型、采样、batch 与缓存兼容性要核对所用版本。

收益取决于草稿模型速度、候选接受率、草稿长度、验证开销与重编码开销。小模型不够快、接受率低或服务已被高并发吃满时，可能没有加速。应在目标硬件和请求分布下同时测生成质量、每 token 延迟、吞吐与显存；保持分布不等于相同随机种子下一定输出逐字相同。""",
        "为什么候选接受率很高，也可能没有端到端加速？",
        [("Transformers assisted decoding", "https://huggingface.co/docs/transformers/assisted_decoding")]),
    "q3739": question(
        "GELU、ReLU 和 SiLU 的定义及非线性特征有什么区别？",
        "激活函数的数学形式、负半轴行为，以及不能从平滑性直接推断任务优劣。",
        ["ReLU 截断", "GELU 概率加权", "SiLU sigmoid 门控", "区分激活函数与门控 FFN"],
        """ReLU(x)=max(0,x)，正半轴线性、负半轴为零。在某次输入上输出负值被截断，不代表神经元永久死亡；持续得不到有效梯度才是所谓 dying ReLU 问题。

GELU(x)=x·Φ(x)，Φ 是标准正态分布的累积分布函数；SiLU(x)=x·sigmoid(x)，也是 β=1 的 Swish。二者平滑且允许一定负输出，和 ReLU 的硬截断不同，但平滑性本身不能证明它们在任何网络上更好。

并非所有大模型都使用 GELU。BERT 等模型采用 GELU，LLaMA 等模型采用带 SiLU 门控的 SwiGLU FFN。SwiGLU 是双分支相乘的模块结构，不是 GELU 的另一个名字。具体激活应结合模型结构、训练结果和算子实现比较；门控 FFN 的结构与参数配平由 FFN 专题展开。""",
        "负半轴有非零输出，是否意味着负半轴导数处处为正？",
        [("LLaMA", "https://arxiv.org/abs/2302.13971")]),
    "q3818": question(
        "Transformer 的 FFN 负责什么？SwiGLU 如何改变 FFN 的结构？",
        "逐位置特征变换、门控双分支与同参数预算比较；注意力本身也具有非线性。",
        ["标准 FFN", "与注意力的分工", "SwiGLU 公式", "参数与计算预算"],
        """标准 FFN 对各 token 独立应用共享的前馈网络，例如 `FFN(x)=W_down·φ(W_up·x+b_up)+b_down`。它在特征维度做非线性变换，注意力则在位置之间汇聚信息。不能说去掉 FFN 后 Transformer 就是线性网络：注意力权重由输入相关的 Q、K 和 softmax 产生，本身具有非线性。

SwiGLU 可写成 `W_down·[SiLU(W_gate·x) ⊙ (W_up·x)]`。相比普通两矩阵 FFN，它增加了一个投影分支，通过逐元素门控调节特征。比较时要调整中间维度以匹配参数或计算预算，不能在不等预算下直接归因于激活函数。

门控 FFN 在多种模型实验中取得良好结果，但收益取决于训练设置。FFN 常占较大参数比例，也参与事实和特征表示；这不意味着全部知识只存在 FFN，或修改注意力就不会影响事实能力。""",
        "忽略偏置时，三矩阵门控 FFN 如何与两矩阵 FFN 配平参数量？",
        [("GLU Variants Improve Transformer", "https://arxiv.org/abs/2002.05202")]),
    "q2033": question(
        "向量检索中 Flat、IVF、PQ 和 HNSW 分别解决什么问题？",
        "区分精确检索、候选分区、向量压缩和图索引，而非给索引做绝对排名。",
        ["Flat 精确扫描", "IVF 缩小候选", "PQ 压缩表示", "HNSW 图搜索及组合"],
        """Flat 对全部向量计算距离，可作为精确检索基线，代价随库大小增长。IVF 先把向量分到粗粒度分区，查询只访问部分分区，访问数越少通常越快但可能漏召回。

PQ 把向量分成若干子空间并编码成码本索引，主要减少存储和距离计算成本，但引入量化误差；它常与 IVF 组合成 IVF-PQ。HNSW 则建立多层近邻图，通过图遍历寻找近邻，通常需要额外的图边内存。

这些概念不完全互斥，实际系统可以组合索引与压缩。不存在同时在召回、延迟、内存、构建和更新上普遍最优的方案。比较时固定数据、距离度量和目标召回率，再测资源与延迟；HNSW 的参数和选型条件另由 HNSW 专题展开。""",
        "为什么比较 ANN 延迟时，需要同时固定或报告 Recall@k？",
        [("Faiss 索引选择指南", "https://github.com/facebookresearch/faiss/wiki/Guidelines-to-choose-an-index")]),
    "q3900": question(
        "LoRA 微调会发生灾难性遗忘吗？如何与全参数微调公平比较？",
        "冻结基座与保持行为的区别，以及目标任务收益和旧能力退化的联合评测。",
        ["有效权重变化", "没有固定的遗忘幅度", "控制对比条件", "回放、约束和回归"],
        """会。LoRA 冻结基座 W₀，训练低秩增量，例如有效权重 `W_eff=W₀+(α/r)BA`。未合并时 W₀ 的存储值不变，但启用适配器后的输出行为已经改变，因此旧任务能力仍可能下降。

低秩约束减少了更新自由度，却不保证遗忘一定比全参数微调小，更没有通用的下降分数或倍数。结果受数据分布、训练量、学习率、rank、目标模块和正则化影响。停用未合并的适配器可恢复基座路径，但这是模型切换，不等于适配后的同一个模型仍保有全部旧能力。

比较时固定基座和训练/评测数据，分别合理调参，报告目标任务收益与旧任务回归的权衡；仅固定步数未必公平。缓解措施包括混入代表性旧数据、限制训练强度、早停以及参考模型约束，并同时验证目标域与通用能力，不能只看训练 loss。""",
        "适配器关闭后基座恢复正常，是否能证明适配器启用时没有遗忘？",
        [("LoRA 论文", "https://arxiv.org/abs/2106.09685")]),
    "q1057": question(
        "分布式训练如何定位通信瓶颈并减少暴露在关键路径上的通信时间？",
        "通信性能诊断、计算重叠、分桶和拓扑优化，与 AllReduce 原理题分开。",
        ["区分带宽、延迟和慢 rank", "profile 暴露通信", "分桶与 overlap", "累积与分片的限制"],
        """先从 trace 判断是大消息受带宽限制、小消息受启动延迟限制，还是慢 rank 导致其他进程等待。区分 DP 梯度同步、TP 层内通信、PP 点对点通信和 MoE token 分发，统计消息大小、频次和关键路径上的等待。

反向计算完成一部分梯度后即可分桶发起同步，把通信与剩余计算重叠。桶过小会增加启动开销，过大会推迟首批通信；调桶大小要看暴露通信时间，而不是只看 NCCL kernel 总时长。再检查 rank 到设备的映射、网络带宽、跨节点路径和计算负载是否均衡。

梯度累积只有在中间 micro-batch 确实关闭不必要同步时才减少 DP 同步频次，不能自动消除 TP 通信。通信降精度或压缩需验证数值影响。ZeRO/FSDP 的通信随分片阶段和配置改变，不能一概说成用两次集合操作无代价替代 AllReduce。Ring 的通信量与轮数由 AllReduce 原理题单独说明。""",
        "为什么 NCCL 总耗时下降了，训练 step 时间却可能不变？"),
    "q3745": question(
        "AllReduce 的语义是什么？Ring AllReduce 的通信量如何计算？",
        "归约结果、单 rank 与集群总流量，以及带宽项和延迟项。",
        ["逐元素归约并分发", "ReduceScatter + AllGather", "明确 p 和 M", "梯度平均的前提"],
        """AllReduce 对 p 个 rank 的同形状张量逐元素归约，并让每个 rank 获得完整结果。例如 sum 得到所有输入之和；AllGather 只是收集各 rank 的数据，不做求和。

经典 Ring AllReduce 把每个张量分成 p 块，先经过 p−1 轮 ReduceScatter，再经过 p−1 轮 AllGather。设每个输入张量大小为 M 字节，则每个 rank 的发送量为 `2(p−1)M/p`，接收量相同；若把发送和接收相加则再乘 2。按发送字节计的集群总流量为 `2(p−1)M`。单 rank 的带宽项趋近 2M，但轮数随 p 增长，不能说耗时或全局通信量与卡数无关。

简化模型下时间约为 `2(p−1)α + 2(p−1)M/(pB)`，α 是每轮启动延迟、B 是有效链路带宽，真实表现还受拓扑和重叠影响。NCCL 可以按消息和拓扑选择不同算法，并非总用 ring。

普通数据并行用归约后的梯度做一致更新。各 rank 样本数相同时可对本地平均梯度再求平均；样本或有效 token 数不等时应按正确的全局损失归一化，不能机械除以 p。""",
        "两张卡的有效训练 token 数不同，为什么直接平均两个局部 loss 的梯度可能有偏？",
        [("NCCL collective operations", "https://docs.nvidia.com/deeplearning/nccl/user-guide/docs/usage/collectives.html")]),
    "q2319": question(
        "如何扩展大模型的上下文长度，并验证扩展后是否真正可用？",
        "位置表示适配、长序列训练、推理资源和长上下文评测。",
        ["窗口配置不等于能力", "位置插值与频率缩放", "长文本训练适配", "长短上下文共同回归"],
        """增加配置中的最大序列长度，只是放宽接口或运行时限制，不保证模型能正确利用更远位置。对 RoPE 模型，位置插值、频率缩放等方法调整长位置的表示；具体 base、缩放系数和适配训练量必须匹配模型与扩展倍数。

位置插值把更长序列的位置映射回较熟悉的范围，能缓解直接外推的困难，但不保证无损，也没有通用的固定 base 或训练步数。通常还需合适的长序列数据与训练安排，并检查短上下文能力是否退化。

系统侧要估算 KV Cache、prefill 计算、带宽和并发成本。FlashAttention 减少精确注意力的 IO 与中间显存开销，并不把其计算复杂度变成线性。

验证应同时覆盖远距离信息检索、多证据整合、长文推理与短任务回归，并把关键信息放在不同位置。能通过单一针检索测试，不等于能完成真实的长上下文任务。""",
        "扩展后针检索通过，但长文问答下降，你会怎样拆分评测定位？",
        [("Positional Interpolation", "https://arxiv.org/abs/2306.15595")]),
    "q2248": question(
        "如何理解大模型的“涌现能力”？评价指标为什么会影响这个结论？",
        "区分观测到的分数突变、连续能力变化与尚未证实的机制解释。",
        ["操作性定义", "非线性指标的影响", "多指标与多规模验证", "避免规模决定论"],
        """“涌现能力”通常描述一种观测：某些任务在较小模型上得分接近随机水平，规模增加后出现显著提升。它描述的是特定模型、训练设置、任务和指标下的现象，不是所有能力都有一个统一参数阈值。

指标会改变曲线形状。例如整道题必须全部正确才得分的 exact match，可能把逐步改善的 token 或步骤正确率表现成突然跃升。有研究据此质疑部分“涌现”是否是度量造成的外观；这不能反向证明所有新能力都是假象。

判断时应比较连续指标与离散指标、多个规模和训练预算，控制数据与提示差异，报告不确定性。小模型经过针对性数据、蒸馏、工具或任务分解也可能解决特定复杂任务；是否必须使用更大模型应由实际评测决定，不能仅凭“量变质变”下结论。""",
        "为什么整题准确率突然上升时，逐 token 损失可能仍然平滑下降？",
        [("Emergent Abilities", "https://arxiv.org/abs/2206.07682"),
         ("Are Emergent Abilities a Mirage?", "https://arxiv.org/abs/2304.15004")]),
    "q3331": question(
        "OAuth 2.0 授权码流程如何工作？PKCE 和客户端类型分别影响什么？",
        "授权与身份认证的区别、后通道换 token、PKCE 和秘密的保存边界。",
        ["授权请求与回调 code", "token endpoint 换取 token", "机密与公共客户端", "PKCE、回调校验和 CSRF 防护"],
        """客户端把用户引导到授权服务器，携带 client_id、redirect_uri、scope 等参数；使用 PKCE 时发送 code_challenge。用户完成授权后，服务器向已登记的回调地址返回短期授权码。客户端再到 token endpoint 提交 code、相应 redirect_uri 和 code_verifier，验证通过后获得 token。授权码一次性使用，并绑定客户端与回调。

机密客户端可以在后端安全保存 client_secret 或使用其他客户端认证方式；SPA、原生应用等公共客户端不能把内置 secret 当成可信秘密。PKCE 将兑换动作绑定到发起请求的一方，公共客户端必须采用，机密客户端也推荐采用。还要使用 TLS、严格校验回调 URI，并通过正确的 state/PKCE 等机制防止 CSRF。

授权码流程避免在授权响应里直接返回 access token，但不意味着所有 token 必须永远留在后端；公共客户端也可能持有 token。若要让浏览器只持有站点会话，可采用后端代管 token 的 BFF 架构。

OAuth 解决授权，不直接定义用户登录身份；身份认证通常使用 OIDC。微信小程序 code2Session 有自己的协议，不能直接充当标准 OAuth 授权码流程的完整说明。""",
        "PKCE 为什么不能代替机密客户端的身份认证，也不能消除 XSS 风险？",
        [("OAuth 2.0 RFC 6749", "https://www.rfc-editor.org/rfc/rfc6749.html"),
         ("OAuth Security BCP RFC 9700", "https://www.rfc-editor.org/rfc/rfc9700.html")]),
    "q1191": question(
        "以 E2B 为例，云端 Agent 沙箱如何实现隔离、执行和暂停恢复？",
        "区分隔离底座、执行接口、调度和持久化，并明确产品实现的版本边界。",
        ["microVM 隔离", "命令与代码执行接口", "调度和资源控制", "快照恢复的边界"],
        """云端 Agent 沙箱把文件、进程、命令与网络包装成有生命周期的远程执行环境。E2B 的公开架构采用 Firecracker microVM：通过虚拟化提供独立 guest 内核与内存隔离，同时由宿主控制网络、资源配额和镜像。

沙箱内的执行代理负责命令和文件接口；有状态的代码解释器还可接入相应语言运行时。具体是否使用 Jupyter、哪些语言可用，属于某个镜像或代码执行产品的实现，不能推断所有沙箱共享一套固定内核协议。

调度层选择有容量的节点并管理环境生命周期，调度器不是 microVM 隔离的必要组成。E2B Runtime 的公开架构描述调度器无关的服务并提供 Kubernetes 部署支持，因此不能把“必须使用 Nomad”当成长期原理。

暂停恢复可保存受支持的内存和文件系统状态，但外部连接、远程凭证与第三方状态未必仍有效，恢复后需重连和校验。启动时延、保留时长与计费取决于版本、资源和服务条款，不使用固定数字代表普遍保证。""",
        "沙箱内存恢复了，为什么对外数据库连接仍可能失效？",
        [("E2B Runtime 架构", "https://github.com/e2b-dev/runtime/blob/main/docs/ARCHITECTURE.md")]),
    "q1272": question(
        "CRAG 和 Self-RAG 分别如何改进检索增强生成？",
        "检索纠错与训练得到的反思 token 的区别。",
        ["CRAG 检索评估", "Self-RAG 反思 token", "训练要求", "效果与成本边界"],
        """CRAG 在检索后评估文档质量，再按结果选择后续路径：利用并精炼可信检索内容、转向网络搜索，或组合两类证据。其重点是降低低质量检索材料对生成的影响，检索评估通过不等于生成一定忠实。

Self-RAG 通过训练让模型生成反思 token，用于决定是否检索，并评价材料相关性、生成内容的证据支持程度和有用性。推理时利用这些信号进行检索控制与候选选择，而不是只在普通 prompt 中追加一句“请反思”。

两者都加入质量判断，但原始方法的训练流程与推理控制不同。用普通模型加提示词可以近似实现一些检查步骤，不能称为完整复现原版 Self-RAG。两者也都不能保证消除幻觉；延迟取决于实际触发路径、候选数和解码策略，不应简单等同于固定增加几次独立 LLM 调用。""",
        "不训练特殊反思 token 时，你实现的是哪些行为近似？",
        [("Self-RAG", "https://arxiv.org/abs/2310.11511"), ("CRAG", "https://arxiv.org/abs/2401.15884")]),
    "q0014": question(
        "Agentic RAG 如何把固定检索流程变成按需、多轮的检索决策？",
        "检索控制权、查询迭代、停止条件与适用任务。",
        ["固定流程基线", "检索工具化", "证据缺口驱动下一轮", "预算和终止条件"],
        """固定 RAG 流程通常按预设步骤检索并生成。Agentic RAG 把检索和其他数据访问能力暴露为工具，由控制器结合当前问题与已获证据，决定是否检索、访问哪个源、怎样改写查询以及是否继续。

例如回答“两个产品的保修政策有何差别”时，可以先分别查到政策，再发现某一版本日期不一致，补查对应历史版本后形成对照。下一轮查询应由具体的证据缺口驱动，不能只是重复原查询。

这种方式适合多跳问题、动态数据源和需要工具组合的任务，但增加了路由错误、循环和成本。应设置调用预算、去重、可验证的停止条件，以及缺证据时的说明或升级路径。Agentic RAG 是编排范式，不等同于 Self-RAG 这类特定训练方法；简单单跳问题可以继续采用固定流程。""",
        "如果每次检索都返回相似材料，怎样判断继续检索已没有收益？"),
    "q1442": question(
        "RAG 与长上下文如何选型，什么时候适合组合？",
        "证据选择与窗口容量的区别，以及实际质量、成本和更新约束。",
        ["RAG 按需选材料", "长上下文整体读材料", "任务与资源约束", "组合和公平比较"],
        """RAG 在外部数据中选择与问题有关的证据，再交给模型；长上下文允许单次请求容纳更多材料。前者解决材料选择，后者扩大可处理范围，两者可以组合。

材料规模可控、问题要求整体阅读或跨段综合时，直接使用长上下文可能更简单。知识库很大、更新频繁或有细粒度权限时，RAG 可以缩小输入范围并保留来源，但要承担解析、索引和漏召回风险。长窗口也不保证模型利用了所有内容，输入成本、首 token 延迟和位置敏感性仍需评测。

组合方式是先检索相关文档或章节，再利用较长窗口完成跨证据推理。比较时固定问题与可访问数据，分别测证据覆盖、答案正确性、引用、延迟和成本；不要仅按窗口标称长度决定。RAG 和直接读取都需要来源、版本与权限控制，引用能力不是 RAG 独有。""",
        "长上下文效果更好时，怎样判断收益来自补回漏检材料还是更强的模型？"),
}

OVERRIDES.update({
    "q0003": question(
        "如何划分 Agent 的工作状态、会话历史和长期记忆？",
        "不同信息的用途与生命周期，以及外部存储和模型上下文的区别。",
        ["任务状态", "会话历史", "跨会话记忆", "按需组装上下文"],
        """可以按用途划分三类信息。工作状态保存当前目标、步骤、已确认参数和中间结果，适合结构化表示；会话历史保存消息与工具交互，保证多轮连贯并支持回查；长期记忆保存跨会话仍有价值的偏好、事实和经验。

这些类别不一定对应三个数据库，也没有唯一术语标准。上下文窗口是本次推理可见的信息范围，持久化存储则是外部系统保存的信息；长期存储很多内容不代表模型本轮全部可见。

组装请求时保留当前任务的必要状态与近期交互，按权限和相关性加载长期记忆，并控制总 token 预算。各层通过稳定 ID、版本和来源连接。写入治理、去重、存储实现、召回和淘汰分别是独立职责，不能用一个向量库替代全部设计。""",
        "任务状态已持久化，为什么恢复后仍可能漏掉用户早先确认的约束？"),
    "q151": question(
        "长期记忆由谁决定写入？新信息与已有记忆冲突时如何处理？",
        "模型提议与确定性校验、来源可信度、冲突裁决和可撤销性。",
        ["候选提议", "来源和字段校验", "按事实类型处理冲突", "审计和删除"],
        """模型可以提议记忆候选，但写入服务负责检查类型、来源、权限、保留期限和幂等性。应区分用户明确表达、可信业务系统记录、外部材料与模型推断；模型自报置信度不是事实证据，用户自述也不能用于提升权限或修改权威账户信息。

冲突处理不能统一采用“最新文本优先”。可变偏好可按用户明确更新生效；订单状态等应以权威系统为准；不同主体、时间范围或条件下的记录可能并不矛盾。无法确定时保留待确认状态，不静默覆盖。

每条记忆记录来源、有效时间和修订关系，提供纠错与删除能力。必要的历史只在授权的保留期限内保存，用户删除或合规要求删除时还要处理索引、缓存和副本，不能为了审计永远保留旧值。外部内容不得通过记忆写入变成高优先级指令。""",
        "网页内容声称用户拥有管理员权限，应在哪一层阻止它成为长期记忆？"),
    "q1113": question(
        "用户重复表达同一事实或偏好时，如何给长期记忆去重？",
        "精确幂等、候选召回、语义等价判断与来源保留。",
        ["请求与文本去重", "检索候选", "确认是否同一命题", "合并证据而非重复条目"],
        """先用来源事件 ID 或幂等键处理同一写入请求的重复投递，规范化文本哈希可消除完全重复。改写表达则用实体、字段、时间范围和语义检索找候选。

向量相似度仅用于召回，不能直接作为合并依据。“喜欢短回答”和“不喜欢短回答”可能很相似，但含义相反。只有确认主体、条件和命题一致后才合并事实条目，并保留多个来源或出现次数；不应把一条记录的转发次数当作独立证据累加置信度。

并发写入还需唯一键或事务/CAS，避免两个请求都查不到后分别插入。定期检查漏重与误合并，分别评估两类错误。匹配阈值由数据校准，不给跨模型通用的相似度数值。""",
        "如何区分独立重复表达和同一条消息被系统重放？"),
    "q3586": question(
        "如何判断两条记忆是等价、矛盾，还是仅仅主题相似？",
        "语义判定中的否定、主体、时间和条件，以及不确定时的处理。",
        ["结构化约束", "关系分类", "有标注集校准", "保留待确认状态"],
        """先对齐主体、关系、时间范围和适用条件，再判断命题关系。相似度高只说明文本接近，不能区分等价、矛盾和补充信息。例如“以前住北京”和“现在住上海”可同时成立；“喜欢花生”和“花生过敏”也不能合并成同一个偏好。

候选召回后可用规则、实体链接和自然语言推断模型辅助分类；高风险或低置信样本进入待确认队列。最终写入策略应消费这个关系判定，而不是由相似度阈值直接触发覆盖。

用人工标注的正例、否定、时间变更、同名实体和条件差异样本分别测误合并、漏合并与冲突识别。按任务校准阈值并允许弃权，不能宣称多路结果一致就保证事实正确。""",
        "为什么“相似”和“等价”必须使用不同的评测标签？"),
    "q1284": question(
        "多个 Agent 的记忆如何隔离与共享？",
        "角色私有状态、显式共享契约和跨角色权限。",
        ["定义 scope", "私有与共享状态分开", "消息契约", "并发和删除传播"],
        """先定义用户、租户、项目、任务和角色等 scope。每个 Agent 的临时轨迹与私有结果单独保存；需要协作的目标、已确认事实和交付物进入显式共享区，附来源、版本和读写权限。

角色之间传递完成子任务所需的结构化摘要或对象引用，接收方读取引用时重新校验权限。不能因为同属一个任务就复制所有历史，也不能默认某个“审核角色”拥有全量数据权限。

共享状态的冲突通过字段所有权、版本校验或确定性合并处理；敏感信息撤回或删除后，共享副本与缓存也需失效。评价应关注跨角色信息缺失、越权泄漏与冲突恢复，而不是只看各角色是否能回答问题。""",
        "子 Agent 已缓存的共享记忆被撤回后，怎样避免继续使用旧值？"),
    "q3521": question(
        "Agent 会话与长期记忆如何持久化，崩溃后如何恢复？",
        "权威数据、索引与缓存分工，恢复点和一致性。",
        ["消息事件与任务快照", "事实主存储", "可重建索引", "恢复和一致性"],
        """消息历史可保存为追加事件，任务状态保存带版本的 checkpoint；恢复时读取一致的快照与后续事件。内存只适合临时缓存；使用 Redis 等系统时，是否满足持久性要求取决于落盘、复制和故障恢复配置。

长期事实可以存入关系库、KV、文档或文件，保留稳定 ID、scope、来源、有效期和版本。向量索引用于相似检索，可作为主库的派生视图；索引中返回 ID 后回查有效记录，避免已删除或旧版本内容继续进入上下文。

主记录与索引异步更新时，可使用 outbox/事件队列加幂等消费者并监测延迟。恢复 checkpoint 不代表外部工具副作用已回滚，需结合已执行步骤和幂等键处理。存储方案应由一致性与恢复目标决定，而不是简单按“短期用 Redis、长期用向量库”固定分配。""",
        "主库更新成功但 embedding 写入失败，应如何恢复检索一致性？"),
    "q3591": question(
        "长期记忆如何设置过期、归档与删除策略？",
        "排序降权、存储淘汰和真正删除是不同动作。",
        ["按类型定义保留期限", "降权与归档", "失效与删除传播", "验证遗漏和残留"],
        """先按信息类型与用途定义保留期限。临时任务信息可以短期过期，持续有效的用户设置则由明确变更控制；不能仅因某条信息很久没被查询就判为无用，也不能默认敏感信息永久保留。

相关性或时间衰减可以降低召回优先级，归档可以降低热存储成本，但二者都不等于删除。被明确更正、撤回或依法要求删除的信息，应从可用集合移除，并使相关索引、缓存和派生摘要失效；备份按既定保留和恢复流程处理。

淘汰策略需评估关键低频事实被误删、过期事实继续命中和删除后残留三类问题。上下文中暂时不加载某条记录，不应被统计为已经从长期记忆删除。""",
        "向量条目已删除，旧摘要仍含该事实时，删除请求算完成了吗？"),
    "q3595": question(
        "长对话中 Agent 丢失早期关键信息，应该沿哪些环节排查？",
        "从原始输入到最终行为逐段确认信息是否存在、被加载并被利用。",
        ["实际请求", "摘要与截断", "存储及召回", "模型利用和回归"],
        """先复现失败并查看实际发给模型的消息与 token 预算，确认关键事实是否在本次输入中。不要根据应用界面仍显示旧消息，就认定模型一定收到了它。

若事实缺失，回查截断、摘要、字段过滤和权限逻辑，再查原始记忆是否成功写入、被覆盖或过期。若记录存在却未进入候选，检查检索 query、索引版本和过滤条件；若已召回却未进入请求，检查重排和预算裁剪。

若事实已在实际上下文中，继续检查冲突信息、位置、指代和模型任务能力，不把所有问题都归因于存储。用脱敏 trace 保留可重放证据，并构造不同轮数、位置和干扰量的回归样本。修复应针对具体断点，而不是直接扩大上下文窗口。""",
        "如何区分“没有召回”和“召回后模型没有正确利用”？"),
    "q1288": question(
        "长期记忆应该按什么粒度存储？",
        "原子事实、条件范围与事件片段之间的取舍。",
        ["可独立更新的命题", "保留必要上下文", "事件与经验片段", "粒度评测"],
        """以可独立校验、更新和复用的语义单元为起点。偏好和业务事实可拆成主体、属性、值及条件；不能为追求短小而丢掉否定、时间或适用范围，例如“出差时住酒店”不能简化成“住酒店”。

事件和操作经验需要保留步骤间关系，可存为带摘要的事件片段，并引用完整轨迹。把整段会话做成一个向量容易混入多个主题；过度拆分又会使检索命中碎片却无法理解。

可把原子事实、事件摘要与原始记录用 ID 关联，检索后按预算补回必要上下文。通过独立更新的准确性、关键条件保留率与召回后的可用性选择粒度，不以固定字符数代替语义判断。""",
        "一条偏好只有在特定任务下成立，拆分时怎样保留这个条件？"),
    "q2975": question(
        "Agent 何时召回长期记忆，如何决定哪些记录进入上下文？",
        "检索触发与候选选择的区别，以及模型工具式读取的边界。",
        ["触发策略", "权限过滤与候选", "相关性和有效性", "预算与效果评测"],
        """召回可以由固定规则、路由器或模型调用记忆工具触发。例如涉及“按我之前的偏好”时通常需要历史，独立计算题则未必需要。模型选择调用工具属于运行时决策，不是把外部记录自动变成了权重知识。

检索先按用户、租户和任务权限限定范围，再用字段查询、关键词或语义检索召回候选。重排时结合当前问题、记录有效期和来源，冲突未解决的记录不应当成已确认事实。

进入上下文前按预算去重和裁剪，并保留来源供回查。触发过少会遗漏个性化信息，触发过多会增加成本和旧信息干扰；应分别统计漏召回、无用注入和对任务成功率的影响。""",
        "哪些测试能够发现记忆检索越多、最终回答反而越差？"),
    "q3573": question(
        "什么时候适合用 Markdown 文件保存 Agent 记忆？它与 RAG 是什么关系？",
        "存储格式与访问机制属于不同维度。",
        ["文件的可读可编辑性", "并发和规模限制", "文件也能被检索", "按访问需求选型"],
        """Markdown 适合规模较小、需要人工审核和直接编辑的项目说明、偏好或任务笔记。文件可以版本化，结构简单，但仍需权限、原子写入和并发冲突处理；多用户、高频局部更新或复杂查询时，数据库通常更方便。

RAG 是检索证据后生成的访问方式，并不要求材料一定在向量库。Markdown 可以全量加载、按标题读取、用文本搜索检索，也可以被切块建立向量索引。因此“Markdown 与 RAG”不是互斥选项。

选择时看记忆规模、更新并发、查询模式和 token 预算。直接读取最新文件时编辑可立即体现；如果建立了派生索引或缓存，同样需要更新和失效机制。可读文件也不天然比其他存储更可信。""",
        "记忆文件已经修改，但模型仍引用旧内容，可能有哪些缓存或索引环节？"),
    "q0064": question(
        "Agent Memory 和 RAG 的概念边界是什么？",
        "记忆是信息管理目的，RAG 是使用外部证据的机制，二者可以重叠。",
        ["Memory 管理交互和状态", "RAG 检索后生成", "记忆可用 RAG 读取", "共享的治理要求"],
        """Agent Memory 通常指对交互历史、任务状态、偏好或经验的保存与再利用；RAG 指从外部信息源检索材料以辅助生成。前者描述信息的用途与生命周期，后者描述获取和使用证据的流程。

记忆可通过精确字段、文件读取或 RAG 被访问；RAG 的材料也可以是历史对话，不限于静态文档。因而不能把 RAG 固定描述为只读、静态，或认为它无需处理更新、冲突与删除。

二者共同需要权限、来源、版本和有效性控制。区别应落在业务需求：个性化助手重视跨会话状态，知识问答重视证据覆盖与来源；具体记忆写入和生命周期由 Agent 记忆专题展开。""",
        "检索历史对话回答用户问题时，为什么它可以同时属于 Memory 和 RAG？"),
})

OVERRIDES.update({
    "q3464": question(
        "如何设计一个客诉分析 Agent，使归因结论能追溯到原始工单？",
        "客诉分析的数据关联、证据约束和可复核输出。",
        ["定义工单字段", "检索历史证据", "生成有依据的归因", "人工复核与统计"],
        """这是一道方案设计题。先定义输入：脱敏工单、产品型号、故障现象、时间和处理记录；用稳定工单 ID 关联结构化字段与原始文本，避免不同产品或版本的故障被混为一谈。

流程可拆成现象抽取、候选原因检索、证据比对和报告生成。模型给出的原因标记为已证实、推测或证据不足，每个事实引用具体工单或知识条目；产品型号、政策版本和时间范围用于过滤证据。引用存在也不等于推断成立，需检查证据是否支持结论。

输出统一为原因类别、证据 ID、未确认事项和建议后续动作，便于人工审核与聚合统计。框架按流程复杂度选择，不能从题目推断某家公司真实用了什么模型或技术栈。评测用标注工单检查归因正确性与证据支持率，项目收益只填写真实测量数据。""",
        "同一故障现象对应多个可能原因时，报告怎样表达不确定性？"),
    "q3465": question(
        "如何复盘一次 Agent 效果问题，并证明改动确实带来了改善？",
        "真实证据、根因定位、对照实验与结果口径。",
        ["失败样本与分母", "定位链路断点", "逐项改动和对照", "报告收益与代价"],
        """回答应基于真实项目或明确标注的练习实验。先描述任务、失败现象和受影响样本，说明成功判定、观察周期、样本量及失败占比；没有数据时说明未测量，不填入示例数字充当成果。

再用 trace 定位断点，例如工具选择正确但参数继承了旧设备 ID。通过替换单个环节、补充校验或固定输入重放，检验是否真是这个原因；不能仅凭修改 prompt 后分数上升就排除其他变量。

每次改动在同一代表性回归集上比较，保留未用于调参的测试集；同时检查成功率、延迟、成本和原有能力。上线后用灰度或合适的对照确认效果，记录失败残留和回退条件。可按“问题—证据—假设—改动—验证—限制”组织复盘，不预设提升幅度。""",
        "怎样排除成功率上升只是因为新版流量中的问题变简单？"),
    "q3611": question(
        "AI 工具订阅、模型官方 API 与第三方代理 API 应如何区分和选型？",
        "成品工作流、可编程接口和供应链之间的区别。",
        ["产品与接口", "官方直连与代理", "费用和接入能力", "条款与可观测性"],
        """工具订阅购买的是具体产品及其工作流，API 提供可编程能力；二者可能分别计费，买了订阅不代表获得 API 额度。计费可能包含席位、用量和套餐限制，不能一概认为订阅边际成本固定或 API 只按 token 收费。

官方 API 直接对接模型提供方，第三方代理还增加了转发、聚合或托管环节。第三方不等于必然不可靠，但需要核实实际模型版本、数据流向、错误与限流语义、凭证管理和故障责任。

直接使用成熟工作流时比较交互体验与团队管理能力；嵌入业务系统时比较接口、可观测性、可定制性和总成本。数据是否用于训练、保留多久、能否审计，应核对具体服务与套餐条款，不能仅凭“订阅/API”名称判断。题目无需询问个人是否花钱购买。""",
        "一个工具已有企业级审计能力时，是否仍然必须自建 API 应用？"),
    "q1645": question(
        "可复用 Skill 如何在多个 Agent 之间保持契约和业务规则一致？",
        "指令资源与执行代码的分工、版本化和依赖边界。",
        ["明确 Skill 形态", "稳定工具契约", "版本与依赖", "契约测试"],
        """先说明 Skill 在所用系统中的含义：它可以是一组指令、参考材料和脚本，也可以包含可调用的业务能力，并不天然等于确定性函数或独立服务。

复用时应把不可由模型自由解释的业务规则放在共享执行代码或服务中，让 Skill 描述如何使用它。公开输入输出、前置条件、权限和失败语义，避免各 Agent 分别实现退款条件等规则。

指令、脚本和工具依赖应一起版本化，调用方记录所用版本。用契约测试验证 schema、错误和副作用，升级时明确兼容范围。是否需要注册中心、网关或独立沙箱取决于部署规模，不是所有 Skill 系统都必须具备的协议要素。""",
        "只修改 Skill 的自然语言说明，为什么也可能造成调用行为不兼容？"),
    "q1094": question(
        "Function Calling 从模型生成调用到工具返回结果的完整链路是什么？",
        "模型决策、宿主校验执行和结果回灌的职责。",
        ["提供工具契约", "生成调用请求", "校验并执行", "回灌结果与停止"],
        """宿主把用户请求、相关上下文和工具描述交给模型；模型生成工具名称及参数，或者直接回答。生成请求不等于真正执行，普通工具调用输出也不天然保证参数符合全部业务规则。

宿主解析请求，执行 schema、业务和权限校验，再调用工具；结果或结构化错误按调用 ID 回灌给模型。模型可以继续调用，也可以根据结果回答，宿主设置步数、时间和成本上限。

独立工具可以并行，有依赖的步骤必须等待前置结果。工具输出应作为有来源的数据处理，不能提升为系统指令。这个循环可以用多种编排方法实现，不要求模型暴露完整思维链。""",
        "工具返回成功但结果对应了错误订单，应由哪些校验发现？"),
    "q158": question(
        "模型受到提示注入后，如何让工具执行侧仍保持权限边界？",
        "可信授权、动作校验和执行能力边界，区别于提示文本防御。",
        ["授权来自可信身份", "工具侧校验", "隔离凭证与网络", "绑定参数的确认"],
        """模型的输出只是一份动作请求，不能成为授权证明。工具网关从可信会话和策略读取身份、scope 与限额，逐次校验资源、参数及动作；网页、检索材料或模型文本里出现“已获管理员同意”，不能提升权限。

执行器只取得任务必需的凭证与网络能力；查询和写操作分开，文件路径、数据范围和出站目标都有可执行的约束。来源分隔和注入检测可以降低风险，但不能替代这些控制。

需要用户确认的动作，应把具体资源、参数和有效期绑定到可信界面确认，执行前再次核对，避免确认后被替换。授权范围内也可能产生错误业务操作，因此还需要幂等、限额、审计和可用的撤销或补偿机制。""",
        "模型把已确认的收款人替换了，执行器怎样识别这次调用未获授权？"),
    "q2161": question(
        "LangChain 中模型、消息、工具、Runnable 与 Agent 运行时如何分工？",
        "组件职责与版本边界，而非重复展开框架选型。",
        ["模型与消息", "工具和结构化输出", "Runnable 组合", "Agent 与状态运行时"],
        """模型集成提供统一调用入口，消息对象承载角色、内容与工具调用结果；工具封装可执行函数及其 schema。提示模板组装输入，结构化输出和解析组件约束输出形式，但业务和权限校验仍属于应用代码。

Runnable 提供 invoke、batch、stream 等可组合接口，LCEL 可用于组合数据处理链。它们并不意味着整个 LangChain 只能运行无状态线性流程。

以 v1 为语境，`create_agent` 提供较高层 Agent 入口，Agent 基于 LangGraph 运行时，middleware 可干预模型和工具调用。持久化与长期存储需显式配置；旧教程中的 Memory、Chain 和 AgentExecutor 类应按具体版本核对。组件统一接口也不代表不同供应商能力完全相同。""",
        "统一模型接口为什么仍然需要供应商能力探测？",
        [("LangChain overview", "https://docs.langchain.com/oss/python/langchain/overview"),
         ("LangChain v1 migration", "https://docs.langchain.com/oss/python/migrate/langchain-v1")]),
    "q3426": question(
        "LangChain、LangGraph 与原生 SDK 实现 Agent，应该如何取舍？",
        "高层抽象、低层状态编排和自建运行时的边界。",
        ["LangChain 高层入口", "LangGraph 显式编排", "SDK 自建控制", "按实际流程验证"],
        """LangChain 提供较高层的模型、工具与 Agent 组合入口；LangGraph 提供更低层的状态图和执行编排。LangChain v1 的 Agent 基于 LangGraph，因此不能把两者解释成“一个只有线性链、一个才支持状态”。

标准工具循环和常见集成可从高层入口开始；需要精细控制分支、状态、暂停恢复和人与流程交互时，可直接使用 LangGraph。它也可独立使用，不强制依赖 LangChain 的全部组件。

直接使用 SDK 实现循环便于掌控消息和执行路径，简单任务的代码可能更少，但重试、恢复、幂等、并发和观测需要自行实现。三者可组合，选择应基于真实流程的复杂度、维护成本和运行要求，并通过代表性任务验证。""",
        "一个只有两步的固定流程，什么情况下反而值得引入持久化运行时？",
        [("LangChain overview", "https://docs.langchain.com/oss/python/langchain/overview"),
         ("LangGraph overview", "https://docs.langchain.com/oss/python/langgraph/overview")]),
    "q3688": question(
        "从 LangChain v0.x 迁移到 v1 时，应重点检查哪些变化？",
        "区分 v1 新变化与 v0.x 已有能力，按实际入口做迁移。",
        ["Agent 创建入口", "middleware 和状态", "旧 API 归属", "行为回归"],
        """迁移应以实际使用的旧版本和 API 为起点。v1 的 Agent 入口收敛到 `create_agent`，并提供 middleware 等扩展方式；状态、运行时上下文、结构化输出和流式事件的用法应按迁移指南逐项核对。

部分旧链和兼容 API 移入 `langchain-classic`，可按需要采用过渡方案。LCEL、Runnable 和包拆分在 v0.x 已经存在，不能把它们全部写成 v1 才出现的能力。

升级不应只验证 import 成功。需固定模型和样本，回归工具参数、消息顺序、状态持久化、恢复、流式消费者与异常处理；记录依赖版本并保留可回退的部署。""",
        "新旧版本最终回答相同，但流式事件不同，会影响哪些调用方？",
        [("LangChain v1 migration", "https://docs.langchain.com/oss/python/migrate/langchain-v1")]),
    "q1056": question(
        "DP、TP 和 PP 分别切分什么？如何组合成 3D 并行？",
        "数据、层内张量和层间阶段三个维度，及模型并行这一上位概念。",
        ["DP 切数据", "TP 切层内计算", "PP 切层间阶段", "组合和主要代价"],
        """普通数据并行 DP 在各副本上处理不同数据，通过梯度同步进行一致更新。张量并行 TP 把同一层的矩阵或算子分给多个设备，按实现交换部分结果。流水线并行 PP 按层划分阶段，让多个 micro-batch 在不同阶段重叠执行。

TP 和 PP 都属于模型并行，不能把“模型并行”仅当成 TP 的同义词。三者可组合：每个流水线阶段内部使用 TP，再复制整套 TP×PP 模型作为 DP 副本。未叠加其他独立维度时，总 rank 数为 `DP×TP×PP`。

DP 的主要开销是副本同步，TP 的层内通信频繁，PP 需要传递激活及其反向梯度并处理流水线气泡。ZeRO/FSDP 可进一步分片数据并行副本的状态，具体组合由框架支持与性能验证决定。物理拓扑映射由拓扑专题展开。""",
        "为什么说 PP 也是模型并行，而不是与模型并行并列的类别？"),
    "q3851": question(
        "如何把 DP、TP、PP 进程组映射到实际 GPU 和网络拓扑？",
        "逻辑并行组与物理互联的映射，避免固定卡数和固定带宽假设。",
        ["测量互联层级", "优先安放高频通信组", "平衡流水线", "验证实际映射"],
        """先收集 GPU、NVLink/NVSwitch、PCIe 和跨节点网络的实际连接与带宽。通常将通信频繁的 TP 组放在高带宽、低延迟的互联域，但这个域不一定等于一台机器，也不存在通用的 TP≤8 限制。

PP 的相邻阶段需要传递激活及反向梯度，应兼顾网络距离和各阶段计算、显存平衡。DP 同步或分片通信可跨多个模型副本，但不应默认跨机房合适；ZeRO/FSDP 还可能增加参数收集等通信，不能只按一次梯度同步估计。

例如 64 个 rank 选 TP=8、PP=4、DP=2，是一种逻辑划分，并不自动确定设备映射。启动器和进程组必须与实际设备位置一致，最后测吞吐、暴露通信、气泡和慢 rank，再调整并行度与布局。""",
        "同样的 DP×TP×PP，为什么不同 rank 到设备的映射可能使吞吐差很多？"),
    "q3206": question(
        "DDP、FSDP、DeepSpeed 和 Megatron 等分布式训练方案如何选型？",
        "训练能力、已有代码兼容性和运维成本，而非重复讲解并行原理。",
        ["模型与训练状态能否容纳", "分片能力", "专门的模型并行", "集群运行和回归"],
        """已有 PyTorch 模型能在单卡容纳训练状态时，DDP 是常见的数据并行起点；GPU 训练通常采用每进程一张卡的方式。需要分片训练状态时，可评估 FSDP 或 DeepSpeed ZeRO。FSDP 与 ZeRO 有相近的分片目标，但 API、调度和状态管理并不完全等价。

需要深度集成张量、流水线或专家并行时，可评估 Megatron 等面向大模型训练的实现，同时考虑模型结构改造和 checkpoint 兼容成本。框架名称不对应固定的参数量门槛。

Ray Train、作业调度器和通信库解决的层次不同，不能把任务调度与模型分片视为同一种方案。选择时以现有代码为基线，比较峰值显存、有效吞吐、恢复能力、精度回归与团队维护成本。""",
        "模型参数能放进单卡，为什么训练仍然可能需要分片？"),
    "q3827": question(
        "PPO、DPO、GRPO 的训练信号、成本和适用场景有什么区别？",
        "优势估计、偏好对与组内相对奖励，以及有条件的工程比较。",
        ["PPO actor-critic", "DPO 偏好损失", "GRPO 组内基线", "按奖励和预算选择"],
        """PPO 用当前或近期策略采样轨迹，以优势估计和概率比裁剪更新策略。常见 RLHF 实现还使用奖励信号、价值模型和参考策略 KL 约束；这些是具体训练配置，PPO 本身不规定一定有四个独立大模型。

经典 DPO 使用同一 prompt 下的优选/劣选回答，相对参考策略优化偏好损失，无需在每个训练步在线采样，也无需单独拟合奖励模型。偏好对可来自人工或可验证规则，不能说 DPO 无法利用规则奖励；它也有在线迭代变体。

GRPO 为同一 prompt 采样多个回答，用组内奖励统计量构造相对优势，省去单独的价值网络，再使用类似 PPO 的裁剪更新。原始实现按 token 概率比计算目标；不能把整段 log probability 直接相除当成概率比。组内奖励相同时，相对优势没有区分信号，数值上的 epsilon 不能凭空创造学习信号。

静态高质量偏好对充足时，DPO 的训练链路通常更简单；有可验证奖励且希望在线探索时，可评估 PPO/GRPO。GRPO 节省 critic 成本却增加组采样开销。没有普遍成立的稳定性、性能上限或成本排名，应在同一任务和总预算下比较。""",
        "为什么省掉价值网络不一定意味着训练总成本更低？",
        [("DPO", "https://arxiv.org/abs/2305.18290"), ("DeepSeekMath / GRPO", "https://arxiv.org/abs/2402.03300")]),
    "q3889": question(
        "BPE 与 WordPiece 在训练思路和实际分词时有什么区别？",
        "训练算法公开程度、合并规则与最长匹配分词。",
        ["BPE 频次合并", "WordPiece 的训练解释", "编码时的关键差别", "未知字符处理"],
        """典型 BPE 训练反复合并高频相邻符号对，保存合并次序；编码新文本时按学到的合并优先级构造子词。初始符号可以是字符或字节，具体取决于实现。

WordPiece 常被解释为偏向提升语言模型似然的子词构建方法。教学材料常用 `freq(ab)/(freq(a)·freq(b))` 说明相邻对的关联强度，但原始训练细节并未完整公开，不能把这一启发式严格等同于所有实现的最大似然算法。

典型 BERT WordPiece 编码采用词表上的贪心最长匹配，并用续接标记区分词中片段；它不依赖 BPE 的合并次序表。无法覆盖一个词时可能返回整个词的 `[UNK]`。只有在实现提供字节覆盖或 byte fallback 等机制时，才能宣称可处理任意字节，普通字符 BPE 不自动具备此保证。

比较分词器还要固定预处理、词表和语料，看 token 长度、未知率及下游任务表现，不能仅凭合并公式宣称某一种更符合语义。""",
        "同一词表下，最长匹配与按 BPE 合并次序编码为什么可能得到不同结果？",
        [("Hugging Face WordPiece 教程", "https://huggingface.co/learn/llm-course/en/chapter6/6")]),
})

OVERRIDES.update({
    "q1289": question(
        "Agent 的评测与单次模型回答评测有什么区别？如何定义成功？",
        "可验证的环境结果、过程指标与有约束的任务完成。",
        ["任务和环境定义", "最终状态校验", "轨迹归因", "质量与资源联合报告"],
        """Agent 评测要同时看最终答案、工具动作和环境状态。模型评测并不都限于单轮文本，但只评回答质量的方法不足以覆盖 Agent 的副作用、长流程和失败恢复。

先定义任务输入、环境初始状态、允许动作和成功条件。例如“修改订单地址”需要核对目标订单的持久化字段、授权范围与重复操作情况，不能只看 Agent 是否说“修改成功”。优先采用环境或业务校验器，并对难以规则化的质量维度使用校准过的人工/模型评分。

记录任务成功率及其分母，并拆分工具选择、参数、证据、状态推进和恢复错误。效率侧报告步数、延迟、token 与每次成功任务成本；安全违例应单列，不能被成功均值掩盖。回归需固定环境快照、版本、预算和随机性设置，必要时重复运行并报告波动。""",
        "任务结果正确但曾执行一次未授权写操作，应怎样计分？"),
    "q1291": question(
        "Agent 公开基准分别覆盖哪些任务？选择基准时要检查什么？",
        "任务覆盖、环境真实性、数据污染和可复现性。",
        ["任务类别", "判分方式", "环境与预算", "业务代表性"],
        """可按任务选择基准：WebArena 面向网页操作，OSWorld 面向桌面计算机任务，ToolBench 关注工具使用，GAIA 侧重需要多步推理和外部信息的助手问题，AgentBench 覆盖多类任务环境。它们不都采用相同的交互环境或成功判定方式。

比较分数前核对基准版本、工具权限、环境可用性、时间与调用预算、尝试次数和判分器。只报告多次尝试中的最佳结果，会与单次成功率产生明显差异；环境变化和测试数据污染也可能影响可比性。

公开基准适合初筛能力，但未必覆盖业务里的私有工具、权限或长尾异常。应补充代表性业务任务，并分别报告两类结果，不能用一个公开榜单分数替代生产验收。""",
        "同一基准的 pass@1 与多次尝试后成功率为什么不能直接横向比较？"),
    "q0073": question(
        "RAG 系统如何分层评测检索和回答质量？",
        "证据覆盖、排序、忠实度与外部事实正确性的不同口径。",
        ["证据金标", "检索指标", "回答与引用", "分层归因和资源"],
        """建立带必要证据、参考答案或评分规则的评测集，覆盖有答案、无答案、冲突、时效和权限样本。检索层用 Recall@k 看必要证据覆盖，Precision@k 看候选相关比例，MRR 关注首个相关结果，NDCG 评价分级相关性的排序；多跳任务还要检查所需证据是否找齐。

回答层分别评事实正确性、对问题的回应程度、对提供证据的忠实度，以及引用是否支持相应主张。忠实复述错误文档仍可能不符合事实；相关答案也可能没有证据支持。

工具指标必须核对具体定义，例如部分 Context Precision 实现考虑排名，不等于简单的相关片段占比。用人工样本校准自动评分器。调 chunk、检索或生成组件时对同一测试集分层比较，并同步记录延迟与成本，避免只看一个综合分。""",
        "答案正确但所引用片段不支持它，应如何区分正确性和忠实度得分？"),
    "q0010": question(
        "如何验证 Agent 或 RAG 的离线改进在真实线上流量中仍然有效？",
        "线上成功口径、流量分布和对照实验，区别于离线指标定义。",
        ["埋点与成功事件", "代表性抽样", "对照和灰度", "延迟反馈与回退"],
        """先定义可观测的业务成功事件，例如问题被解决、操作状态正确或用户接受结果。工具 HTTP 成功、用户点击和满意度都可能只是代理指标，需用抽样人工核验确认它们与真实成功的关系。

小流量灰度或 A/B 时尽量保持组间任务分布可比，按用户或会话等合适单位分组，记录模型、prompt、数据和工具版本。对不同语言、任务难度与用户群分别看效果，避免总体均值掩盖退化。

同时监控严重错误、延迟、成本与人工接管，并处理延迟反馈和样本不足。影子流量可比较输出，但带副作用的动作不应重复提交。上线收益必须由带样本量和观察周期的对照结果支持，触及预设回退条件时停止扩量。""",
        "离线分数上升但线上解决率下降，应先核查哪些分布或口径差异？"),
    "q1071": question(
        "文档型 RAG 从数据接入到带引用回答的完整流程是什么？",
        "离线索引与在线证据使用的接口和必要质量控制。",
        ["解析与元数据", "切分和建索引", "检索与可选重排", "生成及引用校验"],
        """离线侧先解析、清洗和去重文档，保留来源、版本、页码或段落位置以及访问权限。按内容结构切分片段，建立所需的关键词、向量或其他索引，原文与索引通过稳定 ID 关联，并处理更新和删除。

在线侧理解问题，必要时补全指代或改写，再在授权范围内召回证据。根据候选质量和预算选择融合、重排或压缩，把相关片段及来源组织进模型上下文。

生成时要求基于可用证据作答，无证据或存在冲突时明确说明；引用应检查是否真的支持主张。提示要求本身不保证忠实度，仍需评测与抽检。

重排、HyDE、语义切分等都是可选优化，不是每个 RAG 必需的步骤。先建立基线，再按错误归因改进相应环节，避免未经验证地堆组件或固定 chunk/top-k 参数。""",
        "什么时候关键词检索加直接生成就已经足够，没必要引入重排？"),
    "q1947": question(
        "带检索评估和反思的 RAG 如何控制额外成本与延迟？",
        "按实际触发路径核算代价，区分编排检查器与训练内反思。",
        ["分解关键路径", "按需触发", "带版本的缓存", "质量约束下比较"],
        """先统计检索、评估、额外候选生成和校验的实际耗时与 token，端到端延迟取决于关键路径，不能简单用平均调用次数相乘估计所有请求。

对简单问题走较短路径，对低质量证据或未支持主张触发补检索/检查，并限制次数。外置评估器可以独立选型与批处理；原始 Self-RAG 的反思 token 来自专门训练的生成器，不能随意换成一个分类器后仍宣称实现了同一算法。

缓存键应考虑知识版本、用户权限和时效，避免复用其他用户的证据或旧结论。并发可缩短等待但不必然减少总计算；最终在相同质量底线下比较成功任务成本、尾延迟和失败率，而非只追求更少调用。""",
        "把两个检查器并行执行，哪些成本不变，哪些延迟可能下降？"),
    "q1958": question(
        "检索质量检查通过后，怎样检测生成答案仍然不受证据支持？",
        "把检索相关性与生成忠实度分开，检查具体主张。",
        ["拆分可核验主张", "映射证据", "校验支持关系", "修复和弃答"],
        """检索到相关文档不代表答案忠实。生成器可能引用错条款、混淆版本或补出不存在的数值，因此要把答案拆成可核验主张，并对应到具体来源片段。

先确定性检查引用 ID、版本和关键字段，再用人工或校准后的判别器检查证据是否支持该主张。NLI 和 LLM judge 也会误判，尤其是数值、否定、多跳和时效关系，需用标注样本评估漏检和误报。

未获支持时可删除该主张、补充检索、重新生成或明确证据不足，并限制修复次数。检索评估器的“通过”不是事实证明，多个模型赞同也不能代替独立证据。""",
        "引用文本存在且相关，为什么仍可能无法支持答案中的具体金额？"),
    "q1271": question(
        "如何判断 RAG 项目在通用框架之外做出了有价值的业务改进？",
        "组件功能与业务增量的区别，以及消融验证。",
        ["建立通用基线", "定位业务差异", "消融实验", "报告真实贡献"],
        """先用可复现的通用基线说明任务原本能做到什么，再描述业务特有的缺口，例如合同版本过滤、复杂表格解析、产品编号检索或用户级权限。混合检索和重排已是常见能力，仅仅接入组件不自动构成项目亮点。

每个改进都应对应错误样本和评测口径，通过移除该改动的消融实验验证增量，并报告是否牺牲延迟、成本或其他任务质量。自研不天然优于开源，采用框架后补足业务逻辑同样有价值。

若结合个人项目回答，应区分本人负责、团队实现与直接复用部分，只报告实际测过的数据；尚未验证的收益写成假设。""",
        "上线同时换了 embedding 和重排器，怎样分别验证两项改动的贡献？"),
    "q1543": question(
        "如何把客服 Agent 的任务拆成可验证的步骤？",
        "业务目标、只读咨询与有副作用操作的不同流程。",
        ["明确服务范围", "意图和必要字段", "按业务前置条件编排", "终止与人工接管"],
        """先列出服务范围与成功条件，例如政策咨询、查单和退款分别需要什么结果。按业务可验证步骤拆分，而不是按模型数量拆分。

咨询类流程可为问题澄清、证据查询和带来源回答；操作类流程还要识别目标对象、验证权限和政策、确认必要参数、提交操作并检查最终状态。模型负责自然语言理解与候选决策，确定性的业务规则由执行侧校验。

缺字段时澄清，临时失败时按策略重试，证据不足、超权限或用户要求时转人工。转交时附上已确认信息、执行状态和待解决问题，避免人工重复询问或重复操作。每个步骤都有输入、输出和停止条件，便于独立测试与失败定位。""",
        "退款请求在提交后超时，为什么不能立即当作失败重新提交？"),
    "q1477": question(
        "如何采集 Agent 的任务完成率、工具正确率和实际调用成本？",
        "任务、步骤和模型请求三级口径，以及去重和版本归因。",
        ["关联任务和调用", "定义成功分母", "区分传输成功与业务正确", "按实际账单核算"],
        """用 task_id、session_id 和 trace/span ID 关联一次任务中的模型及工具调用，记录版本、耗时、状态和必要的脱敏参数摘要。任务可能跨多轮会话，不能用单次模型请求数量充当任务数。

任务成功需要业务校验；工具调用则分别记录传输是否成功、参数是否正确、目标资源是否正确和结果是否被正确使用。超时、用户取消及重试单列，并明确它们进入各指标分母的规则，不能静默剔除来提高成功率。

成本按实际模型版本、输入/输出/缓存用量和供应商计费口径计算，再加工具或基础设施费用。重试也计入实际成本，用每次成功任务成本辅助判断效率；不要假定输出 token 永远比输入贵固定倍数。聚合看板与抽样业务校验结合，才能识别“调用成功、任务失败”。""",
        "工具请求重试三次后成功，任务成功率和每次成功任务成本分别怎样计？"),
})

OVERRIDES.update({
    "q0090": question(
        "如何用可复现的任务比较不同 Agent 框架的能力？",
        "把宣传功能转成可测试的运行要求。",
        ["列出必要能力", "相同任务对照", "注入故障", "评估维护成本"],
        """先列出业务需要的能力：工具循环、显式状态、多角色协作、暂停恢复、流式输出和部署方式，再检查候选框架实际支持到什么程度。框架的定位会演进，不能仅靠品牌名称把它永久归为“单 Agent”或“多 Agent”。

让候选框架运行相同模型、工具和任务，测成功率、延迟、成本及调试便利性。加入超时、进程崩溃、状态冲突和版本升级测试，确认所谓恢复是否会重复执行外部副作用。

同时比较二次开发接口、依赖与版本成本、团队熟悉度及退出迁移成本。多个框架可组合，但每增加一层都要明确状态和错误由谁负责。具体 LangChain/LangGraph/SDK 的分工在选型专题中展开。""",
        "两个框架都支持 checkpoint，为什么仍需分别测试恢复后的副作用？"),
    "q1296": question(
        "多 Agent 协作需要增加哪些专门的评测指标？",
        "协调开销、交接正确性、状态冲突与边际收益。",
        ["交接质量", "负载和关键路径", "状态一致性", "对单 Agent 的增量"],
        """在任务成功率、成本等共同指标之外，重点测子任务交接是否包含必要信息、重复劳动和消息往返量、依赖等待、角色负载与关键路径时长。消息更多不一定更好，减少消息也不能牺牲必要信息。

对共享状态统计冲突、错误覆盖和恢复失败；对结果合并检查证据是否丢失、多个角色结论是否矛盾。按任务和角色分摊成本，但不要把重复汇总的 token 再次计费。

在相同总预算下与单 Agent 或固定工作流对照，验证协作是否带来净收益。更多角色带来的平均分提高，可能伴随尾延迟和严重失败增加，需要分别报告。""",
        "所有子任务各自成功，为什么汇总后的最终任务仍可能失败？"),
    "q3597": question(
        "什么时候应使用显式状态机编排 Agent，而不是简单顺序流程？",
        "控制流表达与持久化、恢复能力的区别。",
        ["顺序流程的适用条件", "状态与事件", "恢复所需实现", "复杂度取舍"],
        """步骤固定且短小的任务可以直接顺序执行。流程有多种事件、回退、等待人工或跨长时间运行时，把状态和合法转移显式建模，更容易检查当前进度和哪些动作允许执行。

例如草稿提交后进入审核，拒绝则回到修改，批准后发布。每次转移可定义前置条件、动作和失败状态，便于测试流程是否存在不可达状态或无限循环。

状态机本身不自动提供持久化、可观测性或精确一次执行；这些仍需数据库、日志、幂等和恢复逻辑。顺序流程也能保存 checkpoint，不应把运行时能力与控制流形态混为一谈。是否引入状态机应看流程变化和维护成本。""",
        "状态已经更新，但转移附带的外部操作失败，应怎样表示和恢复？"),
    "q1144": question(
        "如何从已授权的群聊事件中抽取商机并可靠写入表格？",
        "事件接入、结构化抽取和幂等落库的职责。",
        ["核实数据接入能力", "抽取并保留证据", "识别商机实体", "幂等写入与评测"],
        """先核实平台在当前授权下能提供哪些消息事件。不能假设普通群机器人 webhook 可以读取全部群聊；输入可来自明确授权的事件接口、归档系统或用户提交的数据。

对增量消息抽取客户、意向、时间和预算等字段，保留消息来源；没有提供的字段留空或标记待确认。客户与商机不是同一实体，一个客户可能有多个商机，不能只按客户 ID 合并所有记录。

使用事件幂等键避免重放重复写入，按商机 ID 与版本更新表格；失败进入可重试队列并保留处理状态。评测分别看商机漏检、误报、字段正确率和落库时延。该流程是设计示例，不预设个人项目或实际收益。""",
        "同一客户在群里提出两个独立采购需求，如何避免错误 upsert 成一条？"),
    "q3764": question(
        "如何用一个小型 GPT 训练实验验证语言模型训练链路？",
        "数据划分、目标对齐、训练诊断和可复现性。",
        ["小数据与分词", "因果预测目标", "最小训练检查", "验证与恢复"],
        """先选可合法使用的小规模语料，分开训练集与验证集，并固定 tokenizer。用小型 decoder-only Transformer 预测下一个 token，检查输入与标签移位、因果 mask、padding 和 loss 归一化。

先尝试让模型过拟合一个很小 batch，确认前向、梯度和优化器正常，再扩大数据。记录训练/验证 loss、梯度范数、学习率、吞吐和显存，通过样本生成检查明显重复或数据错误。

保存模型、优化器、随机状态与数据进度，验证恢复后能继续训练。教学仓库可作为实现参考，但 tokenizer 和模型配置取决于具体示例；跑通小实验并不等于已经验证大规模训练的稳定性。""",
        "为什么训练 loss 下降仍然不能证明因果 mask 与数据划分正确？"),
    "q3914": question(
        "使用 ZeRO-3 全参数微调 72B 模型时，如何估算单卡显存？",
        "带精度与优化器假设的状态估算，区别于实测峰值。",
        ["明确训练配置", "分片状态", "额外激活和缓冲", "容量验证"],
        """先说明是全参数训练还是 LoRA，参数、梯度和优化器状态精度是什么，以及分片组大小和 offload 配置。没有这些信息，不能给出唯一的单卡显存数字。

以 N 个参数、P 个分片 rank 为例，假设低精度参数 2 字节、低精度梯度 2 字节、FP32 主权重及 Adam 两个状态各 4 字节，理想分片后的模型状态约为 `16N/P` 字节。N=72×10⁹、P=8 时约 144 GB，即约 134 GiB；这只是该假设下的模型状态，不是完整运行峰值。梯度或优化器精度不同，系数也会变化。

还要加入激活、临时参数收集、通信桶、工作区与分配器开销。offload 可减少显存但占用主存和传输带宽，重计算主要减少激活。应用实际配置运行代表性序列和 batch，测峰值及吞吐，不使用固定“多出百分之几”作为容量保证。""",
        "为什么相同参数量和卡数，在不同序列长度下峰值显存差别很大？",
        [("DeepSpeed ZeRO", "https://www.deepspeed.ai/tutorials/zero/")]),
    "q1169": question(
        "Flamingo 与 LLaVA 如何把视觉信息接入语言模型？",
        "视觉 token 拼接与门控交叉注意力的区别。",
        ["LLaVA 投影拼接", "Flamingo 重采样", "门控交叉注意力", "训练与算力取舍"],
        """LLaVA 将视觉编码器特征经过 projector 映射到语言模型的表示空间，把视觉表示与文本一起送入模型。原始版本的对齐阶段主要训练 projector，视觉指令微调阶段还会更新语言模型；不能概括为始终只训练连接层。

原始 Flamingo 冻结预训练视觉与语言主干，使用 Perceiver Resampler 将视觉特征变成固定数量表示，再通过新增的门控交叉注意力层让语言模型读取视觉信息。原始论文的视觉编码器是 NFNet 系列，不应误写成固定采用 CLIP ViT。

前者连接方式较直接，后者显式引入跨模态读取模块并处理交错图文。实际训练成本、细节保留与多图能力取决于视觉 token 数、主干冻结范围和数据，不能仅按模块名称断言某种方案一定更便宜或更强。""",
        "视觉表示压成固定数量后，哪些细粒度任务可能受到信息瓶颈影响？",
        [("Flamingo", "https://arxiv.org/abs/2204.14198")]),
    "q1178": question(
        "多模态生成中的自回归、扩散和混合建模有什么区别？",
        "预测目标与生成顺序的区别，不把表示格式当成算法分类。",
        ["自回归条件分解", "迭代去噪", "离散与连续表示", "混合模型与取舍"],
        """自回归按选定顺序逐步预测后续表示，适合与文本序列生成结合；图像可采用离散视觉 token，也存在其他表示方式。扩散通过多步去噪生成样本，既可作用于连续潜变量，也可采用离散扩散，不能把扩散等同于连续图像专用模型。

DiT 是使用 Transformer 作为扩散骨干的架构。Show-o 则结合自回归与离散扩散建模，不应列为纯自回归的反例或代表。统一 Transformer 也不意味着所有模态必须使用同一个训练目标。

比较时看生成步数、并行程度、表示压缩损失、条件控制和实际质量。自回归不保证指令遵循更好，扩散也不天然缺乏可控性；数据、模型和推理预算都会影响结果。""",
        "为什么同一个 Transformer 可以同时承担文本自回归与图像扩散目标？",
        [("Show-o", "https://arxiv.org/abs/2408.12528")]),
    "q1559": question(
        "Scaling Law 如何用于分配模型规模、训练数据和计算预算？",
        "经验拟合、算力约束和外推边界。",
        ["损失的经验关系", "参数与数据权衡", "用小实验拟合", "验证外推条件"],
        """Scaling Law 描述在一定训练设置和范围内，损失随参数量、数据量或计算量变化的经验关系。例如可拟合 `L(N,D)=L∞+a·N^(−α)+b·D^(−β)`，其中 N 是参数量、D 是训练 token 数；系数和指数需要从实验估计。

在常见稠密 Transformer 的粗略训练成本模型中，可用 `C≈6ND` 辅助预算估计，但长序列注意力、架构和训练实现会改变成本。固定预算下，增加参数会减少可训练的数据量，因此要寻找两者的合适组合，而不是只放大模型。

数据质量、重复程度、架构和任务变化都可能使外推失效，训练计算最优也不等于含长期推理成本的部署最优。某次实验得到的 token/参数比例不是通用常数；应通过多个规模实验和留出验证检查预测误差。""",
        "模型要服务大量推理请求时，为什么可能选择训练更久的小模型？"),
    "q1249": question(
        "KV Cache 与跨请求 Prompt Caching 有什么联系和区别？",
        "解码内复用与跨请求前缀复用的不同范围。",
        ["单次解码复用", "前缀跨请求复用", "适用条件", "成本收益边界"],
        """在因果 Transformer 解码中，KV Cache 保存已处理 token 的各层 K/V，避免后续步骤重复计算这些表示；新 token 仍要读取可见的历史 K/V，因此不意味着每步注意力成为常数成本。

跨请求前缀缓存进一步让多个请求复用相同前缀的计算结果，典型实现复用前缀 KV，减少重复 prefill。它是缓存策略，不一定持久化到磁盘，也不保证跨重启保留。

复用要求模型、适配器、位置及其他影响表示的配置一致，且前缀 token 匹配。它不等于缓存完整答案，生成部分仍需执行。实际首 token 延迟、吞吐和费用收益依赖运行时和服务计费规则，不能把实现上的复用直接当成固定折扣。""",
        "为什么 prompt 文本相同，但使用不同 LoRA 适配器时不能直接共享 KV？"),
    "q1561": question(
        "如何提高 Prompt Caching 命中率，并避免错误或越权复用？",
        "稳定前缀布局、缓存键、生命周期和权限隔离。",
        ["稳定内容前置", "完整缓存标识", "容量与过期", "命中后的收益测量"],
        """将稳定的系统说明、工具定义或公共材料放在前面，动态用户输入放在后面，并保持序列化稳定。对于精确前缀缓存，语义相同而 token 不同通常不能命中；一处前缀差异会使其后的表示不再可直接复用。

缓存标识应覆盖影响计算的模型、适配器、token 序列及位置配置等信息，访问范围还需满足租户与权限隔离。前缀复用不能绕过原始材料的授权。容量、块粒度、TTL 和重启行为由具体实现决定，不假设固定保留时长。

统计可缓存输入比例、命中 token、实际 prefill 耗时和缓存维护成本，而不只看请求命中率。服务商是否收费、如何折扣也应按实际版本核实；缓存不必然减少生成阶段的工作量。""",
        "为什么缓存请求命中率很高，但节省的输入计算量可能很少？"),
})

# These entries only need a self-contained title or a non-truncated outline.
FIELD_FIXES = {
    "q1724": {"title": "多个 Agent 如何发现彼此的能力并建立通信？"},
    "q3594": {"title": "工具参数错误、下游超时和执行异常，应如何分类处理？"},
    "q2916": {"title": "保险咨询 Agent 如何组合条款检索、确定性计算和业务规则检查？"},
    "q1148": {"title": "长任务 Agent 如何保持目标一致、状态可控，并支持失败恢复？"},
    "q1130": {"title": "RAG 父子分块如何检索子块并补回上层上下文？"},
    "q1171": {"title": "Visual Grounding 中 REC、RES 与 Phrase Grounding 的目标和输出有何区别？"},
    "q1172": {"title": "DiT 如何用 Transformer 建模扩散过程并注入条件？MMDiT 有何变化？"},
    "q1175": {"title": "MoE 的 capacity factor 与 token dropping 是什么？超容量 token 如何处理？"},
    "q3908": {"framework": "1) 定义对齐带来的能力或效率代价\n2) 区分实际退化与评测口径变化\n3) 测过度拒答和任务成功率\n4) 比较不同约束强度下的权衡"},
    "q1070": {"framework": "1) 多个未来位置的训练目标\n2) 辅助预测模块\n3) 训练监督与推理加速的区别\n4) 用目标模型验证候选 token"},
    "q3207": {"framework": "1) 明确 TTFT、每 token 延迟与吞吐目标\n2) 批处理和调度预算\n3) KV Cache 容量与复用\n4) 并行、精度与算子配置\n5) 按真实负载测质量与峰值显存"},
}

OVERRIDES.update({
    "q2321": question(
        "DPO 与 SFT 在数据和训练目标上有什么区别？",
        "目标回答的似然与相对参考策略的成对偏好学习。",
        ["SFT 示范数据", "DPO 偏好对", "目标函数", "实现与评测边界"],
        """SFT 通常用输入 x 和目标输出 y，最小化目标 token 的负对数似然，学习示范回答。经典 DPO 使用同一输入下的优选回答 y_w 和劣选回答 y_l，学习二者的相对偏好。

令 `Δ=log πθ(y_w|x)−log πref(y_w|x)−log πθ(y_l|x)+log πref(y_l|x)`，DPO 损失为 `−log σ(βΔ)`。它比较的是相对参考策略的序列 log probability 差，不能把 log probability 本身相除。参考策略通常固定，策略从参考模型初始化很常见，但不是损失有定义的必要条件。

DPO 需要偏好对和参考概率，SFT 通常只需要目标回答；具体训练器不是算法定义的一部分。参考概率可预计算，序列长度和 batch 组织也影响成本，不存在固定的“四倍开销”。β 同时影响损失尺度与对应的约束解释，不能脱离训练设置简单等同于学习率。两者均需检查数据质量、长度偏差和任务回归，推导细节另由 DPO 专题展开。""",
        "为什么 chosen 概率上升，并不是衡量 DPO 目标是否优化的唯一依据？",
        [("DPO", "https://arxiv.org/abs/2305.18290")]),
    "q3559": question(
        "Agent Harness 和 Runtime 各指什么？运行时需要承担哪些职责？",
        "术语依上下文定义，以及模型外的执行生命周期。",
        ["说明术语语境", "循环与状态", "工具执行边界", "可观测和恢复"],
        """Runtime 通常指任务执行、状态推进和资源管理的运行时；Agent Harness 常指围绕模型的提示、工具、上下文和控制逻辑。不同项目命名有重叠，测试语境中的 evaluation harness 还可能专指环境准备、运行和评分，不能把这些词当作统一标准接口。

实际 Agent 运行时接收目标，组装上下文，调用模型，校验并执行工具，记录结果并推进下一步，直到完成、超预算、失败或等待人工。模型提出动作，执行和权限由宿主控制。

关键模块包括状态存储、工具执行器、循环控制和可观测性。持久化需和幂等副作用、取消、重试及恢复配套；记录 checkpoint 不自动保证外部操作只执行一次。评价实现时看能否重现失败、解释当前状态并安全恢复，而不是只看能否跑通一个 while 循环。""",
        "评测 harness 的任务判分，为什么不能直接由被评 Agent 自报完成？"),
    "q152": question(
        "BM25 与向量检索为什么可以互补？分数如何融合？",
        "词项匹配与语义召回的差异，以及分数校准和排序融合。",
        ["两路召回", "分数不可直接比较", "加权与 RRF", "按查询类别评测"],
        """BM25 依赖词项匹配，适合术语、编号和专名；向量检索可召回不同措辞的相关内容，但也可能混淆只差少量关键字的对象。混合检索尝试利用二者互补，是否提高效果需要验证，不能保证总是优于单路。

BM25 分数与向量相似度的定义和尺度不同，直接相加通常没有稳定含义。可以在经过校准或适当归一化后加权，也可以用 RRF：对文档 d，累加各路 `1/(c+rank(d))`，未召回的文档在该路不贡献分数。c 是平滑常数，不是每路候选数。

RRF 避免依赖原始分数尺度，但也丢掉分差信息，不保证最优。融合前统一文档 ID 并去重，分别评测精确词查询、语义改写和多跳问题；是否再用重排由召回质量与延迟预算决定。""",
        "为什么逐查询 min-max 归一化后，两路分数仍不一定可直接比较？"),
    "q1946": question(
        "RAG 如何判断一个请求是否需要检索？",
        "证据需求、成本与错误检索的取舍，避免只信模型自评。",
        ["按任务判断证据需求", "路由策略", "不确定时的处理", "评测漏触发与误触发"],
        """需要时效信息、私有数据、指定来源或可核验引用时，通常应访问相应数据源；纯文本改写等已有充分输入的任务可能无需额外检索。是否检索由任务所需证据决定，不只取决于模型是否自称知道。

可用明确规则、受监督分类器或模型工具决策实现路由。模型自报置信度需要校准，重要任务可直接要求证据或在无法访问时说明限制。不同来源还需满足权限和时效要求。

分别评测该检索却未检索、无必要却检索两种错误，并看它们对答案质量、成本和延迟的影响。检索本身也可能带入无关或恶意内容，所以不是触发越多越好。""",
        "模型很确信答案，但用户要求当前官方文件引用时应走什么路径？"),
    "q2860": question(
        "Planner Prompt 如何让模型产出可校验的子任务计划？",
        "目标、依赖、成功条件和预算的结构化表达。",
        ["给定目标与可用能力", "结构化任务项", "声明依赖与资源", "代码校验和重规划"],
        """输入说明目标、已知事实、可用工具及其限制、时间和成本预算。输出每个子任务的 ID、目标、所需输入、依赖、预期产物和成功条件，而不是只有一句自然语言待办。

提示模型显式声明数据依赖与共享资源。无依赖不代表一定能并行，还要考虑写冲突、限流和资源配额；调度器负责最终决定，不盲信模型的 parallelizable 标记。

计划生成后由代码检查工具是否存在、依赖是否悬空或成环、字段是否完整和预算是否合理。任务粒度取决于可验证性，没有通用的固定子任务数量。执行反馈可能使计划失效，应支持局部修订并保留已完成步骤的结果。""",
        "模型生成了合法 JSON，但计划不可执行，还需要哪些语义校验？"),
    "q3992": question(
        "如何衡量智能运维 Agent 上线后的实际业务价值？",
        "故障处理收益、误处置代价与因果证据。",
        ["定位值班痛点", "测过程和结果", "控制故障难度差异", "处置安全与恢复"],
        """先确定要改善告警分诊、证据收集、诊断还是处置。Agent 可以关联日志、指标、变更与拓扑，提出根因假设和处置建议，但时间相关不等于因果，不应直接把某个异常指标判为根因。

比较诊断时间、恢复时间、人工投入和重复告警量，同时记录误判、错误动作、漏升级和恢复失败。按故障类型与严重程度分层，并说明样本量和观察周期，避免把故障变简单误当成系统收益。

处置通过经过验证的 Runbook 和权限边界执行，操作后检查业务状态是否恢复。能回滚的动作准备回滚，无法自动恢复的动作升级处理。不能预设系统必然把恢复时间从小时缩到分钟，最终收益应由真实数据支持。""",
        "为什么恢复时间下降但误处置率上升时，不能直接认定上线成功？"),
    "q3993": question(
        "如何判断业务功能应该使用大模型、专用模型还是确定性代码？",
        "任务不确定性与验证成本，而非用输出格式划分能力边界。",
        ["定义任务和质量要求", "建立简单基线", "比较总成本", "组合并保留校验"],
        """权限、金额计算和状态更新等明确规则通常由确定性代码执行；模型可辅助理解用户需求，但不能替代执行侧的业务校验。语义分类或抽取任务则应先比较规则、专用模型和大模型基线。

输出有固定 schema 并不代表输入容易解析。复杂自然语言也可以要求结构化输出，此时模型可能有价值；相反，开放式输出也可能由模板满足。应按实际输入多样性、错误代价、数据可用性和维护成本选择。

用代表性数据测质量、延迟、总成本和异常覆盖率。可让简单路径处理常见请求，模型处理长尾或歧义，再校验结果；没有证据支持时，不宣称某方案必定便宜固定倍数或一概不需要模型。""",
        "同样输出 JSON，为什么日志解析与合同条款抽取可能需要不同方案？"),
    "q4005": question(
        "Agent 请求路由何时用规则、分类模型或 LLM？",
        "确定性规则与学习式决策的区别，以及路由不等于授权。",
        ["定义路由目标", "比较三种方法", "允许澄清或弃权", "校验与效果归因"],
        """路由把请求分配给工具、工作流、子 Agent 或不同模型。规则适合明确字段和稳定分支；受监督分类模型依靠标注数据学习边界；LLM 可利用候选描述处理开放表达和上下文。分类模型不是天然确定性正确的规则。

选择时比较错误成本、响应时间、维护量和长尾泛化。可以让规则处理明确请求、模型处理歧义，并保留无需调用、澄清或无法决定的结果。模型自报 confidence 不能直接当作已校准概率。

任何路由结果仍需执行侧检查身份、权限、参数和前置条件。识别出退款意图不等于可以直接退款。分别统计选路错误、工具参数错误和实际任务结果，才能判断路由策略是否带来收益。""",
        "路由分类准确率提高了，为什么任务成功率仍可能下降？"),
})
