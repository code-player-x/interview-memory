# 项目复查问题修复记录（2026-09-26）

本轮根据用户“直接帮我修复这些问题”的确认，处理审查报告中的数据不同步、C1—C9、题答错误和重复边界。保留此前所有本地修改；随后根据反馈补充背题页分享按钮布局调整。

## 修复结果

| 审查项 | 已完成处理 | 验证 |
| --- | --- | --- |
| C1 代码块被再次包裹 | 启发式缩进识别跳过已有围栏 | 全库已有代码围栏保真检查；浏览器展示完整 Python 循环，复制数据与显示相等 |
| C2 空答误报题不存在 | 空回答保留题目对象和参考答案，独立于题目缺失 | 临时库同时覆盖未答/缺题，不新增活动统计 |
| C3 新一轮重复上一题 | 按输入顺序排除最后有效题 ID，修正剩余题数 | 逆序题号、无效 ID 和正常未完成轮次测试 |
| C4 判题坏响应 500 | Agent/LLM 统一校验结果类型，不合格进入 pending；JSON 只去外围代码围栏 | 单题/整卷接口，以及字典、列表、null 等非法字段；保留正文中的 json 单词 |
| C5 嵌入实验误判 | 校验数量、维度、有限值、非零范数，并稳定归一化 | 正交通过、坍缩失败；零向量/NaN/Inf/缺项/变长/空维度/非法文本/null 全部失败 |
| C6 同批重复导入 | 两个批量入口增加批次内已见标题集合 | 同批仅入库一次，再次导入全部跳过 |
| C7 遗忘后状态不一致 | 复习回退时同步恢复 reviewing | 通过标记掌握与遗忘两个正式 API 验证状态转换 |
| C8 非法待办输入 | 标题去首尾空格且非空，优先级保留 UI 的 1—4，有效日期，专注时长 1—1440 分钟 | 新建/更新拒绝非法数据；合法低优先级与正常专注不受影响 |
| C9 标题被空格切断 | 不再以空格判定标题结束；修复 7 道已持久化的碎片标题 | 中英文混合标题测试、源题对应检查与重复生成零变化 |

主要代码：[前端](/Users/didi/aiStudy/interview-memory/frontend/app.js)、[后端接口](/Users/didi/aiStudy/interview-memory/backend/app.py)、[判题适配](/Users/didi/aiStudy/interview-memory/backend/judge.py)、[待办校验](/Users/didi/aiStudy/interview-memory/backend/todo_routes.py)、[嵌入实验](/Users/didi/aiStudy/interview-memory/crawler/scripts/test_embed_discriminative.py)。

## 题库修正与去重

在[可重放的审校规则](/Users/didi/aiStudy/interview-memory/crawler/questions_v2/reviewed_audit_fixes.py)中记录了 15 道题的完整修正，再由原有清洗流程生成 authored 和 full_v2，防止下次生成恢复旧答案。

- q2061：可选调用的短路、非函数值、对象为空三种边界。
- q3108：只讲在线推理吞吐、批处理、KV cache、并行与调度，不再用训练优化代替作答。
- q0940：Service、EndpointSlice 控制器与数据平面的职责。
- q2483：区分 Teleport、v-html、手动 DOM 和 scoped 选择器。
- q2409：区分原生可运行代码与需要转换/优化的工程产物。
- q0782：区分 wire type 与完整 schema，并说明多消息流的 framing。
- q0761 / q2834：基础 HTTP/HTTPS 区别与 TLS 1.3 握手/0-RTT 风险分开，不再重复大段基础定义。
- q2697、q1965、q1647：补成自包含问题，同时修正拖拽排序、工作流条件分支和阻塞式输入的相关表述。
- q0397、q3683、q2437、q0860：承接下面四组重复内容，按独立考点重写。

| 合并移出的题号 | 保留题号 | 处理 |
| --- | --- | --- |
| q0398 | q0397 | Redis 扩缩容共用一份渐进式 rehash 解释 |
| q3684 | q3683 | RunnableSequence 定义、组合与接口边界 |
| q3288 | q2437 | rem 与视口单位的参照系和选择 |
| q0861 | q0860 | 烧绳计时作为一道母题及追问，明确预先燃烧的条件 |

原始题目仍保存在 [review_archive](/Users/didi/aiStudy/interview-memory/crawler/questions_v2/review_archive/) 中，并记录合并目标。源题库从 2,586 道变为 **2,582 道、26 个领域**。这些合并只针对本轮确认的重复组，不表示全库其他潜在语义重复已经穷尽。

## 网页数据库同步与恢复

新增 [sync_questions_v2.py](/Users/didi/aiStudy/interview-memory/scripts/sync_questions_v2.py)：默认只读预览；写入时先备份，使用单个事务原位更新；源 ID 映射使后续改名仍保留应用题目 ID。仅处理 questions_v2 平台，保留其他平台条目、图片和所有学习表。删除旧题需显式 --prune，任何待删题有关联记录就拒绝执行。数据库触发器会在旧应用或 --replace 删除题目时同步解除源 ID 映射，避免 SQLite 复用数字 ID 后误覆盖另一道题。

正式库执行前确认学习记录表为空，并短暂停止服务，执行同步后重新启动。实际结果：

- 保留并关联 2,299 个已有题目 ID，其中更新 1,316 条的内容或元数据。
- 新增 283 条当前源题，清理 338 条不再匹配当前源题库的旧条目；其中包含旧题面/改名版本，不能解释为 338 道都是错误题。
- 同步后共 2,582 道、26 个分类；题目标题唯一。
- 逐字段核对源题与数据库，题面、答案、分类、标签、考点和难度差异为 0。
- 所有非题目业务表与备份逐表比较一致；SQLite quick_check 为 ok。
- 再次预览：更新 0、新增 0、清理 0、映射更新 0。

完整备份：[before-corpus-sync-knjydu4j.db](/Users/didi/aiStudy/interview-memory/data/backups/before-corpus-sync-knjydu4j.db)。本次清理的旧数据库条目可以从此备份恢复，合并源题另有 JSONL 归档。

回退时应先停应用，保留当前数据库与 WAL，再恢复备份到另一文件并通过 DATABASE_URL 切换；不要直接覆盖仍被服务打开的数据库。日常同步方法已写入 [README](/Users/didi/aiStudy/interview-memory/README.md)。当前同步脚本仅支持本地 SQLite，不宣称支持其他数据库的迁移。

## 验证结果与边界

- 分享按钮并入题目标签栏右侧，移除独立统计栏及其上下边框；窄屏自然换行，保留原有复制链接逻辑和键盘焦点样式。浏览器确认 384px 视口下没有横向溢出，也没有分享专用空行；新增回归覆盖首题和翻题后的分享题号。
- 全部 pytest：**96 passed**，比修复前增加 38 个测试用例。
- 流程回归：**31 通过，0 失败**。
- 题库 dry-run、Python 编译、前后台 JavaScript 语法、pip check、git diff --check 全部通过。
- 清洗/生成再次运行：authored、full_v2、review_archive 没有文件变化。
- 同步测试覆盖只读预览、备份内容、保留 ID/笔记/进度、源题改名、拒绝危险删除、歧义匹配、失败事务回滚及重复执行。
- 浏览器确认了新的题目总数/分类，搜索并打开修正后的题目，完整代码显示、复制按钮及复制载荷一致，无页面横向溢出。
- 页面保留全空试卷提交拦截。验收该提示时浏览器控制连接超时，未完成这一步之后的 UI 复盘；未作答与缺题的区分已通过临时数据库接口测试。验收未写入正式作答、笔记、错题或热力图记录。

应用已在 [本地 8000 端口](http://127.0.0.1:8000) 重新启动，HTTP 200。

NumPy 已可用；本轮修复和验证的是嵌入脚本的判定逻辑。Ollama 服务/模型仍未配置，真实嵌入模型效果尚未验证。外部 LLM、SMTP、对象存储联调及全库每句话的语义正确性，也不在本次通过的断言中。
